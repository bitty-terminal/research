在 Bitty 中，IPC 的设计对插件系统来说可以带来哪些帮助呢？

## IPC 对 Bitty 插件系统的价值其实很大

如果 Bitty 的目标是「终端核心 + Lua 插件 + Panel + Agent + 外部服务」，那么 IPC 可以成为 **Core 和扩展世界之间的第二条扩展边界**。

可以把整体理解成：

```text
                   ┌───────────────┐
                   │  bitty-core   │
                   │ terminal / PTY│
                   │ panel / render│
                   └───────┬───────┘
                           │
             ┌─────────────┴─────────────┐
             │                           │
        In-Process                    IPC Boundary
          Lua API                         │
             │             ┌─────────────┼─────────────┐
             │             │             │             │
      lightweight       bitty-ai     external       other bitty
       plugins          process       plugins        instances
```

这里最关键的是：**Lua Plugin API 和 IPC 不应该互相替代，而是解决不同层次的问题。**

### 1. 插件不再必须运行在 Bitty 进程内部

现在如果插件都是 Lua：

```text
bitty
 ├─ core
 ├─ Lua VM
 ├─ plugin A
 ├─ plugin B
 └─ plugin C
```

插件和 Core 本质上还是同一个进程。

IPC 出现之后，可以有：

```text
bitty
  │
  ├── Unix Socket
  │
  ├── plugin-git
  ├── bitty-ai
  ├── bitty-mail
  └── custom daemon
```

于是插件可以用：

* Rust
* Python
* Go
* TypeScript
* Java
* Shell

甚至根本不需要遵守 Bitty 的 Lua runtime。

这会让 Bitty 从：

> Lua 可扩展终端

变成：

> 有统一控制协议的 terminal platform

这是非常明显的一次能力升级。

---

## 2. 特别适合「重量级插件」

Lua 插件适合：

```text
keybinding
theme
statusline
panel behavior
command
event hook
small UI
```

但有些东西放 Lua 里面并不合适，比如你之前讨论的 `bitty-ai`。

例如：

```text
bitty-ai
├── provider
├── agent runtime
├── MCP
├── embedding
├── memory
├── sqlite
├── network
└── context management
```

如果全部塞进 Bitty Core：

```text
terminal emulator
+ HTTP client
+ TLS
+ SQLite
+ AI SDK
+ embedding
+ MCP
...
```

Bitty Core 很快就膨胀了。

IPC 可以让它变成：

```text
bitty-core
    │
    │ IPC
    ▼
bitty-ai daemon
```

Core 甚至不需要知道 OpenAI/Anthropic/MCP 是什么。

它只知道：

```text
agent.spawn
agent.send
agent.cancel

panel.attach
panel.output
panel.close
```

因此你之前「bitty-ai 的 network dependency 应该独立」这个思路，和 IPC 是完全契合的。

---

## 3. IPC 可以把 Panel 变成真正的公共抽象

这一点可能是 Bitty 最值得利用 IPC 的地方。

因为 Bitty 的 Panel 本身已经不仅仅是 terminal tab/window 了。

它可以逐渐抽象成：

```text
Panel
├── terminal
├── floating
├── overlay
├── headless
├── AI
├── dashboard
└── custom UI
```

有 IPC 后，外部程序就可以：

```text
create_panel()
write_panel()
focus_panel()
move_panel()
resize_panel()
close_panel()
```

例如：

```text
bitty-msg panel create \
    --mode floating \
    --title "Git Status"
```

或者：

```json
{
  "method": "panel.create",
  "params": {
    "mode": "floating",
    "title": "Agent"
  }
}
```

于是插件不用直接碰内部 Rust struct。

只需要操作公共协议。

这会变成：

```text
Plugin
   │
   ▼
Bitty IPC Protocol
   │
   ▼
Panel Manager
   │
   ├── Tiled Panel
   ├── Floating Panel
   └── Headless Panel
```

---

## 4. 对 Agent 尤其重要

你之前 Bitty 的设计里有一个很重要的特点：

> Agent 和 Panel 生命周期不是绑定的。

Agent 可以：

```text
Agent A
  ↓
Headless Panel #17
  ↓
任务完成部分工作
  ↓
离开 Panel #17
  ↓
进入 Panel #23
```

Panel #17 又可以继续存在。

IPC 天然适合这种 architecture：

```text
                    ┌────────────┐
                    │ bitty-ai   │
                    │ Agent A    │
                    └─────┬──────┘
                          │ IPC
                          ▼
┌─────────────────────────────────────┐
│               Bitty                 │
│                                     │
│ Panel #1  user                      │
│ Panel #2  user                      │
│ Panel #17 headless                  │
│ Panel #23 headless                  │
└─────────────────────────────────────┘
```

Agent 不需要：

```rust
&mut Panel
```

而是：

```text
panel.list
panel.inspect
panel.attach
panel.detach
panel.send_input
panel.read_output
```

这种模型比把 Agent 嵌进 terminal core 干净很多。

---

# 5. 插件之间也可以通信

IPC 不一定只是：

```text
plugin → core
```

还可以变成：

```text
plugin A
   ↓
Bitty IPC Bus
   ↓
plugin B
```

例如：

```text
Git Plugin
   ↓
git.branch.changed
   ↓
Statusline Plugin
```

或者：

```text
Panel History Plugin
   ↓
command.completed
   ↓
AI Plugin
```

再比如：

```text
bitty-ai
   ↓
agent.status.changed
   ↓
Dashboard Plugin
```

因此 Bitty 可以逐渐出现一个 event bus：

```text
panel.created
panel.closed
panel.focused

command.started
command.finished

cwd.changed
env.changed

agent.started
agent.finished

workspace.changed
```

插件：

```lua
bitty.on("panel.focused", function(event)
    ...
end)
```

外部进程：

```text
subscribe("panel.focused")
```

两边甚至可以共享同一套事件语义。

---

# 6. Lua API 和 IPC API 可以统一语义

这个设计我非常建议。

不要搞：

```text
Lua API       IPC API
完全两套系统
```

而应该尽量：

```text
             Bitty Capability API
                     │
            ┌────────┴────────┐
            │                 │
         Lua binding       IPC binding
```

例如核心能力：

```text
panel.list
panel.create
panel.focus
panel.close

workspace.list
workspace.switch

terminal.send_input
terminal.read_history

command.run

notification.send
```

Lua：

```lua
bitty.panel.create({
    mode = "floating"
})
```

IPC：

```json
{
  "method": "panel.create",
  "params": {
    "mode": "floating"
  }
}
```

CLI：

```bash
bitty msg panel create --mode floating
```

最终三个东西：

```text
Lua Plugin API
IPC API
CLI control API
```

最好只是同一个 capability model 的三个 frontend。

这会极大降低未来维护成本。

---

# 7. IPC 可以天然形成权限系统

这一点对第三方插件很重要。

例如插件 manifest：

```toml
[permissions]
panel.read = true
panel.create = true
terminal.input = false
filesystem.read = false
network = false
agent.control = false
```

连接 IPC 时：

```text
Plugin
   │
   │ authenticate
   ▼
Capability Token
   │
   ▼
Bitty IPC Server
```

Core 根据 capability 判断：

```text
panel:list            ✓
panel:create          ✓
terminal:send-input   ✗
agent:control         ✗
```

以后甚至可以：

```text
bitty plugin permissions foo
```

显示：

```text
foo

✓ panel.read
✓ panel.create
✓ notification
✗ terminal.input
✗ filesystem
✗ agent.control
```

这比让第三方插件直接拿到整个 Lua Core API 更容易做 security boundary。

---

# 8. Crash Isolation

这是 out-of-process plugin 最大的现实优势之一。

如果：

```text
Python AI Plugin
```

崩溃：

```text
plugin crashed
```

Bitty 本身仍然存在。

如果插件都运行在同一个进程：

```text
Bitty
├── Rust Core
└── Plugin runtime
```

plugin runtime 的严重 bug 可能影响整个 terminal。

IPC：

```text
Bitty Core        Plugin
    │               │
    │               X crash
    │
    └── alive
```

Bitty 甚至可以：

```text
plugin process exited
restart?
disable?
show log?
```

---

# 9. IPC 会让调试能力非常强

例如以后：

```bash
bitty msg panel list
```

得到：

```text
ID   TYPE       CWD              OWNER
1    terminal   ~/bitty          user
2    terminal   ~/capglyph       user
7    headless   ~/bitty          agent:coder
9    floating   -                plugin:git
```

再比如：

```bash
bitty msg tree
```

```text
Workspace 1
├── Panel 1
│   └── shell zsh
├── Panel 2
│   └── nvim
└── Panel 7 [headless]
    └── cargo test
```

甚至：

```bash
bitty msg events
```

```text
12:34:01 panel.focused #2
12:34:04 command.started cargo test
12:34:08 command.finished exit=0
12:34:10 agent.attached #7
```

这对于你以后做 Bitty DevTools 会非常有价值。

---

# 10. 还能控制已经运行的 Bitty

这也是 IPC 很经典的用途。

类似：

```bash
hyprctl
```

控制运行中的 Hyprland：

```bash
hyprctl dispatch workspace 2
```

Bitty 可以：

```bash
bittyctl panel focus 3
bittyctl workspace switch 2
bittyctl panel float 5
bittyctl command run "cargo test"
```

甚至：

```bash
bittyctl agent ask \
  --panel 3 \
  "解释刚才的错误"
```

所以 `bittyctl` 本质上不需要实现 Bitty 功能。

它只是：

```text
CLI
 ↓
IPC client
 ↓
Bitty
```

这一点和 Bitty 从 window manager 借鉴设计哲学也非常契合。

---

# 11. 甚至可以支持多 Bitty instance

例如：

```text
/run/user/1000/bitty/
├── instance-1.sock
├── instance-2.sock
└── instance-3.sock
```

然后：

```bash
bittyctl instances
```

```text
1  ~/Projects/bitty
2  ~/Projects/vectojs
3  ~/Projects/capglyph
```

插件或 AI 可以指定：

```text
instance → workspace → panel
```

形成清晰的 address：

```text
bitty://instance/1/workspace/2/panel/7
```

以后做跨窗口甚至跨 instance agent 控制都会很好用。

---

## 12. 但不要把所有插件都做成 IPC

这里需要明确划线。

### Lua Plugin

适合：

```text
keybinding
theme
render hook
UI decoration
statusline
layout behavior
small commands
event handler
```

原因是：

```text
低延迟
调用频繁
和 UI/Core 联系紧密
```

例如每帧都：

```text
IPC → plugin → IPC
```

显然不合理。

---

### IPC Plugin

适合：

```text
AI
Git daemon
language tooling
indexer
sync
database
network service
large computation
external application integration
```

特点：

```text
生命周期独立
可能 crash
依赖复杂
可能使用其他语言
需要网络/数据库
调用粒度较粗
```

---

# 我比较推荐 Bitty 最终形成三层扩展模型

```text
┌─────────────────────────────────────────────┐
│                 bitty-core                  │
│                                             │
│ PTY / VT / Render / Input / Panel / Layout  │
└──────────────┬──────────────────┬───────────┘
               │                  │
          Lua Plugin API       IPC API
               │                  │
      ┌────────┴───────┐    ┌─────┴─────────────┐
      │                │    │                   │
   Theme           UI Plugin bitty-ai       Services
   Layout          Status    MCP            Python
   Keymap          Pet       Agent          Rust/Go
```

再往上：

```text
                   Capability Layer
                         │
       ┌─────────────────┼────────────────┐
       │                 │                │
    Lua API           IPC API          CLI API
       │                 │                │
 bitty.xxx()          socket          bittyctl
```

这可能是最重要的一点：

> **不要把 IPC 仅仅理解为“进程间发送消息”。**

对于 Bitty，它更适合被设计成：

**Bitty Capability Protocol**。

也就是规定外部世界能够对 Bitty 做什么：

```text
Panel
Workspace
Terminal
Command
Agent
Plugin
Notification
Event
Clipboard
History
```

而 Lua、`bittyctl`、`bitty-ai`、第三方 daemon 都只是这个 capability model 的不同消费者。

如果这个层一开始定义得好，之后 `bitty-ai`、headless Panel、插件市场、DevTools、甚至远程控制都会自然地建立在同一套基础设施上，而不是每加入一个新功能就再造一套通信机制。

