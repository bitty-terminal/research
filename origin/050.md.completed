现在统一改成 **`.wheel/` 比 `.bitty/` 更正确**。因为随着架构逐渐明确，`.bitty/` 和 `.wheel/` 实际上已经代表两个完全不同的作用域：

```text
Bitty  = Terminal / Runtime Platform
Wheel  = Agent Harness
```

因此项目里的 AI Agent 行为不应该伪装成 Bitty 本身的项目配置。

我会明确分成：

```text
.agents/     → 生态兼容层 / Capability Definition
.wheel/      → Wheel 原生 Harness Control Plane
```

这两个目录应该长期共存，而且职责不要重叠。

## `.agents/`：描述“项目有什么能力”

`.agents/` 最适合继续作为 Wheel 的 interoperable layer。

例如：

```text
.agents/
├── skills/
│   ├── use-carryctx/
│   │   └── SKILL.md
│   ├── bitty-development/
│   │   └── SKILL.md
│   └── rust-development/
│       └── SKILL.md
│
└── mcp/
    └── ...
```

Wheel 对它的态度应该是：

> **我支持并读取这个已有生态，但不会把 Wheel 自己的高级能力塞进去污染这个目录。**

这样最大的好处是项目不是 Wheel-exclusive。

比如一个项目：

```text
project/
├── .agents/
│   └── skills/
│       └── project-development/
│
├── .wheel/
│
└── src/
```

换到：

```text
Claude Code
Codex
OpenCode
其他支持 .agents 的 Harness
```

`.agents/` 里的东西仍然具有迁移价值。

而 `.wheel/` 则是：

> **如果你使用 Wheel，我还能提供更多 Harness-level behavior。**

这种边界非常干净。

---

# `.wheel/` 应该描述“Wheel 应该怎样工作”

这才是关键区别。

`.agents/`：

```text
What capabilities exist?
```

`.wheel/`：

```text
How should Wheel behave?
```

所以 `.wheel/` 可以管的事情非常多：

```text
.wheel/
├── init.lua
│
├── commands/
├── tools/
├── agents/
├── context/
├── policies/
├── hooks/
└── profiles/
```

不过实现上我反而建议 **只有 `init.lua` 是固定入口**。

其他目录只是普通 Lua modules：

```lua
-- .wheel/init.lua

local wheel = require("wheel")

wheel.setup({
    ...
})

require("commands.review")
require("policies.context")
require("tools.project")
```

也就是说不要规定：

> `.wheel/tools/` 必须存在。

保持 Lua/Neovim 风格：

```text
.wheel/init.lua
```

是唯一 contract。

剩下用户自由组织。

---

# `.wheel/` 最重要的第一类功能：Capability Filtering

这个恰好解决我们之前讨论的 Skill Hell / MCP Hell。

例如项目里有：

```text
.agents/skills/
├── frontend-design
├── rust
├── use-carryctx
├── deployment
└── security-audit
```

但是这个项目不希望 Agent 自动启用：

```text
deployment
security-audit
```

那么：

```lua
wheel.skills.disable({
    "deployment",
    "security-audit",
})
```

甚至：

```lua
wheel.skills.enable({
    "rust",
    "use-carryctx",
})
```

意味着 whitelist。

MCP 同理：

```lua
wheel.mcp.disable("postgres-production")

wheel.mcp.enable({
    "github",
    "docs",
})
```

所以整个关系不是：

```text
.wheel replaces .agents
```

而是：

```text
.agents
   │
   │ discovery
   ▼
Capability Registry
   │
   │ .wheel policy
   ▼
Resolved Capabilities
   │
   ▼
Context Compiler
```

这非常重要。

---

# `.wheel/` 第二类功能：Context Policy

这里就可以直接控制我们刚讨论的 Context Compiler。

例如：

```lua
wheel.context.setup({
    budget = {
        soft = "64k",
        hard = "128k",
    },

    code = {
        prefer_symbols = true,
        max_file_lines = 400,
    },

    logs = {
        retain_raw = false,
        summarize = true,
    },
})
```

甚至：

```lua
wheel.context.pin({
    "ARCHITECTURE.md",
    "docs/invariants.md",
})
```

或者：

```lua
wheel.context.exclude({
    "target/**",
    "dist/**",
    "vendor/**",
})
```

但这里最好保持：

> **用户描述 policy，而不是编写 Context Compiler 算法。**

不要让配置变成：

```lua
on_every_prompt(function(ctx)
    -- 1000 行用户自己操纵 prompt
end)
```

否则缓存稳定性、安全性、可调试性都会迅速恶化。

应该提供高层 API。

---

# `.wheel/` 第三类：Rules / Constraints

这个可以比传统 `AGENTS.md` 强很多。

传统 Agent 配置可能写：

```md
Never modify generated files.
Always run cargo fmt.
Do not add dependencies.
```

Wheel 可以直接结构化：

```lua
wheel.rules.add({
    id = "no-generated-files",

    match = {
        "generated/**",
    },

    action = "deny_write",
})
```

例如：

```lua
wheel.rules.add({
    id = "dependency-policy",

    action = "require_confirmation",

    when = {
        add_dependency = true,
    },
})
```

或者：

```lua
wheel.rules.add({
    id = "rust-verification",

    when = {
        files = "**/*.rs",
    },

    verify = {
        "cargo fmt --check",
        "cargo test",
    },
})
```

这比把所有规则写进 System Prompt 强得多。

因为：

```text
Policy Engine 能确定性执行的规则
```

就根本没必要浪费 Context 告诉 LLM：

> “请记得不要修改 generated/。”

---

# 这实际上能大幅缩短 Core Prompt

这是 `.wheel/` 非常重要的价值。

假设传统 Harness：

```text
System Prompt:

- don't touch generated files
- don't read .env
- don't add dependency
- run cargo fmt
- run cargo clippy
- use carryctx
- don't use deployment skill
- ...
```

Wheel 可以把这些全部下沉到：

```text
Policy Runtime
Capability Resolver
Verification Runtime
Permission Runtime
```

模型只需要知道：

```text
You operate under project policies enforced by Wheel.
```

这对于我们刚刚讨论的：

```text
Context quality
Prompt cache stability
Instruction conflicts
```

都有好处。

---

# `.wheel/` 第四类：Custom Commands

这个非常适合 Lua。

例如：

```lua
wheel.command("review", function(ctx)
    return ctx.agent:run({
        goal = "Review current changes",
        mode = "review",
    })
end)
```

于是：

```text
/review
```

可以是完全项目自定义的。

另一个项目可以定义：

```text
/release
/benchmark
/check-api
/update-snapshots
```

这比把所有 `/xxx` 都硬编码进 Wheel Core 好得多。

---

# `.wheel/` 第五类：Custom Tools

这个也非常适合项目级扩展。

例如 Bitty 项目自己有一个特殊工具：

```lua
wheel.tool({
    name = "bitty.inspect_protocol",

    description = "Inspect Bitty protocol definitions",

    input = {
        protocol = "string",
    },

    run = function(args)
        ...
    end,
})
```

但是这里我建议严格区分：

```text
Native Tool
Lua Tool
MCP Tool
Skill Script
```

Agent 看起来可以统一成为：

```text
Capability
```

但是 Wheel 内部一定要知道 provenance。

比如：

```text
tool://wheel/native/code.search
tool://project/lua/inspect_protocol
tool://mcp/github/get_issue
```

这样权限、安全、调试都容易做。

---

# `.wheel/` 第六类：Agent Definitions

例如：

```lua
wheel.agent("reviewer", {
    model = "reasoning",

    tools = {
        "code.*",
        "git.diff",
    },

    skills = {
        "rust",
    },

    permissions = {
        write = false,
    },
})
```

然后：

```lua
wheel.agent("researcher", {
    tools = {
        "web.*",
        "docs.*",
        "code.read",
    },

    permissions = {
        write = false,
        process = false,
    },
})
```

这比 System Prompt：

```text
You are a reviewer...
```

更加结构化。

也能让 Context Compiler 根据 Agent Profile 自动裁剪：

```text
Reviewer
→ 不加载 write tool

Researcher
→ 不加载 process tools
```

从而进一步降低 context/tool pollution。

---

# `.wheel/` 第七类：Verification

这一块应该非常强。

例如：

```lua
wheel.verify.on_change("**/*.rs", {
    "cargo fmt --check",
    "cargo clippy --all-targets",
})
```

测试：

```lua
wheel.verify.test({
    discover = "cargo test -- --list",
    targeted = true,
    full = "cargo test",
})
```

甚至可以声明：

```lua
wheel.verify.require("before_complete", {
    "format",
    "typecheck",
    "targeted-tests",
})
```

于是“任务是否完成”不是完全交给 LLM 判断。

而是：

```text
Agent:
I think I'm done.

Wheel:
Verification gate failed.

Agent:
继续修。
```

这是非常重要的 Harness 责任。

---

# `.wheel/` 第八类：Hooks

例如：

```lua
wheel.hook("task:start", function(task)
    ...
end)

wheel.hook("tool:before", function(call)
    ...
end)

wheel.hook("tool:after", function(result)
    ...
end)

wheel.hook("context:compile", function(meta)
    ...
end)

wheel.hook("agent:spawn", function(agent)
    ...
end)
```

不过这里要注意：

**普通项目配置和高级插件扩展最好分级。**

否则 hook 太强，很容易破坏所有 invariant。

可以类似：

```text
safe hooks
advanced hooks
unsafe hooks
```

或者通过 capability permissions 控制。

---

# 我特别建议：`.wheel/` 不保存运行时状态

例如不要：

```text
.wheel/
├── init.lua
├── history.db
├── sessions/
├── cache/
└── checkpoints/
```

这样会把：

```text
Version-controlled configuration
```

和：

```text
Mutable runtime data
```

混到一起。

我会保持：

```text
project/.wheel/
```

全部都是：

> **用户可以 commit 的项目配置。**

而实际 Runtime 数据：

```text
$XDG_STATE_HOME/wheel/projects/<project-id>/
```

或者：

```text
$XDG_DATA_HOME/wheel/...
```

缓存：

```text
$XDG_CACHE_HOME/wheel/...
```

如果需要 project-local runtime state，也应该明确是类似：

```text
.git/wheel/
```

这种不会进入仓库的位置。

---

# `.wheel/` 的 Lua 安全问题必须从第一天考虑

这个非常关键。

因为：

```text
.agents/skills/SKILL.md
```

主要还是 instruction/data。

而：

```text
.wheel/init.lua
```

是：

> **Executable Code**

所以 clone 一个陌生项目以后，Wheel **绝对不能无提示自动执行 `.wheel/init.lua`**。

否则：

```bash
git clone malicious-repo
cd malicious-repo
bitty
```

直接执行：

```lua
os.execute(...)
```

会成为非常严重的 supply-chain attack。

这里可以参考 `direnv` 的思想。

首次进入：

```text
.wheel/init.lua detected

Project Wheel configuration is not trusted.
```

用户：

```text
wheel trust
```

记录：

```text
project path
config hash
```

如果 `.wheel/init.lua` 修改：

```text
hash changed
→ trust invalidated
```

重新审核。

甚至：

```text
wheel trust --inspect
```

展示：

```text
requested capabilities:
- filesystem read
- process execution
- custom tools

network: no
external paths: no
```

这一块值得认真设计。

---

# 我甚至会把 `.wheel/` 的权限做成 capability sandbox

Lua 默认不要：

```lua
os.execute()
io.open()
```

随便访问系统。

而应该：

```lua
wheel.fs.read(...)
wheel.process.run(...)
wheel.tool(...)
```

然后 Wheel 知道这个项目配置申请了什么权限。

类似：

```lua
wheel.permissions({
    filesystem = {
        project = "read-write",
        outside_project = "deny",
    },

    process = true,

    network = false,
})
```

这样 `.wheel` 本身也可以成为一个可审计的 dependency。

---

# 最终目录关系我会定成这样

```text
bitty/
│
├── .agents/
│   ├── skills/
│   │   ├── bitty-development/
│   │   └── use-carryctx/
│   │
│   └── ...
│
├── .wheel/
│   ├── init.lua
│   │
│   ├── commands/
│   │   ├── review.lua
│   │   └── benchmark.lua
│   │
│   ├── tools/
│   │   └── protocol.lua
│   │
│   └── policies/
│       ├── context.lua
│       └── verification.lua
│
├── Cargo.toml
└── ...
```

加载过程则是：

```text
                 Project
                    │
         ┌──────────┴───────────┐
         ↓                      ↓
      .agents/                .wheel/
         │                      │
 Capability Discovery        Lua Config
         │                      │
         ▼                      ▼
     Capability            Wheel Policy
      Registry                 Engine
         │                      │
         └──────────┬───────────┘
                    ▼
            Capability Resolver
                    │
              ┌─────┴──────┐
              ↓            ↓
        enabled Skills    MCP/Tools
              │            │
              └─────┬──────┘
                    ▼
            Context Compiler
                    │
                    ▼
               Agent Runtime
```

## 这样边界会非常漂亮

我会直接把两者定义成：

> **`.agents/` = portable agent capabilities**
> **`.wheel/` = Wheel-native harness behavior**

因此 `.wheel` 不应该再复制：

```text
skills/
mcp/
```

而应该**引用、过滤、约束、组合** `.agents` 暴露出来的能力：

```lua
wheel.skills.disable(...)
wheel.skills.prefer(...)
wheel.skills.allow(...)

wheel.mcp.disable(...)
wheel.mcp.allow(...)
wheel.mcp.permissions(...)
```

这实际上非常符合 Wheel 整体哲学：

```text
.agents 给能力
Wheel 决定什么时候值得使用能力
.wheel 让项目作者定义这种决策边界
```

而且从命名上也终于彻底清晰了：以后看到 `.wheel/`，就知道这是 **Wheel Agent Harness 的项目级配置**；看到 Bitty，则仍然是终端平台本身，不会继续把 Terminal Core 和 AI Harness 的概念混在一起。
