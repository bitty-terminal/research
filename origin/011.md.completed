carryctx 和 ctxctl 应该只是 bitty 可以借鉴的，bitty 我觉得是会进化出属于自己的原生的 "carryctx" 的，不过可能很多东西都作为工具调用了吧，比如任务制定、任务依赖管理、工作分配等等都是可以变成内置工具了吧，而不是调用 carryctx 这个 cli 工具，因为 carryctx 本身的功能现在已经很好了，对应的仓库现在已经拆分成各个 crates 了，而且说不定一个项目目录会出现 .bitty 这种的配置目录呢？当然也会兼容 .agents 这种的目录吧。
确实这种架构的设计很好，我其实希望的是之后 bitty-ai 所做出来的 Agent 不止是个 Coding Agent，也应该可以像是 hermes-agent 这种不断进步的 agent，甚至更多类型的，因为这些都是可以自定义角色、权限、LLM 等等的，比如日常用什么、读代码用什么、编写代码用什么等等都是可以配置不同的。
我现在还是挺清楚 bitty 设计了 IPC，那么就得把这个好好利用起来，因为 AI Agent 是最能体现这个功能的，而且你可以了解下 lua 设计的原理有哪些 lua 插件系统做不了，哪些东西只能通过上游给提前做好，我记得 bitty 在很多方面设计的是很安全的
## 我也更倾向于这个方向

`CarryCtx` 和 `ctxctl` 对 Bitty 最有价值的地方，应该是**验证了一些抽象确实有用**，而不是让 Bitty 永久依赖这两个 CLI。

最终更合理的关系是：

```text
CarryCtx ─────┐
              ├── design references ──> Bitty native subsystems
ctxctl ───────┘
```

而不是：

```text
bitty-ai
  ├── shell out: carryctx ...
  └── shell out: ctxctl ...
```

尤其 Agent Runtime 已经在 Bitty 进程内部，很多东西再通过 CLI → parse stdout → 返回 Agent，反而绕了一圈。

---

# 1. Bitty 应该长出自己的原生 “CarryCtx”

我觉得甚至可以不把它理解为一个完整模块，而是拆成几个基础服务：

```text
bitty-ai-runtime
│
├── Task Service
│   ├── create
│   ├── assign
│   ├── depend
│   ├── block/unblock
│   ├── complete
│   └── query ready tasks
│
├── Agent Service
│   ├── spawn
│   ├── stop
│   ├── delegate
│   ├── message
│   └── inspect
│
├── Workspace Service
│   ├── worktree
│   ├── snapshot
│   ├── overlay
│   └── isolation
│
├── Context Service
│   ├── repository index
│   ├── symbol index
│   ├── memory
│   └── checkpoints
│
└── Evidence Store
    ├── command execution
    ├── diff
    ├── diagnostics
    └── agent artifacts
```

然后 Agent 看到的是 Tool：

```text
task.create
task.assign
task.list
task.ready
task.block
task.complete

agent.spawn
agent.delegate
agent.message

workspace.create
workspace.diff
workspace.merge

context.search
context.symbol
context.read
context.remember

exec.run
exec.inspect
```

这就非常自然。

**Tool 是对内部 Service 的 Agent-facing projection。**

以后 CLI 反而可以变成：

```text
bitty task list
bitty agent list
bitty workspace diff ...
```

CLI 和 AI Tool 共用下面的 Rust service，而不是 AI 调 CLI。

---

# 2. `.bitty/` 我认为很可能会出现，但不要拿它存所有东西

这里最好一开始区分：

### Portable project definition

可以进入 Git：

```text
.bitty/
├── project.toml
├── agents/
│   ├── architect.toml
│   ├── coder.toml
│   ├── reviewer.toml
│   └── researcher.toml
├── workflows/
├── prompts/
├── policies/
├── tools/
└── skills/
```

例如：

```toml
[agent.reviewer]
model = "reasoning"
prompt = "Review changes for correctness."

capabilities = [
    "repo.read",
    "repo.search",
    "git.diff",
    "diagnostics.read",
]

[agent.reviewer.workspace]
write = false

[agent.reviewer.network]
enabled = false
```

但是动态运行状态不要全扔 `.bitty/`：

```text
current task
agent session
execution logs
token statistics
runtime locks
SQLite DB
temporary overlays
```

否则 Git workspace 会很脏。

更合理的是：

```text
.bitty/                     # declarative / portable / git tracked

<git-dir>/bitty/            # repository-local runtime state
~/.local/share/bitty/       # global/user runtime state
~/.config/bitty/            # user config
```

这其实也吸收了 CarryCtx 已经验证过的一些经验。

而 `.agents/` 完全可以做兼容层：

```text
Project Discovery

.bitty/
  ↓ native highest fidelity

.agents/
  ↓ compatibility adapter

CLAUDE.md / AGENTS.md / ...
  ↓ contextual conventions
```

不要让 `.bitty` 和 `.agents` 变成两个 competing source of truth。

---

# 3. Agent 的模型也不应该和 Role 绑定死

你说“日常用什么、读代码用什么、编写代码用什么”，这个非常值得做。

甚至可以把：

```text
Role
Model
Capability
Tool
Context policy
```

完全正交化。

例如：

```toml
[models.fast]
provider = "..."
model = "fast-model"

[models.reasoning]
provider = "..."
model = "reasoning-model"

[models.code]
provider = "..."
model = "coding-model"

[models.private]
provider = "ollama"
model = "..."
```

角色：

```toml
[agents.architect]
model = "reasoning"

[agents.coder]
model = "code"

[agents.shell_assistant]
model = "fast"

[agents.private_assistant]
model = "private"
```

以后甚至可以做到 **within-agent model routing**：

```text
Architect Agent

普通状态汇报
    → fast model

阅读 architecture
    → reasoning model

生成 patch
    → coding model

敏感本地数据
    → local model
```

所以 Bitty 的 Agent 不应该只是：

> 一个 Prompt + 一个 LLM。

而应该是：

```text
Agent =
    Identity
  + Role
  + Policy
  + Capabilities
  + Model Routing
  + Memory
  + Workspace
  + Tools
  + Lifecycle
```

这样它自然就不限于 Coding Agent。

---

# 4. Hermes 式“不断成长”也可以做，但最好不是“让 Agent 自己改自己”

这里可以做得比传统 memory 更系统。

我会把“成长”拆成四种：

```text
Observation
    ↓
Memory

Repeated solution
    ↓
Recipe

Reusable procedure
    ↓
Skill

Stable behavior change
    ↓
Policy / Agent profile proposal
```

例如 Agent 做了三次 Rust workspace debugging 后发现：

```text
cargo check -p foo
→ rust-analyzer diagnostics
→ targeted test
```

通常最快。

它可以生成：

```text
skill://rust-targeted-debug
```

下一次直接复用。

但最好不是：

```text
Agent:
"I have modified my own security policy."
```

而是：

```text
Agent
 ↓
propose skill/policy
 ↓
Bitty policy engine
 ↓
validate / approve / version
 ↓
install
```

于是：

> **Agent 可以成长，但不能成长出宿主没有授予的 Capability。**

这是非常重要的 invariant。

---

# 5. Lua 在这里的位置其实非常清楚

Lua 官方本身就是为了 **embedded scripting** 设计的。宿主程序持有 `lua_State`，并决定给 Lua 暴露什么 C/native API；Lua 自身并不能凭空取得宿主内部能力。Lua userdata 也特别适合把宿主对象以受控 handle 的方式暴露给脚本。([Lua][1])

所以 Bitty 可以采用：

```text
             Rust trusted core
                    │
          capability-checked API
                    │
              Lua environment
                    │
          plugin / config / workflow
```

而绝对不是：

```text
Lua plugin
   ↓
arbitrary Bitty internals
```

WezTerm 实际上就是很好的参考：它会把 `Window`、`Pane`、`MuxWindow` 这样的 native object 作为 handle 暴露给 Lua；例如 `Window` 明确不能由 Lua 自己创建，而是由宿主通过 callback 交给 Lua。([WezTerm][2])

Bitty 很适合进一步把这个模型做得更严格。

---

# 6. Lua 非常适合做什么

Lua 层特别适合：

```text
Configuration
Keybindings

Panel composition
Panel lifecycle callbacks

Agent definitions
Role definitions

Workflow orchestration

Tool composition

Events
Hooks

Prompt templates

Status line
Commands

UI behavior

Task strategies

Model routing rules

Skill definitions
```

比如：

```lua
bitty.agent.register {
    name = "reviewer",

    model = "reasoning",

    capabilities = {
        "repo.read",
        "repo.search",
        "git.diff",
    },

    on_task = function(task)
        ...
    end,
}
```

或者：

```lua
bitty.on("command.finished", function(event)
    if event.status ~= "success" then
        bitty.panel.open("diagnostics")
    end
end)
```

非常适合。

---

# 7. 但有一类事情 Lua 永远做不了：宿主没有提供的能力

例如：

```lua
bitty.panel.create(...)
```

Lua 能不能调用？

取决于 Rust 有没有实现：

```rust
fn panel_create(...)
```

并暴露给 Lua。

所以 Lua 插件系统的上限其实就是：

> **Bitty 上游提供的 semantic primitives。**

这也是为什么 Bitty Core API 的设计非常重要。

插件可以：

```text
组合 primitive
```

但不能：

```text
创造一个不存在的 primitive
```

---

# 8. 这些东西必须是 Bitty 上游实现

我会明确划出下面这条边界。

### Terminal / PTY

```text
PTY lifecycle
VT parser
OSC parsing
shell integration
scrollback
terminal semantic zones
process lifecycle
```

必须 native。

Lua 可以监听：

```lua
on("command.finished")
```

但它不能可靠地自己解析整个 PTY byte stream。

---

### Renderer / Compositor

```text
GPU renderer
glyph atlas
frame scheduling
damage tracking
layout primitives
floating/tiled compositor
input hit testing
```

必须 native。

Lua 可以说：

```lua
panel:set_floating(true)
```

但：

```text
怎么画
什么时候画
GPU resource 生命周期
frame deadline
```

不能交给 Lua。

否则一个 plugin 卡 30ms，整个 terminal 都掉帧。

---

# 9. IPC 更必须属于 Trusted Core

这是尤其重要的一层。

Lua 可以：

```lua
bitty.ipc.send(...)
```

但不能负责：

```text
IPC transport
Agent authentication
Principal identity
Capability checks
Message routing invariants
Replay protection
Resource quotas
Connection lifecycle
```

真正应该是：

```text
Agent A
  │
  │ ToolRequest
  ▼
Bitty IPC
  │
  ├─ identify principal
  ├─ validate capability
  ├─ validate scope
  ├─ validate arguments
  ├─ audit
  ▼
Service
```

Lua plugin 只是提出：

```text
request
```

Rust Core 决定：

```text
allow / deny
```

**这一点会直接决定 Bitty 的安全模型是否真的成立。**

---

# 10. 文件、Process、Network 也绝不能只靠 Lua 权限

这是很多插件系统容易踩的坑。

假设配置写：

```text
fs.write = false
```

但 Lua 还有：

```lua
os.execute("rm foo")
```

这个权限系统就没有任何意义。

Lua 标准库本身包含 `io`、`os`、`debug`、`package` 等能力，而且 Lua 官方明确允许宿主选择是否加载这些库；官方还特别警告 `debug` library 会破坏正常的安全假设。([lua.org][1])

更加危险的是默认 module loader 可以通过 `package.cpath` 加载 native C module，`package.loadlib` 本质上甚至能动态链接本地 native library。([Lua][1])

所以 Bitty 的 safe Lua VM **绝对不应该直接 `luaL_openlibs()` 然后结束**。

应该类似：

```text
safe Lua environment

✓ table
✓ string
✓ math
✓ utf8
✓ coroutine (受控)

restricted:
△ base

disabled/replaced:
✗ io
✗ os
✗ debug
✗ package.loadlib
✗ arbitrary package.cpath
✗ arbitrary native require
```

然后由 Bitty 提供：

```lua
bitty.fs.read(...)
bitty.fs.write(...)
bitty.process.run(...)
bitty.net.request(...)
```

每一个走 capability check。

这就是：

> **deny-by-default host capability model**

---

# 11. Lua sandbox 本身不是 OS sandbox

即使把：

```text
io
os
debug
loadlib
```

全砍掉，也只能说明：

> Lua API 层没有明显 escape route。

它仍然不是 Linux namespace / seccomp / filesystem sandbox 这种 OS boundary。

所以如果 Bitty 将来允许 Agent：

```text
process.spawn
```

真正的权限应该下沉：

```text
Tool policy
       ↓
Bitty capability system
       ↓
Execution sandbox
       ↓
OS
```

例如概念上：

```text
Agent Reviewer

Tool:
  repo.read ✓
  process.spawn ✗

Filesystem:
  project ro

Network:
  deny

Environment:
  filtered

Secrets:
  none
```

这个一定是 Rust/native host 做。

---

# 12. Lua 还不适合做 Hard Real-time / Hot Path

Lua 非常快，但 Bitty 是 terminal emulator。

有些代码可能每秒调用几百、几千、几万次：

```text
glyph rendering
cell updates
PTY parser
input decoding
layout hot path
animation frame
GPU submission
```

不应该允许：

```text
for every cell:
    call Lua callback
```

正确方式应该是：

```text
native hot path

      ↓ aggregated semantic event

Lua
```

例如不是：

```lua
on_terminal_byte(byte)
```

而是：

```lua
on_command_finished {
    command = "...",
    exit_code = 1,
}
```

不是：

```lua
on_cell_render(cell)
```

而是：

```lua
panel:set_style(...)
```

这就是上游必须提前设计好 **semantic hook points** 的原因。

---

# 13. Lua 插件还有一个天然问题：它只能看到你暴露出来的 abstraction

例如以后想写一个：

> “折叠 shell command output” 插件。

如果 Bitty Core 只暴露：

```text
raw terminal text
```

插件就很难实现得好。

但如果 Core 已经有：

```text
CommandExecution {
    command,
    cwd,
    start,
    end,
    exit_code,
    stdout_range,
}
```

Lua 就可以非常优雅地：

```lua
if execution.success then
    execution:set_display("collapsed")
end
```

所以**Bitty 上游真正重要的工作不是提前做完所有插件，而是提前提供正确的 primitives。**

这个区别非常关键。

---

# 14. 我会把 Bitty Core 与 Lua Plugin 的边界设计成这样

```text
┌─────────────────────────────────────────────┐
│                Lua ecosystem                │
│                                             │
│ Config / Plugin / Agent / Workflow / UI     │
│ Skills / Commands / Hooks / Policies        │
└──────────────────────┬──────────────────────┘
                       │ safe handles
                       │ semantic APIs
                       ▼
┌─────────────────────────────────────────────┐
│              Bitty Service API              │
│                                             │
│ Panel   Task    Agent    Context    Tool     │
│ Exec    Repo    Workspace    Model           │
└──────────────────────┬──────────────────────┘
                       │ capabilities
                       ▼
┌─────────────────────────────────────────────┐
│               Trusted Rust Core             │
│                                             │
│ IPC / PTY / Renderer / Compositor           │
│ Capability / Sandbox / Persistence          │
│ Scheduler / Resource limits / Secrets       │
└──────────────────────┬──────────────────────┘
                       │
                       ▼
                  Operating System
```

这应该是比较健康的 architecture。

---

# 15. 而 IPC 正好把整个 Agent 系统串起来

你之前设计 IPC，到了这里价值其实开始真正体现。

以后 Bitty 内部可以都是：

```text
Agent
Panel
PTY
Tool
Task
Workspace
Model
Plugin
```

作为不同主体。

比如：

```text
Architect Agent
      │
      │ task.delegate
      ▼
     IPC
      │
      ├───────────────> Coding Agent
      │
      └───────────────> Research Agent

Coding Agent
      │
      │ workspace.changed
      ▼
     IPC
      │
      ├───────────────> Reviewer
      └───────────────> Diff Panel
```

而 Human 同样只是系统里的一个重要 participant：

```text
Reviewer Agent
    ↓
approval.request

IPC
    ↓
Human Panel

Allow / Deny
```

这就是为什么我觉得 **AI 可能最终成为 Bitty IPC 最有说服力的 use case**。

---

# 16. 甚至 Panel 本身都可以只是 IPC endpoint 的视觉投影

最后可能形成一个很漂亮的模型：

```text
                Bitty Object Graph

Agent ───────┐
Task ────────┤
Process ─────┤
Workspace ───┼── Event / IPC Bus
Tool ────────┤
Model ───────┤
Panel ───────┘
```

Panel 只是：

```text
view(endpoint)
```

于是：

```text
Agent
```

可以：

* 有 UI；
* 没有 UI；
* 后台运行；
* 临时打开 Panel；
* 被多个 Panel 同时观察。

这比“Agent = Chat Window”先进很多。

---

## 最值得坚持的一条原则

我现在反而会把 Bitty Lua 插件设计原则总结成：

> **Lua decides policy and composition; Rust enforces capability and mechanism.**

即：

```text
Lua:
"What should happen?"

Rust:
"Can it happen?"
"How does it safely happen?"
```

例如：

```text
Lua 决定：
Reviewer 收到 diff 后启动 review workflow。

Rust 决定：
Reviewer 到底能不能读这个 workspace、
能不能启动 process、
能不能联网、
能不能修改文件。
```

这样 Bitty 才能同时得到 **Emacs/Neovim 式的高度可扩展性**，又不至于变成“安装一个插件 = 给整个电脑 arbitrary code execution”。

而且这和你之前 Bitty 的方向其实非常一致：**核心保持小、稳定、安全，把无限的组合空间留给 Lua；但所有真正形成 trust boundary 的 primitive，都必须由 Bitty 上游先设计好。** ([Lua][1])

我觉得现在 `bitty-ai` 最值得先写的甚至不是具体 Agent，而是一份 **`Bitty Capability Model + Lua Host API + IPC Principal Model` RFC**。这三样一旦定下来，后面的 Agent、插件、`.bitty/`、Tool system、Panel system 都能落到同一套安全模型里。

[1]: https://www.lua.org/manual/5.4/manual.html?utm_source=chatgpt.com "Lua 5.4 Reference Manual"
[2]: https://wezterm.org/config/lua/window/index.html?utm_source=chatgpt.com "object: Window - Wez's Terminal Emulator"
