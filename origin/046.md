对于 Wheel 的设计，之后应该是有不同类型的 Agent 的呈现吧，既有像 opencode, claude code, codex, copilot 那样的 Coding Agent，也可以作为 Hermes Agent 这种管理全局、自我进化的 Agent 呢？我觉得目前的话就好好的只做 Coding Agent 就行了？因为对于 Hermes Agent, OpenClaw, 甚至之后专门用来办公的那种 Agent 就不在 Wheel 中实现了吧，因为这些 Agent 的用途不一样，而且很多组装策略不一样，比如 Hermes Agent 是有自我总结的能力，这种一般是作为管理系统、平时修改配置文件、平时获取新闻信息、管理云平台的服务、日常问答之类的任务吧，而且存储层也是不一样的。
也就是说之后 Wheel 的话就只设计成 Coding Agent 就行了吧，这样对于 MVP 也会更快吧

## 我赞成：Wheel 先明确成 **Coding Agent Harness**

而且我会进一步把边界写得很明确：

> **Wheel = Bitty 官方的、面向 Software Engineering 的 Agent Harness。**
>
> 它可以支持多 Agent、多角色、多模型、多 Workspace/Panel，但它的任务域始终是 **software development**。

这样比一开始把 Wheel 做成 “Universal Agent Framework” 健康得多。

Hermes Agent 官方现在已经明显走向另一类产品：除了 coding，它强调跨会话 persistent memory、自我创建/改进 skills、cron、消息平台 Gateway、远程执行环境等；官方甚至直接把它描述成不限于 coding copilot 的 autonomous agent。([Hermes Agent][1])
OpenClaw 的定位也非常类似，是运行在用户设备和各种聊天渠道中的 personal assistant，带持久 state/memory 和 Gateway。([GitHub][2])

这和 Wheel 想解决的问题确实已经不是同一个 scope 了。

---

# Wheel 内部仍然可以有很多种 Agent

这里最好区分两个概念：

```text
Agent Domain
    Coding Agent
    Personal Agent
    Office Agent
    Ops Agent
    Research Agent
    ...

Agent Role
    Planner
    Coder
    Reviewer
    Debugger
    Researcher
    Commander
    Subagent
    ...
```

**Wheel 固定 Domain = Coding。**

但是 Wheel 内部的 Role 可以非常丰富。

例如：

```text
Wheel
├── Primary Agent
│   ├── interactive coding
│   └── user-facing
│
├── Commander
│   ├── decompose task
│   ├── allocate worktree
│   ├── spawn subagents
│   └── merge results
│
├── Coding Agent
│   ├── implement
│   ├── refactor
│   └── fix
│
├── Review Agent
│   ├── code review
│   └── security review
│
├── Debug Agent
│   ├── reproduce
│   ├── inspect logs
│   └── diagnose
│
└── Research Agent
    ├── inspect codebase
    ├── docs
    └── dependency research
```

这些本质上甚至不一定需要变成不同的 Rust 类型：

```rust
Agent {
    role,
    model,
    tools,
    permissions,
    context,
    budget,
    workspace,
}
```

也就是说：

> **Agent primitive 尽量统一，Role 是配置。**

这样不会过早把 `CodingAgent / ReviewAgent / DebugAgent` 做成一大堆 class/type。

---

# Wheel 应该解决的核心问题

Wheel 的设计几乎可以围绕一个中心展开：

> **如何让 LLM 安全、高效、可控地完成一个软件工程任务。**

因此它天然关心：

```text
Repository
     │
     ▼
Task
     │
     ▼
Agent Session
     │
     ├── Context
     ├── Tools
     ├── Skills
     ├── LSP
     ├── AST
     ├── Git
     ├── Shell
     └── MCP
     │
     ▼
Job
     │
     ├── Panel
     ├── Worktree
     ├── Process
     └── Resource Budget
     │
     ▼
Result
     ├── Patch
     ├── Commit
     ├── Test Result
     ├── Artifact
     └── Progress Note
```

这和你之前讨论的 `job ↔ task ↔ worktree ↔ panel ↔ agent` 正好能形成一个相当完整的系统。

---

# 而 Hermes/OpenClaw 类 Agent 会需要另一套东西

例如一个 Personal Agent 的中心可能根本不是 Repository，而是：

```text
User
 │
 ├── Memory
 ├── Calendar
 ├── Email
 ├── Messaging
 ├── Browser
 ├── Cloud
 ├── Home Automation
 ├── Cron
 ├── Notifications
 └── Long-running Tasks
```

生命周期也完全不同。

### Wheel

可能是：

```text
task start
    ↓
session
    ↓
edit / build / test
    ↓
commit / result
    ↓
session end
```

持续几十分钟、几小时甚至几天都可以，但核心仍然是一个 **bounded engineering task**。

---

### Personal Agent

更接近：

```text
daemon start
    ↓
──────────── months ────────────
 ↓        ↓         ↓        ↓
cron    email     user     webhook
 ↓        ↓         ↓        ↓
task     task      task      task
 │
memory continuously evolves
```

这时候你马上就会遇到：

* lifelong memory
* user modeling
* cron
* webhook
* notification
* inbox
* OAuth
* secrets vault
* remote execution
* messaging gateway
* multi-device sync
* long-lived state
* skill self-evolution

Hermes 现在正是在处理这些问题。它官方描述的 learning loop 就包括跨会话记忆、skills 创建与改进等机制。([Hermes Agent][1])

所以没必要把这些复杂度带进 Wheel。

---

# 存储层也确实应该分开

你这里的判断尤其重要。

Wheel 可以天然围绕：

```text
Global
~/.local/share/bitty/wheel/
├── providers/
├── sessions/
├── cache/
└── models/

Project
<repo>/.bitty/
├── context/
├── tasks/
├── skills/
├── agents/
└── wheel/
```

它的 knowledge 往往是：

```text
这个项目用了什么架构
这个函数为什么这么实现
cargo test 出现过什么问题
这个 task 已经完成了什么
这个 branch/worktree 在做什么
```

也就是 **project-centric memory**。

而 Personal Agent 的 memory 可能是：

```text
用户喜欢什么
昨天做了什么
下周有什么会议
某人的邮箱是什么
家里的服务器在哪里
以前采用了什么习惯
```

这是：

> **user-centric / lifelong memory**

两者的数据生命周期、隐私等级、检索策略甚至 embedding/schema 都可能完全不同。

没必要强行统一。

---

# Office Agent 又会是第三种形态

以后如果真的做：

```text
bitty-terminal
├── Wheel            Coding Agent
├── ???              Personal Agent
└── ???              Office Agent
```

Office Agent 甚至又不同。

它会更关注：

```text
Document
Spreadsheet
Slides
Email
Calendar
Meeting
Approval
Workflow
CRM
ERP
```

它的 result 可能不是：

```text
git diff
```

而是：

```text
document
email draft
spreadsheet
calendar event
approval request
```

权限模型也不同。

Coding：

```text
read(src/)
write(src/)
run(cargo test)
git commit
```

Office：

```text
read Gmail
send Email
modify Calendar
share Document
```

后者甚至具有更强的 external side effect。

所以也应该是另外一个 Harness。

---

# 但是 Bitty Core 可以为它们共享底层 primitive

这里才是我认为真正值得提前设计好的地方。

不要：

```text
Wheel
= Universal Agent Framework
```

而应该：

```text
                Bitty Agent Primitives

 Provider        Tool            Permission
    │             │                  │
    └─────────────┼──────────────────┘
                  │
              Agent Runtime
                  │
       ┌──────────┼───────────┐
       │          │           │
     Wheel      Future A    Future B
    Coding      Personal     Office
```

例如 Core/API 层可以慢慢沉淀：

```rust
AgentId
SessionId
JobId
TaskId

AgentRuntime
AgentContext

Tool
ToolRegistry

Permission
Capability

ResourceBudget

AgentEvent

AgentHandle
AgentLifecycle

Provider

ContextStore
Artifact

Job
JobResult
```

以及 Bitty 特有的：

```text
Panel
Workspace
PTY
Process
IPC
Worktree
LSP
```

这些能力 Wheel 使用。

以后另一个 Agent 插件也可以复用其中一部分。

---

# 但现在甚至不要急着抽象这么多

这里还有一个很重要的软件工程原则：

> **不要为了未来可能存在的 Personal Agent，今天就设计 Universal Agent API。**

否则非常容易变成：

```rust
trait Agent {
    fn memory(...)
    fn cron(...)
    fn email(...)
    fn code(...)
    fn browser(...)
    fn messaging(...)
    fn cloud(...)
    ...
}
```

最后谁都不好用。

更合理的是：

```text
2026
    Wheel needs X
        ↓
implement X

Wheel needs Y
        ↓
implement Y

Future Personal Agent needs X
        ↓
X 已经证明是通用能力
        ↓
extract X into shared API
```

也就是：

> **Generalize from two real implementations, not from one imagined implementation.**

这对 Bitty 尤其重要。

---

# Wheel MVP 我甚至建议再缩小一点

第一版不用一开始就做完整的 Claude Code / Codex / OpenCode competitor。

可以先做到：

```text
Wheel MVP

User
 ↓
Primary Coding Agent
 ↓
Tool Call
 ├── read
 ├── search
 ├── edit
 ├── shell
 ├── git
 └── LSP
 ↓
Bitty Panel
 ↓
Result
```

再加几个你 Bitty 本身最有优势的东西：

```text
Panel-aware
Worktree-aware
Process-aware
LSP-aware
Task-aware
```

这已经非常有特色了。

---

第二阶段：

```text
Primary Agent
     │
     ├── Subagent A
     ├── Subagent B
     └── Subagent C
```

加入：

* resource budget
* permissions
* typed result
* progress note
* task binding
* worktree binding

---

第三阶段再做：

```text
Commander
    │
    ├── plan
    ├── assign
    ├── monitor
    ├── review
    └── merge
```

也就是你前面讨论的真正 Agent Harness。

---

# Wheel 的 Non-goals 可以现在就写

我甚至建议你在 Wheel README / RFC 里面直接写一段：

```text
Non-goals

Wheel is not intended to be:

- a personal AI assistant
- a general-purpose autonomous agent
- a messaging gateway
- an email/calendar assistant
- a home automation agent
- a lifelong user-memory system
- a cron/automation daemon
- a general cloud-management agent
```

然后一句：

```text
Wheel is focused on software engineering workflows.
```

这样以后架构不会不断 scope creep。

---

## 最终结构其实会非常漂亮

我现在比较看好 Bitty 最终形成这样的层次：

```text
Bitty
│
├── Terminal Core
│   ├── Panel
│   ├── Workspace
│   ├── PTY
│   ├── Renderer
│   ├── IPC
│   └── Process
│
├── Agent Infrastructure
│   ├── Provider
│   ├── Tool
│   ├── Permission
│   ├── Resource Budget
│   ├── Session
│   └── Job
│
└── Plugins
    │
    ├── Wheel
    │    └── Coding Agent Harness
    │
    ├── future-personal-agent
    │    └── Personal / Persistent Agent
    │
    └── future-office-agent
         └── Office Automation Agent
```

其中 **Wheel 可以非常强大，但强大在 Coding Agent 这个垂直领域，而不是靠 scope 大**。

所以我认为现在最适合的路线就是：

> **Wheel 只做 Coding Agent。**
>
> `Coder / Planner / Reviewer / Debugger / Commander / Subagent` 都属于 Wheel；
>
> `Hermes/OpenClaw-style Personal Agent`、Office Agent、Always-on Agent 不属于 Wheel。
>
> 等未来真有第二类 Agent 时，再从 Wheel 和第二个实现之间抽象共同的 Agent Infrastructure。

这不仅会让 MVP 快很多，还会直接降低 **storage、security、permissions、memory、daemon、tooling、prompt、context management** 六七个维度的设计复杂度。更重要的是，Wheel 的产品定位会从一开始就非常清楚：**“在 Bitty 里把软件工程工作交给 Agent”**。

[1]: https://hermes-agent.nousresearch.com/docs/zh-Hans/?utm_source=chatgpt.com "Hermes Agent 文档 | Hermes Agent"
[2]: https://github.com/openclaw/openclaw?utm_source=chatgpt.com "GitHub - openclaw/openclaw: The AI that really does things. Any OS. Any Platform. The lobster way. 🦞 · GitHub"

