介绍下 git module

Git Submodule 是什么？

你说的 Git module 一般指的是 Git Submodule（子模块）。

它解决的问题可以概括成：

    让一个 Git 仓库引用另一个独立 Git 仓库的某个确定 commit。

例如：

my-project/
├── src/
├── docs/
├── third-party/
│   └── some-lib/       # 一个独立 Git 仓库
├── .gitmodules
└── .git/

some-lib 看起来是主仓库里的目录，但实际上它有自己的 Git 历史；主仓库只记录：

some-lib -> commit abc123...

所以 Submodule 不是把另一个仓库的源码直接复制进来，而更像是保存一个“仓库 + commit 指针”。
1. 为什么需要 Submodule？

假设你有：

bitty/
bitty-docs/
bitty-plugins/

它们本来是三个独立仓库。

但如果你希望在 bitty 中能够直接访问文档：

bitty/
├── crates/
├── docs/          # 实际来自 bitty-docs
└── Cargo.toml

可以：

git submodule add https://github.com/xxx/bitty-docs.git docs

Git 会生成 .gitmodules：

[submodule "docs"]
    path = docs
    url = https://github.com/xxx/bitty-docs.git

同时主仓库记录：

docs -> bitty-docs 的某个 commit

关键点是：主仓库锁定的是 commit，而不是简单地“跟随 bitty-docs 最新版本”。
2. Clone 有 Submodule 的仓库

普通：

git clone https://github.com/xxx/bitty.git

这时 docs/ 通常不会自动完整 checkout。

需要：

git submodule update --init --recursive

或者直接：

git clone --recurse-submodules https://github.com/xxx/bitty.git

我更推荐后者。

如果已经 clone：

git submodule update --init --recursive

即可。
3. Submodule 的核心模型

这是理解它最重要的地方。

假设：

Main Repository
│
├── commit A
├── commit B
└── commit C
        │
        └── docs = bitty-docs@8ac92fe

主仓库的 commit C 记录：

bitty-docs@8ac92fe

而不是：

bitty-docs main branch

因此即使 bitty-docs/main 后来已经：

8ac92fe
   ↓
921fcab
   ↓
72ab132

主仓库依然使用：

8ac92fe

这实际上是 Submodule 很重要的优点：构建可复现性（reproducibility）。
4. 更新 Submodule

进入子模块：

cd docs
git pull

此时：

docs:
8ac92fe → 72ab132

回到主仓库：

cd ..
git status

你会看到类似：

modified: docs (new commits)

然后：

git add docs
git commit -m "chore: update docs submodule"

本质上提交的是：

-docs -> 8ac92fe
+docs -> 72ab132

而不是 docs 中的具体文件变化。
5. Submodule 和 Monorepo 最大区别

比如你现在有：

carryctx
carryctx-docs
carryctx-skills
carryctx-website

Monorepo

carryctx/
├── crates/
├── docs/
├── skills/
└── website/

它们共享：

一个 Git history
一个 issue/project context
一个 CI
一个 release workflow

修改可以一次提交：

git commit -m "feat: update CLI and docs"

Multi-repo + Submodule

carryctx/
├── crates/
├── docs/       -> carryctx-docs@abc123
├── skills/     -> carryctx-skills@def456
└── website/    -> carryctx-website@789abc

实际上仍然是：

carryctx
carryctx-docs
carryctx-skills
carryctx-website

四个独立仓库。

只是 carryctx 引用另外三个仓库的确定版本。

因此 Submodule 并没有真正解决 multi-repo 的协同问题，它只是建立了：

Repository A
     │
     └── depends on
              ↓
Repository B @ commit X

6. Submodule 的优缺点
特性	Submodule
仓库独立	✅
Git 历史独立	✅
独立 Release	✅
锁定具体版本	✅
可复现性	很好
权限独立	✅
Clone 简单程度	❌ 更复杂
跨仓库修改	❌ 麻烦
CI 配置	略复杂
新手体验	❌ 容易踩坑

Submodule 最经常被吐槽的就是：

git clone ...

之后发现：

    “为什么这个目录是空的？”

或者：

    “为什么我的 submodule 处于 detached HEAD？”

其实这是因为 Submodule 天生是 commit-oriented 的，而不是普通开发者直觉里的 branch-oriented。
7. Submodule、Subtree、Monorepo

这三个很容易混淆：

                    外部仓库是否保持独立
                           │
              ┌────────────┴────────────┐
              │                         │
             是                         否
              │                         │
         Submodule                  Monorepo
              │
       只引用 commit

还有一个中间方案：
git subtree

它会把另一个仓库的内容真正合并进当前仓库：

git subtree add \
  --prefix=docs \
  https://github.com/xxx/bitty-docs.git \
  main \
  --squash

于是主仓库真的拥有：

docs/a.md
docs/b.md
docs/c.md

而 Submodule 只是：

docs -> external repo @ commit

所以可以简单理解：

Submodule
= 引用另一个仓库

Subtree
= 把另一个仓库同步进来

Monorepo
= 从组织方式上就是一个仓库

8. 对你现在这些项目意味着什么

对于你现在正在考虑的 CarryCtx 仓库组织问题，Submodule 确实值得了解，但我不太建议为了“把多个仓库看起来放到一起”就使用它。

例如：

carryctx/
├── carryctx-docs/      submodule
├── carryctx-skills/    submodule
└── carryctx-website/   submodule

技术上完全可行，但如果这些东西经常需要：

CLI 改 API
    ↓
docs 同时更新
    ↓
skills 同时更新
    ↓
website 同时更新

Submodule 反而会增加协调成本，因为一次逻辑修改变成多个 repository commit + 最后更新主仓库的 submodule pointer。

反过来，如果是：

bitty
  ↓
某个完全独立开发的 Lua plugin

或者：

project
  ↓
固定版本的第三方源码

Submodule 就非常合理。

可以把判断标准压缩成一句话：

    “它是不是一个真正独立生命周期的项目，但我又需要精确引用它的某个版本？”

如果答案是“是”，Submodule 很适合；如果只是因为一个项目越来越大想拆目录，通常应该先考虑 workspace / monorepo，而不是 Submodule。

对于 Rust 项目尤其如此：Cargo workspace 和 Git Submodule 是两个完全不同层面的东西。你的 CarryCtx 后续拆成多个 crates，更应该优先考虑 Cargo workspace；而是否拆成多个 Git repository，则是另一个独立的架构决策。

那么一个现成的仓库中是否可以引入一个 submodule 呢？比如我这个 bitty 仓库加一个 docs，然后这个 docs 指向 bitty-docs ？
其实就是我现在想要把 bitty-docs 里面分出三个 module 分别是 bitty-terminal（bitty-terminal-docs）, bitty-plugins（bitty-plugins-docs）, bitty-ai（bitty-ai-docs） 三个文档仓库，是否可以做到呢？而且每一个代码仓库的 docs 可以指向一个文档仓库，这样可以吗？不然并行工作不好管理

可以，而且你的这个场景其实 非常适合 Git Submodule。
推荐结构

你现在可以把文档拆成三个真正独立的仓库：

bitty-terminal-docs
bitty-plugins-docs
bitty-ai-docs

然后对应代码仓库：

bitty/
├── crates/
├── docs/  -> bitty-terminal-docs
└── Cargo.toml

bitty-plugins/
├── ...
└── docs/  -> bitty-plugins-docs

bitty-ai/
├── ...
└── docs/  -> bitty-ai-docs

比如在现有 bitty 仓库里直接执行：

cd bitty

git submodule add \
  https://github.com/bitty-terminal/bitty-terminal-docs.git \
  docs

git commit -m "docs: add documentation submodule"

完全没问题。

之后：

bitty/docs/

在文件系统里看起来就是正常目录，但实际上它是：

bitty-terminal-docs @ 某个 commit

你的设计甚至可以做两层组织

我觉得你这里有一个很自然的架构：

bitty-terminal/
├── bitty
├── bitty-plugins
├── bitty-ai
│
├── bitty-terminal-docs
├── bitty-plugins-docs
└── bitty-ai-docs

然后代码和文档一一对应：

bitty
   │
   └── docs -> bitty-terminal-docs

bitty-plugins
   │
   └── docs -> bitty-plugins-docs

bitty-ai
   │
   └── docs -> bitty-ai-docs

关系非常清晰：

implementation repository
        │
        ▼
documentation repository

也就是：

bitty
        ───────> bitty-terminal-docs

bitty-plugins
        ───────> bitty-plugins-docs

bitty-ai
        ───────> bitty-ai-docs

我认为这比一个越来越大的：

bitty-docs/
├── bitty-terminal/
├── bitty-plugins/
└── bitty-ai/

更适合你现在准备进行的 多 Agent 并行开发。
为什么更适合并行工作

你现在真正遇到的问题其实不是 Git 本身，而是：

    不同 Agent 修改不同领域时，如何降低 context、working tree 和 commit 之间的耦合。

假设全部都在：

bitty-docs/
├── terminal/
├── plugins/
└── ai/

三个 Agent：

Agent A → terminal
Agent B → plugins
Agent C → ai

即便目录不同，仍然共享：

.git/
branch
index
CI
issues
PR
release/history

虽然可以通过 worktree 解决不少问题，但逻辑上还是一个 repository。

拆成：

bitty-terminal-docs
bitty-plugins-docs
bitty-ai-docs

以后 Agent 可以真正完全隔离：

Agent A
cwd = bitty-terminal-docs

Agent B
cwd = bitty-plugins-docs

Agent C
cwd = bitty-ai-docs

三者：

commit 不冲突
branch 不冲突
PR 不冲突
CI 不冲突
context 不冲突

这对你正在设计的 CarryCtx / Agent workflow 尤其合适。
Submodule 在这里解决第二个问题

但是拆仓库以后会出现另一个问题：

    我开发 bitty 的时候，还想非常方便地看到对应 documentation。

这时候 Submodule 正好解决这个问题。

你 clone：

git clone --recurse-submodules \
  https://github.com/bitty-terminal/bitty.git

得到：

bitty/
├── crates/
├── src/
├── Cargo.toml
└── docs/
    ├── architecture/
    ├── rfc/
    ├── design/
    └── ...

开发体验仍然像 Monorepo：

cd bitty

vim src/...
vim docs/architecture/...

但是 Git 层面：

src/*
    ↓
bitty repository

docs/*
    ↓
bitty-terminal-docs repository

这是一个很漂亮的边界。
但有一个重要区别

例如你同时修改：

bitty/src/foo.rs
bitty/docs/foo.md

然后执行：

git status

实际上是两个 Git repository。

你需要：

cd docs

git add .
git commit -m "docs: document foo"
git push

然后：

cd ..

git add src
git add docs

git commit -m "feat: implement foo"

第二个 commit 里面实际上记录的是：

docs:
old commit abc123
        ↓
new commit def456

因此最终形成：

bitty commit #100
│
├── code version
│
└── docs -> bitty-terminal-docs@def456

这反而非常有价值。

因为以后 checkout：

git checkout <bitty-v0.5.0>
git submodule update --init

得到的是：

Bitty v0.5.0
+
与 v0.5.0 对应的文档

而不是现在最新版 documentation。

这对版本化文档非常好。
我甚至建议保留一个 bitty-docs

不过这里我有一个稍微不同的建议：

不要一定删除现在的 bitty-docs。

可以让它从：

    所有文档内容仓库

转变成：

    Bitty Documentation Workspace / Aggregator

例如：

bitty-docs/
├── terminal/    -> bitty-terminal-docs
├── plugins/     -> bitty-plugins-docs
├── ai/          -> bitty-ai-docs
├── website/
├── scripts/
└── README.md

其中三个目录也全部是 Submodule：

git submodule add \
    git@github.com:bitty-terminal/bitty-terminal-docs.git \
    terminal

git submodule add \
    git@github.com:bitty-terminal/bitty-plugins-docs.git \
    plugins

git submodule add \
    git@github.com:bitty-terminal/bitty-ai-docs.git \
    ai

那么：

                    bitty-docs
                   /     |     \
                  /      |      \
                 ▼       ▼       ▼
           terminal   plugins    ai
             docs       docs    docs

而另一边：

bitty ────────────────┐
                      ▼
             bitty-terminal-docs

bitty-plugins ────────┐
                      ▼
             bitty-plugins-docs

bitty-ai ─────────────┐
                      ▼
                bitty-ai-docs

也就是 一个文档仓库可以同时被两个地方引用。

完全没问题。
最终架构会很漂亮

我会比较推荐：

bitty-terminal org
│
├── bitty
│   └── docs/
│       └── submodule: bitty-terminal-docs
│
├── bitty-plugins
│   └── docs/
│       └── submodule: bitty-plugins-docs
│
├── bitty-ai
│   └── docs/
│       └── submodule: bitty-ai-docs
│
├── bitty-terminal-docs
│
├── bitty-plugins-docs
│
├── bitty-ai-docs
│
└── bitty-docs
    ├── terminal/
    │   └── submodule: bitty-terminal-docs
    ├── plugins/
    │   └── submodule: bitty-plugins-docs
    └── ai/
        └── submodule: bitty-ai-docs

这样你同时获得：

代码领域隔离
+
文档领域隔离
+
Agent 工作区隔离
+
独立 Git history
+
独立 issue / PR
+
代码仓库内可以直接读 docs
+
统一文档 workspace
+
代码版本与 docs commit 精确绑定

但建议控制 Submodule 层级

唯一需要避免的是发展成这种：

A
└── B submodule
    └── C submodule
        └── D submodule

也就是 recursive submodule 套娃。

你的设计其实不是这种情况。

你是：

                 terminal-docs
                /             \
             bitty          bitty-docs

属于 DAG 式复用，没有问题。
对多 Agent 来说还有一个隐藏优势

你之后完全可以把 CarryCtx 的 project/workspace 概念设计成：

workspace: bitty

repositories:
  - name: terminal
    path: ./bitty

  - name: terminal-docs
    path: ./bitty/docs

  - name: plugins
    path: ./bitty-plugins

  - name: plugins-docs
    path: ./bitty-plugins/docs

  - name: ai
    path: ./bitty-ai

  - name: ai-docs
    path: ./bitty-ai/docs

然后 Agent 不需要关心：

    这个目录是不是 Submodule？

CarryCtx 可以自动识别：

git rev-parse --show-superproject-working-tree

或者 .gitmodules，从而知道：

这是一个独立 repository
但属于当前 workspace。

这其实与你现在强调的 multi-agent + worktree + repository context boundary 非常契合。
我对 Bitty 的建议

我现在反而比较支持你拆。

原本只有：

bitty
bitty-docs

的时候没有太大必要。

但是现在 Bitty 已经自然生长出了三个相对独立的领域：

Bitty Terminal
Bitty Plugins
Bitty AI

尤其 bitty-ai 已经不是“终端里顺便加个 AI”这么简单了，而会有自己的：

agent runtime
tools
context
memory
storage
compression
AST/code intelligence
model/provider
sandbox
session

Plugin 系统也有自己的：

SDK
API
Lua
permissions
package
lifecycle
UI/Panel
IPC

这时候：

terminal docs
plugin docs
AI docs

分别独立是合理的。

而 Submodule 恰好可以让“仓库拆开”和“开发时放在一起”同时成立。

所以你这个方向我会概括成：

    Multi-repo 负责 ownership 和并行开发，Submodule 负责 workspace composition。

这比单纯为了“仓库小一点”而拆仓库要有意义得多。

