这样看来，其实 Bitty 本身越来越像是一个小型的、可扩展性的 “浏览器”、“操作系统” 这种的？

对，而且随着你现在把 `Panel`、Lua Plugin、Native UI、Capability、Service、Event Bus、Workspace 这些东西逐渐补齐，Bitty 的抽象层级确实已经开始从“终端模拟器”向一个**小型 user-space application platform** 演化。

## 它越来越像“浏览器”

浏览器其实也是一个很有意思的类比：

```text
Browser
├── Window / Tab
├── DOM / UI Runtime
├── JS Runtime
├── Storage
├── Network
├── Permissions
├── Events
├── Workers
├── Rendering Engine
└── Web Applications
```

对应 Bitty：

```text
Bitty
├── Window / Workspace / Panel
├── Native UI Runtime
├── Lua Runtime
├── Plugin Store
├── Network Capability
├── Capability System
├── Event Bus
├── Tasks / Helper Process
├── wgpu Renderer
└── Bitty Applications
```

甚至可以看到非常明显的对应关系：

| Browser                  | Bitty              |
| ------------------------ | ------------------ |
| Tab                      | Panel              |
| Web App                  | Lua Application    |
| JavaScript               | Lua                |
| DOM                      | Widget Tree        |
| CSS                      | Theme / Appearance |
| Web APIs                 | `bitty.*` APIs     |
| Web Permissions          | Capabilities       |
| localStorage / IndexedDB | Plugin Store       |
| Web Worker               | Task / Helper      |
| browser extension        | Bitty Plugin       |
| rendering engine         | `bitty-render`     |

所以以后 Bitty 插件可能真的会有一种：

```lua
local ui = bitty.ui

return ui.column {
    ui.text { "Docker" },

    ui.list {
        items = containers
    }
}
```

类似 Web App 写 DOM 的感觉。

只是：

```text
HTML/CSS/JS
```

变成：

```text
Lua + Bitty Native UI
```

---

## 也越来越像“小型操作系统”

但这里说的是 **user-space OS / application environment**，不是 Linux Kernel 那种 OS。

因为 Bitty 已经开始拥有很多 OS/桌面环境才会有的概念：

```text
Application lifecycle
Window management
Focus management
IPC / Event Bus
Permissions
Storage
Tasks
Process spawning
Notifications
Clipboard
Network access
Application UI
Configuration
Sessions
```

尤其你的：

```text
Window
 └── Workspace
      └── Panel
```

本身已经非常接近：

```text
Desktop
 └── Workspace
      └── Window
```

只是 Bitty 又多了一层非常重要的控制：

```text
Panel
 └── Activity
```

例如：

```text
Panel
├── Terminal Activity
├── Editor Activity
├── Docker Activity
└── AI Activity
```

这其实已经有一点类似移动 OS 的 Activity / View Controller 思想。

---

## 但 Bitty 不是完整 OS

边界最好一直保持清晰。

Bitty **不应该自己做**：

```text
Kernel
Scheduler
Memory manager
Filesystem
Device drivers
TCP/IP stack
USB
GPU driver
Process isolation kernel
```

这些仍然交给：

```text
Linux
macOS
Windows
BSD
```

所以：

```text
┌───────────────────────────────┐
│ Bitty                         │
│                               │
│ Application Runtime           │
│ UI Runtime                    │
│ Plugin Runtime                │
│ Workspace Manager             │
│ Terminal Runtime              │
└───────────────────────────────┘
              ↓
┌───────────────────────────────┐
│ Linux / macOS / Windows       │
└───────────────────────────────┘
              ↓
┌───────────────────────────────┐
│ Kernel / Drivers / Hardware   │
└───────────────────────────────┘
```

这也是为什么我更愿意称它：

> **Application Shell**

或者：

> **Programmable User-space Environment**

而不是 OS。

---

## 甚至“浏览器”这个类比可能比 IDE 更准确

因为 IDE 通常已经假设：

```text
Editor
Project
Compiler
Debugger
```

是中心。

Bitty 并不一定。

比如：

### 开发环境

```text
Editor
Terminal
Git
AI
Docker
Tests
```

### 运维环境

```text
SSH
Kubernetes
Logs
Metrics
Docker
```

### AI 工作区

```text
Agent
Tasks
Terminal
Diff
Browser
Logs
```

### 普通个人工作区

```text
Mail
Telegram
Notes
Calendar
Music
```

它们都只是：

```text
Applications running on Bitty
```

这就很像浏览器：

浏览器本身不是：

```text
Mail Client
IDE
Chat Client
Video Player
Office
```

但是 Web Runtime 足够强，所以 Gmail、VS Code Web、Discord、YouTube、Google Docs 都可以建立在浏览器上。

Bitty 最理想的状态也是：

> Core 不知道 Docker、Mail、Telegram、Editor 是什么。

Core 只知道：

```text
Panel
Widget
Command
Service
Event
Task
Storage
Capability
```

然后生态自己长出：

```text
Docker App
Mail App
Git App
Bitter
Bitty AI
Telegram App
```

这会是一个非常健康的架构。

---

## 我甚至会把 Bitty 分成三层

以后架构文档里可以正式这样定义：

```text
                    Bitty Applications
           ┌──────────┬──────────┬──────────┐
           │ Bitter   │ Docker   │ Bitty AI │
           │ Mail     │ Git      │ Telegram │
           └──────────┴──────────┴──────────┘
                         │
                 Bitty Platform API
                         │
┌────────────────────────────────────────────────┐
│              Bitty Application OS              │
│                                                │
│  UI Runtime     Panel Runtime     Lua Runtime  │
│  Event Bus      Services          Capabilities │
│  Storage        Tasks             Commands     │
│  Workspace      Notifications     Clipboard    │
└────────────────────────────────────────────────┘
                         │
                 Terminal Runtime
                         │
          PTY / VT / Shell / Native Process
                         │
                  Host Operating System
```

其中 `"Application OS"` 可以只是内部架构概念，不一定作为产品宣传语。

因为从工程角度讲，它确实越来越像：

> **在 Linux/macOS/Windows 上运行的一个微型应用操作环境。**

---

## 最有意思的是：Terminal 会反过来成为“应用”

Bitty 最开始的关系是：

```text
Bitty
  ↓
Terminal
  ↓
Everything
```

也就是所有东西都必须通过 terminal。

以后可能变成：

```text
Bitty Runtime
├── Terminal
├── Editor
├── Docker
├── Git
├── Mail
└── AI
```

Terminal 只是其中一个一等公民。

这其实是一个非常大的架构转变：

```text
Terminal Emulator
        ↓
Programmable Terminal
        ↓
Programmable Workspace
        ↓
Application Runtime
```

而我觉得你现在正好处于第三步向第四步过渡的位置。

最值得坚持的原则可能就是：

> **Bitty Core 提供 primitives，而不是 applications。**

只要这一点守住，即使以后 Bitty 真的能承担浏览器、IDE、桌面工作区那样复杂的职责，Core 仍然可以保持相对小而稳定。

而且 Bitter 存在的话，是不是 Bitty 以前是说过插件之上是可以装插件的吧？这样的话，用户也可以在 Bitter 之上继续开发更多的插件，比如 LSP 支持、语法高亮等等插件呢？其实这个部分是在 bitty-ai 设计的时候考虑的话，因为 Bitty 的 Agent 相关的插件出现后肯定是可以继续在这之上继续开发插件进行扩展的，这种扩展的并不是 Bitty 的核心功能，而是插件的核心功能了？

对，这个其实和 Bitty 之前讨论过的方向是一致的：**Bitty 不只是“Core 可以装 Plugin”，而是 Plugin 本身也可以成为一个新的扩展平台。**

而且现在把 Bittor 改成 **Bitter** 之后，这个例子反而更清楚了。Bitter 可以是 Bitty 的一个一等插件/应用，但它自己再暴露一组“编辑器领域”的 extension points，于是其他插件不是在扩展 Bitty Core，而是在扩展 Bitter。

可以把整个关系看成：

```text
Bitty Core
   │
   ├── Plugin Runtime
   ├── UI Runtime
   ├── Services
   ├── Events
   ├── Storage
   ├── Capability
   └── Panel Runtime
          │
          ▼
       Bitter
          │
          ├── bitter-lsp
          ├── bitter-treesitter
          ├── bitter-git
          ├── bitter-format
          ├── bitter-diagnostics
          ├── bitter-snippets
          └── bitter-minimap
```

这里最重要的一点就是：

> `bitter-lsp` 扩展的是 **Bitter 的编辑器能力**，而不是 Bitty 的核心能力。

这和 Bitty AI 是同一种架构思想。之前其实已经讨论过类似的两级扩展模型：Bitty 提供通用 primitives 和 service/capability/event 机制，`bitty-ai` 在上面建立 AI-specific runtime，然后更多 AI 插件继续扩展 `bitty-ai`。

## 所以我会把 Bitty 的“Plugin”分成两个概念

不要把所有插件都理解为直接挂在 Core 上。

更准确的结构应该是：

```text
Platform Plugin
└── Extension Plugin
```

比如：

```text
Bitty
├── Bitter
│   ├── bitter-lsp-rust
│   ├── bitter-lsp-typescript
│   ├── bitter-treesitter
│   └── bitter-git
│
├── Bitty AI
│   ├── bitty-ai-openai
│   ├── bitty-ai-anthropic
│   ├── bitty-ai-memory
│   ├── bitty-ai-dcp
│   └── bitty-ai-review
│
├── Statusline
│   ├── statusline-git
│   ├── statusline-cpu
│   └── statusline-ai
│
└── Docker
    ├── docker-compose
    └── docker-kubernetes
```

这里的 `Bitter`、`Bitty AI`、`Statusline` 都可以理解成一种 **Host Plugin**。

它自己也是 Bitty Plugin，但同时又是其他 Plugin 的宿主。

---

# 不过最好不要实现成“插件里面加载另一个 Lua 插件”

这一点很重要。

表面上看可以叫：

> Plugin on Plugin

但底层最好不是：

```text
Bitty
 └── Bitter Lua VM
       └── bitter-lsp Lua VM
```

这种真正的“嵌套运行时”。

否则生命周期、错误传播、权限和热重载都会变复杂。

更好的方式仍然是：

```text
Bitty Plugin Runtime
├── Bitter
├── bitter-lsp
├── bitter-treesitter
├── bitter-git
├── Bitty AI
└── bitty-ai-memory
```

它们实际上都是 Bitty Runtime 管理的平级插件。

只是它们之间存在：

```text
dependency
service
extension point
contribution
```

关系。

也就是说：

```text
运行时：平级
语义上：分层
```

这是很关键的设计。

---

# 比如 Bitter 可以暴露自己的 Extension API

假设 Bitter 注册：

```lua
bitty.services.provide("bitter.editor", editor_service)
```

然后 `bitter-lsp`：

```lua
local editor = bitty.services.get("bitter.editor")

editor.register_language_provider {
    language = "rust",

    diagnostics = function(document)
        ...
    end,

    completion = function(document, position)
        ...
    end,

    definition = function(document, position)
        ...
    end,
}
```

那么 Bitty Core 完全不需要知道什么是：

```text
LSP
Completion
Diagnostic
Definition
Hover
Code Action
```

这些都是 Bitter 定义出来的领域协议。

Core 只知道：

```text
Plugin A provides service
Plugin B consumes service
```

这就是非常漂亮的边界。

---

# Syntax Highlight 也是一样

比如 Bitter 自己定义：

```lua
editor.register_highlighter(...)
```

然后：

```text
bitter-highlight-treesitter
bitter-highlight-regex
bitter-highlight-semantic
```

都可以存在。

甚至以后：

```text
bitter-treesitter-rust
bitter-treesitter-python
bitter-treesitter-typescript
```

这些插件只是注册 grammar / parser / query。

Bitty Core 不知道 Tree-sitter 是什么。

甚至理论上 **Bitter 本身都不一定知道 Tree-sitter**。

Bitter 只知道：

```text
HighlightProvider
```

Tree-sitter 只是其中一种实现。

这就进一步解耦了：

```text
Bitter
    ↓
Highlight Provider API
    ↓
Tree-sitter Plugin
```

而不是：

```text
Bitter
    ↓
hard-coded Tree-sitter
```

---

# LSP 甚至还可以再分层

比如不要直接做：

```text
bitter-lsp
```

然后里面包办所有语言。

可以有：

```text
bitter-lsp
│
├── LSP client runtime
├── diagnostics adapter
├── completion adapter
├── hover adapter
└── workspace symbols
```

再上面：

```text
bitter-rust
   └── rust-analyzer config

bitter-typescript
   └── typescript-language-server config

bitter-python
   └── basedpyright config
```

于是：

```text
Bitty
 ↓
Bitter
 ↓
bitter-lsp
 ↓
bitter-rust
```

甚至出现三级扩展都是合理的。

但是运行时仍然全都是 Bitty Plugin Runtime 管理的平级组件。

---

# Bitty AI 就更适合这种模式

其实 AI 可能会成为 Bitty 中最典型的“插件平台插件”。

可以想象：

```text
Bitty Core
   │
   ▼
Bitty AI
   ├── Agent Runtime
   ├── Conversation
   ├── Tool Registry
   ├── Context Registry
   ├── Model Registry
   ├── Memory Registry
   ├── Compaction Registry
   └── Agent UI
```

然后生态：

```text
bitty-ai-openai
bitty-ai-anthropic
bitty-ai-gemini

bitty-ai-lsp-context
bitty-ai-terminal-context
bitty-ai-git-context

bitty-ai-dcp
bitty-ai-remote-compact

bitty-ai-memory-sqlite
bitty-ai-memory-files

bitty-ai-review
bitty-ai-plan
bitty-ai-task
```

甚至你之前提到的：

```text
/context
/review
/plan
/task
/compact
/loop
```

都不一定是 `bitty-ai` 自己内置的。

它们完全可以是 extensions：

```text
bitty-ai-review
    contributes slash command "/review"

bitty-ai-plan
    contributes "/plan"

bitty-ai-loop
    contributes "/loop"
```

这样 `bitty-ai` Core 本身就会非常干净。

---

# 这时候其实会出现一个很重要的概念：Extension Point

Bitty Core 现在已经有类似：

```text
Command
Event
Service
UI
Keymap
```

但以后最好正式引入：

```text
Extension Point
```

比如 Bitter 声明：

```text
bitter.language
bitter.highlighter
bitter.formatter
bitter.completion
bitter.code_action
bitter.status_item
bitter.sidebar
```

Bitty AI 声明：

```text
bitty-ai.model
bitty-ai.tool
bitty-ai.context
bitty-ai.memory
bitty-ai.compactor
bitty-ai.agent
bitty-ai.command
bitty-ai.ui
```

Statusline 声明：

```text
statusline.segment
```

Docker App：

```text
docker.action
docker.renderer
docker.inspector
```

于是 Bitty Plugin System 就不再只是：

```text
“加载 Lua 文件”
```

而开始变成：

```text
“组合不同 extension graph”
```

这个意义大很多。

---

# Manifest 也应该能表达这种关系

以后插件 manifest 里可能不只是：

```toml
name = "bitter-lsp"
```

还应该有类似：

```toml
[plugin]
name = "bitter-lsp"
version = "0.1.0"

[dependencies]
bitter = ">=0.3"

[contributes]
extensions = [
    "bitter.language-provider",
    "bitter.diagnostics-provider",
    "bitter.completion-provider",
]
```

或者更声明式一点：

```toml
[[extensions]]
point = "bitter.language"
id = "lsp"
```

这样 Bitty Plugin Manager 本身就知道：

```text
这个插件不能脱离 Bitter 工作。
```

如果用户安装：

```bash
bitty plugin add bitter-lsp
```

Bitty 就可以解析：

```text
bitter-lsp
    ↓ requires
bitter
```

依赖树。

这和普通 package manager 非常像。

---

# 但是权限不能继承

这个特别值得现在就确定下来。

假设：

```text
Bitter
```

拥有：

```text
filesystem.read
filesystem.write
process.spawn
```

并不意味着：

```text
bitter-theme-catppuccin
```

也自动获得这些权限。

应该是：

```text
Bitter permissions
          ≠
Extension permissions
```

例如一个主题插件只需要：

```text
bitter.theme
```

一个 LSP 插件可能需要：

```text
process.spawn
filesystem.read
```

Git 插件可能：

```text
process.spawn: git
```

也就是说：

> **依赖关系不能成为 capability escalation。**

否则只要：

```text
evil-plugin -> depends on bitter
```

就间接继承 Bitter 所有权限，整个 capability sandbox 就失去意义了。

这个原则对 Bitty AI 尤其重要，因为：

```text
bitty-ai
```

未来可能拥有很强的：

```text
filesystem
terminal
process
network
agent
```

能力。

AI extension 默认绝对不能继承全部。

---

# Service API 也应该版本化

因为一旦允许：

```text
plugin extends plugin
```

稳定 API 就不只是 Bitty API 了。

会产生：

```text
Bitty Plugin API
Bitter Extension API
Bitty AI Extension API
Statusline Extension API
```

例如：

```text
bitter.editor@1
bitty-ai.tools@1
bitty-ai.context@2
```

这样 Bitter 以后内部重构：

```text
TextBuffer
Document
View
Cursor
```

都没关系。

只要：

```text
bitter.editor@1
```

契约没变，生态插件就不会全部坏掉。

所以这种架构真正成熟以后，很像 Linux：

```text
Kernel API
 ↓
glibc / system libraries
 ↓
frameworks
 ↓
applications
 ↓
application plugins
```

而不是所有东西全部直接调用 kernel。

---

# 于是 Bitter 的定位也会非常有意思

Bitter 不应该变成：

> “一个塞满所有功能的编辑器插件。”

而应该类似：

> **Bitty 上的 programmable editor platform。**

它自己提供的可能只有：

```text
Document
TextBuffer
EditorView
Cursor
Selection
Undo/Redo

Editor commands
Editor modes
Extension registry

UI integration
```

甚至：

```text
LSP
Tree-sitter
Git
Formatter
Debugger
File Explorer
```

都可以不属于 Bitter Core。

于是一个最小安装：

```text
Bitty
└── Bitter
```

可能只是一个非常小的文本编辑器。

而：

```text
Bitty
└── Bitter
    ├── bitter-treesitter
    ├── bitter-lsp
    ├── bitter-git
    ├── bitter-dap
    ├── bitter-filetree
    └── bitter-ai
```

就已经变成一个完整 IDE。

这和 Neovim 的哲学非常一致。

---

# 最后其实形成的是“插件树”，而不是单层插件列表

以前通常会想：

```text
Bitty
├── plugin A
├── plugin B
├── plugin C
└── plugin D
```

但 Bitty 更适合成为：

```text
Bitty
│
├── Bitter
│   ├── LSP
│   │   ├── Rust
│   │   ├── Python
│   │   └── TypeScript
│   │
│   ├── Tree-sitter
│   ├── Git
│   └── DAP
│
├── Bitty AI
│   ├── Providers
│   ├── Tools
│   ├── Context
│   ├── Memory
│   └── Agents
│
├── Statusline
│   ├── Git
│   ├── System
│   └── AI
│
└── Docker
    └── Kubernetes
```

但从 Plugin Manager 的角度，仍然是一张：

```text
Dependency + Service + Extension Graph
```

而不是物理意义的父子进程树。

我觉得这个模型应该成为 Bitty Plugin Architecture 很核心的一条原则：

> **Every plugin may be an application, and every application may expose its own extension platform.**

再浓缩一点就是：

> **Bitty extends plugins; plugins extend ecosystems.**

这样 Bitter、Bitty AI、Statusline、Docker 以后都可以各自长出自己的生态，而 Bitty Core 不需要随着每个领域的复杂度一起膨胀。

是不是 Bitty 的 Core 部分也是可以升级扩展的，这个就是之前说的只安装 Bitty 本身其实没有非常多功能，如果要用 AI 相关的插件就需要把 Core 扩展 bitty-ai，那么之后才能使用 AI 相关的部分，就像是浏览器升级那样？之后应该不止是有 bitty-ai 这种的 Core，应该还会有更多的这种 Lua 扩展做不到的需要自己构建上游的部分吧？
对，而且我觉得这里最好把 Bitty 的扩展明确分成 **两条完全不同的扩展轴**：

```text
                  Bitty
                    │
        ┌───────────┴───────────┐
        │                       │
   Native Capability        Lua Extension
   扩展“Bitty 能做什么”       扩展“Bitty 怎么做”
        │                       │
   bitty-ai-core             bitty-ai
   editor primitives         Bitter
   native search/index       bitter-lsp
   media/runtime             themes
   future subsystems         workflows
```

你说的 `bitty-ai`，实际上很可能就是第一个证明这种架构必要性的模块。

## “只安装 Bitty”应该真的非常小

我会继续坚持这个原则。

最基础的 Bitty 可能只有：

```text
bitty
├── window / workspace
├── panel
├── terminal
├── renderer
├── input
├── config
├── lua runtime
├── plugin runtime
├── commands / events
├── basic UI primitives
├── capability system
└── package/runtime management
```

装完以后，它首先就是一个：

> 很快、很小、可编程、多 Panel 的终端环境。

并不应该因为：

```text
“Bitty 可以支持 AI”
```

就默认塞进去：

```text
HTTP client
SSE parser
LLM protocol
tool calling
agent runtime
MCP
memory
embedding
tokenizer
conversation DB
```

这些东西。

如果用户完全不用 AI，这些代码和依赖甚至都没有必要存在。

于是：

```bash
bitty plugin add bitty-ai
```

背后可能发现：

```text
bitty-ai
requires native capability:
    bitty-ai-runtime >= 1
```

然后 Bitty 安装相应的 native component。

最后：

```text
Bitty Base
   +
bitty-ai-runtime      ← native
   +
bitty-ai              ← Lua / UI / orchestration
   +
AI extensions
```

这比“所有东西都编译进一个超级大的 bitty 二进制”漂亮很多。

---

# 所以 `bitty-ai` 最好其实拆成两层

以后甚至最好不要把这两个概念都叫 `bitty-ai`。

例如：

```text
bitty-ai-runtime
```

负责 Rust / native 部分：

```text
Provider transport
Streaming
Tool execution runtime
Agent scheduling
Context engine
Token accounting
MCP transport
Process integration
high-performance storage primitives
```

然后：

```text
bitty-ai
```

是建立在上面的 Bitty Application / Plugin：

```text
Chat UI
Agent Panel
Commands
/plan
/review
/context
Settings
Workflows
Prompt configuration
```

再向上：

```text
bitty-ai
├── bitty-ai-openai
├── bitty-ai-anthropic
├── bitty-ai-memory
├── bitty-ai-dcp
├── bitty-ai-review
└── bitty-ai-whatever
```

于是形成三层：

```text
Native Primitive
       ↓
Framework Plugin
       ↓
Extension Plugin
```

这个模型其实非常强。

---

# 而且未来肯定不止 AI 需要 Native Extension

这一点你判断得很准。

Lua 非常适合：

```text
configuration
orchestration
commands
UI composition
workflows
event handling
extension registration
```

但有些东西天然不应该由 Lua 实现。

比如 Bitter。

你可以让 Bitter 的大量逻辑是 Lua：

```text
modes
commands
keymaps
LSP orchestration
plugins
editor behavior
```

但文本编辑器真正底层的：

```text
rope / piece table
UTF-8 / grapheme handling
selection
cursor model
incremental layout
syntax ranges
huge file virtualization
IME
undo tree
text shaping
rendering
```

非常适合 Rust。

于是以后可能有：

```text
bitty-editor-runtime
      ↓
Bitter
      ↓
bitter-lsp
bitter-git
bitter-treesitter
```

这里 Bitter 反而跟 bitty-ai 是同一种东西。

---

# 以后可能出现很多这样的 Native Capability

例如：

```text
Bitty Base
│
├── AI Runtime
│     └── bitty-ai
│
├── Editor Runtime
│     └── Bitter
│
├── Media Runtime
│     └── image/video/audio apps
│
├── Data Runtime
│     └── large table / graph / query apps
│
├── Search Runtime
│     └── full text / index / semantic search
│
├── Browser Runtime ?
│     └── web surface related apps
│
└── Dev Runtime
      └── debugger / profiler / devtools
```

不过这里要很克制。

不能变成：

> 只要 Lua 写起来麻烦，就加一个 native core。

判断标准应该是：

```text
Performance
Native OS integration
GPU integration
Process/runtime control
Security boundary
Large-data processing
Low-level protocol
Reusable primitive
```

满足这些条件才值得下沉。

比如：

```text
Git integration
```

大概率没必要做成 Bitty Native Core。

Lua 调：

```text
git
```

或者 helper 就够了。

但：

```text
high-performance editable text buffer
```

就是非常合理的 native primitive。

---

# 这其实越来越像浏览器的 Browser Engine + Web API

你刚才拿浏览器升级来类比很合适。

例如 Chrome/Firefox 本身升级以后，浏览器获得：

```text
WebGPU
WebRTC
WebAssembly
new CSS primitives
new Web APIs
```

然后网页才可以使用：

```js
navigator.gpu
```

不是网页自己用 JavaScript 实现 GPU driver。

Bitty 也是一样。

Lua Plugin 不应该自己实现：

```text
PTY
GPU text renderer
AI streaming transport engine
high-performance text editor buffer
process supervisor
```

而应该由 Bitty 提供：

```lua
bitty.ai.*
bitty.editor.*
bitty.process.*
bitty.ui.*
```

Lua 是调用这些能力。

于是非常像：

```text
Browser
    Web API
       ↓
    JavaScript App
```

对应：

```text
Bitty
   Bitty API
       ↓
    Lua Plugin
```

---

# 但是这里我会稍微修正“升级 Core”这个说法

因为如果什么都叫 Core，后面会很乱。

例如：

```text
bitty-core
bitty-ai-core
bitty-editor-core
bitty-media-core
bitty-data-core
```

慢慢就不知道什么才是真正的 Core 了。

我更建议术语上区分：

```text
Bitty Kernel/Core
```

永远是最基础的那层。

然后额外的是：

```text
Native Capabilities
Native Components
Runtime Modules
Subsystems
```

比如：

```text
bitty
├── core
├── runtime
├── plugin-host
├── ui
├── render
│
└── components
    ├── ai
    ├── editor
    ├── search
    └── media
```

产品层：

```text
Bitter requires editor capability
Bitty AI requires ai capability
```

这样语义会非常清楚：

> **Core 不会越来越大，但 Bitty 的 native capability set 可以越来越丰富。**

这是我觉得特别重要的区别。

---

# 甚至可以让安装过程看起来像“系统组件”

例如：

```bash
$ bitty component list

core        0.8.0    installed
terminal    0.8.0    installed
lua         0.8.0    installed
ui          0.8.0    installed

ai          0.8.0    not installed
editor      0.8.0    installed
media       0.8.0    not installed
```

然后：

```bash
bitty component add ai
```

或者用户根本不用关心，由插件依赖自动解决：

```bash
bitty plugin add bitty-ai

Resolving dependencies...

  bitty-ai
  └── requires component: ai >= 0.8

Install bitty-ai-runtime? [Y/n]
```

然后：

```bash
bitty plugin add bitter
```

自动得到：

```text
Bitter
└── editor runtime
```

这种体验就真的开始很像：

```text
browser feature
OS optional component
package manager
runtime dependency
```

的结合体了。

---

# 但有一个非常关键的工程问题：不要直接开放 Rust dylib 插件 ABI

这一点我认为应该提前定死。

如果所谓：

```text
native component
```

等于：

```text
第三方编译一个 Rust .so
Bitty dlopen()
```

长期会非常痛苦。

因为 Rust 没有稳定 ABI，而且还涉及：

```text
version mismatch
allocator
panic boundary
UB
security
crash isolation
platform differences
```

所以我会把 native extension 分成两类。

第一类是 **官方 native components**：

```text
bitty-ai-runtime
bitty-editor-runtime
```

由 Bitty project 自己发布，并和 Bitty version 有严格兼容关系。

这种可以深入进程内部。

第二类第三方 native extension，则更适合：

```text
Bitty
   │
   │ IPC / RPC
   ▼
Helper Process
```

例如：

```text
plugin.lua
   ↓
bitty.services
   ↓
native helper executable
```

哪怕 helper 是：

```text
Rust
Go
C++
Python
```

都无所谓。

Crash 也不会把整个 Bitty 带走。

这和你之前 Bitty 一直强调的 helper/service/capability 思路也是一致的。

---

# 所以最终 Bitty 很可能有四级扩展能力

我现在会把整个架构总结成：

```text
┌──────────────────────────────────────────────┐
│ Level 4 · Domain Extensions                  │
│ bitter-rust / bitty-ai-review / docker-k8s │
├──────────────────────────────────────────────┤
│ Level 3 · Applications / Framework Plugins   │
│ Bitter / Bitty AI / Docker / Mail           │
├──────────────────────────────────────────────┤
│ Level 2 · Native Capabilities                │
│ Editor / AI / Search / Media / ...          │
├──────────────────────────────────────────────┤
│ Level 1 · Bitty Core                         │
│ Panel / Terminal / UI / Lua / Renderer      │
│ Events / Commands / Capability / Services   │
└──────────────────────────────────────────────┘
```

其中非常关键的是：

```text
Level 1
```

应该长期保持稳定、小。

Bitty 以后增加能力，优先增加：

```text
Level 2
```

而不是不断把东西塞进 Core。

应用则建立在 Level 2 上。

生态继续建立在应用上。

---

这其实会让 Bitty 的定位进一步变得很有意思：

> **Bitty Core 更像 kernel/runtime，Native Components 像系统能力，Lua Apps 像应用框架，Plugins 则形成上层生态。**

所以你现在说“越来越像浏览器、操作系统”，其实不只是产品表面越来越像，连**扩展模型本身都开始出现类似操作系统和浏览器的层级化结构了**。

而且我觉得 `bitty-ai` 很适合作为第一个实验这种 **Optional Native Capability + Lua Framework + Extension Ecosystem** 三层模型的模块。如果这套跑通，之后 Bitter、Search、Media 等几乎都能复用同一种机制。

那么类似 Bitty 的程序有哪些呢？VScode, Emacs 还是哪些呢？或者说没有完全一致的那种
对，**没有一个现成程序和你现在规划的 Bitty 完全一致**。更准确地说，Bitty 正在把几种已经被证明有效的架构思想组合到一个“terminal-first”的系统里。

如果一定要找“亲戚”，我会这样看：

| 系统                   | 和 Bitty 最像的部分                                           | 关键差异                                              |
| -------------------- | ------------------------------------------------------- | ------------------------------------------------- |
| **Emacs**            | 可编程环境、应用建立在应用之上、编辑器只是其中一种能力                             | UI/渲染架构和现代 GPU 应用差别很大                             |
| **VS Code**          | Workbench、Panel、Extension API、插件生态、IDE 可由扩展组成           | 核心仍然 editor/workspace-centric，大量自定义 UI 依赖 WebView |
| **JupyterLab**       | Application Shell、Widget、Service、Plugin、Extension Point | 面向 notebook/scientific computing，浏览器技术栈           |
| **Eclipse RCP**      | “最小平台 + 可选组件 + 服务 + Extension Point + 应用”               | Java/SWT 时代的 Rich Client，产品定位偏传统桌面平台              |
| **Neovim**           | Lua 配置、Lua Plugin、Core primitives、插件组成 IDE              | UI 模型主要仍是文本编辑器                                    |
| **浏览器**              | Runtime、权限、API、应用、应用扩展、渲染引擎                             | Web 是主要应用模型，而不是 terminal/native UI                |
| **Zed**              | Rust、高性能 native editor、扩展提供 LSP/grammar/debug/MCP 等     | 仍明确是 code editor                                  |
| **tmux / WezTerm 等** | Terminal、Pane、Session、multiplexing                      | 应用平台能力远弱于你现在规划的 Bitty                             |

### 哲学上最像：Emacs

如果只选一个，我觉得现在的 Bitty **思想上越来越像 Emacs，而不是 VS Code**。

GNU Emacs 官方自己就是把功能组织成 Emacs Lisp packages，大量能力都可以由 Lisp 扩展和替换。([GNU][1])

Emacs 最关键的思想并不是“一个很强的编辑器”，而是：

```text
Emacs Runtime
    ↓
Emacs Lisp
    ↓
packages
    ↓
Editor / Mail / Git / IRC / Shell / Notes / ...
```

Bitty 正在形成：

```text
Bitty Runtime
    ↓
Lua + Native Capabilities
    ↓
Applications
    ↓
Terminal / Bitter / AI / Docker / Git / Mail / ...
```

这两者的共同点是：

> **宿主程序提供 primitives，而真正的产品形态由扩展生态决定。**

甚至你刚才说的：

```text
Bitty
└── Bitter
    └── bitter-lsp
```

本身就非常 Emacs。

Emacs 中也经常不是所有 package 都直接面对“Emacs Core”，而是一个 package 建立抽象，其他 package 再扩展它。

---

## 产品体验上最像：VS Code

VS Code 则更像 Bitty 未来用户实际看到的东西。

VS Code 把整个界面称为 **Workbench**，里面有 Activity Bar、Side Bar、Panel、Editor Group、Status Bar，并允许扩展向这些区域贡献 UI。([Visual Studio Code][2])

例如：

```text
VS Code Workbench
├── Editor
├── Terminal
├── Explorer
├── Git
├── Debugger
└── Extension Views
```

和未来 Bitty：

```text
Bitty Workspace
├── Terminal Panel
├── Bitter Panel
├── AI Panel
├── Git Panel
├── Docker Panel
└── Custom App Panel
```

视觉和使用体验会有不少相似之处。

但这里也有一个很大的不同。

VS Code 如果扩展需要完全自定义 UI，通常会使用 WebView，本质上是一个类似 iframe 的 HTML/CSS/JS 环境；官方甚至明确建议 WebView 应谨慎使用，因为相对重。([Visual Studio Code][3])

而你现在给 Bitty 规划的是：

```text
Lua
 ↓
Bitty Native Widget Tree
 ↓
Rust UI Runtime
 ↓
wgpu
```

所以 Bitty 可能没有：

```text
Extension
 ↓
WebView
 ↓
Chromium
```

这条路线。

这是一个挺重要的区别。

---

## 架构上一个非常值得研究的对象：JupyterLab

我觉得 **JupyterLab 是你现在特别值得看一下的项目**。

因为它已经有很多你最近正在给 Bitty 命名的概念：

```text
Application Shell
Plugin
Service
Token
Extension Point
Widget
Workspace
Terminal
Editor
Command
Status Bar
```

JupyterLab 官方的 extension architecture 中，核心组件通过 token 向插件暴露服务，而且很多核心功能本身就是 plugin；它的 shell 有 `main / left / right / top / bottom` 等区域，可以动态加入 widget。([JupyterLab 文档][4])

甚至它已经有：

```text
file editor plugin
completion plugin
LSP plugin
terminal plugin
workspace manager
service manager
plugin manager
```

这些东西。([JupyterLab 文档][4])

这和我们前面讨论：

```text
Bitty
├── Service Registry
├── Extension Point
├── Panel Runtime
├── Bitter
│   └── bitter-lsp
└── Bitty AI
    └── bitty-ai-memory
```

有明显相似性。

区别是 JupyterLab 是 Web/Notebook-first，而 Bitty 是：

> **Terminal / native workspace-first。**

---

## “Core 可以很小，安装组件之后变成完全不同的软件”——Eclipse RCP 更像

这一点其实 VS Code 和 Emacs 都不如 **Eclipse RCP** 类比准确。

Eclipse 官方对 Rich Client Platform 的描述就是：平台只需要一组最小插件，然后可以基于动态插件模型和 extension points 建造各种 client application，而不一定是 IDE。([Eclipse帮助][5])

这特别像你刚才说的：

```text
Bitty Base
+
Editor Runtime
+
Bitter
```

得到编辑环境。

或者：

```text
Bitty Base
+
AI Runtime
+
Bitty AI
```

得到 AI Agent Workspace。

或者全部都不要：

```text
Bitty Base
```

就只是一个强大的 terminal/workspace。

所以从纯软件架构来看：

> **Bitty 的 Native Component / Application / Extension Point 模型，可以认真研究 Eclipse RCP。**

当然，不是让 Bitty 学 SWT 或 Java，而是研究它几十年里怎么解决：

```text
component dependency
extension points
services
plugin lifecycle
optional components
application composition
```

这些问题。

---

## Neovim 更像 Bitty 的“早期阶段”

Neovim 内置 Lua，并且 Lua 可以直接访问其 API；官方把 API 分为 Vim API、Nvim API 和 Lua API，Lua 插件则可以直接建立在这些能力之上。([Neovim][6])

同时 Neovim 还有 RPC API，允许 GUI、remote plugin 和其他进程控制 Neovim。([Neovim][7])

所以以前我们说：

```text
Bitty ≈ Neovim-like terminal
```

是相当合理的。

但现在 Bitty 已经开始往：

```text
Panel
Native UI
Application
Service
Capability
Native Component
Extension Point
```

走了。

于是 Bitty 和 Neovim 的距离正在拉大。

Neovim 最核心的对象始终还是：

```text
Buffer
Window
Tabpage
Editor
```

Bitty 则逐渐变成：

```text
Window
Workspace
Panel
Activity
Application
```

所以可以说：

> **Neovim 是 Bitty 插件哲学的重要祖先，但不是最终形态最接近的产品。**

---

## Zed 可以研究“Native Primitive 怎么给 Extension 用”

Zed 也是很好的现代参考，因为它同样是 Rust/native、高性能编辑器，而且扩展已经可以提供 language、grammar、language server、debug adapter、MCP server、agent server、theme、snippet 等能力。它当前扩展市场也是围绕这些 extension categories 组织的。([Zed][8])

所以 Bitter 这一部分尤其可以参考：

```text
Rust native editor core
       ↓
extension API
       ↓
language extension
LSP extension
DAP extension
theme
```

只是 Zed 不准备成为通用 application shell，而 Bitter 只是 Bitty 上的一个 application。

也就是：

```text
Zed
≈
Bitty + Bitter + 一部分 Bitter plugins
```

而不是：

```text
Zed ≈ Bitty
```

---

# 最终 Bitty 反而像这些东西的“交集”

我觉得现在最准确的描述其实是：

```text
                Emacs
          programmable system
                 │
                 │
 Eclipse RCP ── Bitty ── VS Code
component model   │       workbench
                 │
                 │
             Browser
       capability/runtime
                 │
                 │
              Neovim
           Lua/plugin UX
```

底下再加：

```text
tmux / Ghostty / WezTerm
        │
 terminal substrate
```

所以 Bitty 并不是：

> “做一个更强的 terminal emulator。”

越来越准确的说法是：

> **Bitty 是一个以终端为原生能力之一、以 Panel/Workspace 为交互模型、以 Rust Native Capability + Lua Application 为扩展模型的 programmable application workspace。**

甚至“terminal-first”比“terminal emulator”更加准确。

而且这也解释了为什么你现在感觉它一会儿像 Emacs、一会儿像 VS Code、一会儿像浏览器、一会儿又像小型 OS：

**不是类比出了问题，而是 Bitty 确实开始进入这些系统共同所在的“可编程应用平台”这一层。**

如果之后继续设计这个方向，我认为最值得系统研究的三个项目反而是 **Emacs + JupyterLab + Eclipse RCP**：Emacs 看“可编程环境”，JupyterLab 看“现代 Service/Widget/Plugin composition”，Eclipse RCP 看“Core/Component/Extension Point”。VS Code 和 Neovim则更适合参考具体 UX 和开发者生态。

[1]: https://www.gnu.org/software/emacs/manual/html_node/emacs/Packages.html?utm_source=chatgpt.com "Packages (GNU Emacs Manual)"
[2]: https://code.visualstudio.com/api/extension-capabilities/extending-workbench?utm_source=chatgpt.com "Extending Workbench | Visual Studio Code Extension API"
[3]: https://code.visualstudio.com/api/extension-guides/webview?utm_source=chatgpt.com "Webview API | Visual Studio Code Extension API"
[4]: https://jupyterlab.readthedocs.io/en/latest/extension/extension_points.html?utm_source=chatgpt.com "Common Extension Points — JupyterLab 4.7.0a1 documentation"
[5]: https://help.eclipse.org/latest/topic/org.eclipse.platform.doc.isv/guide/rcp.htm?utm_source=chatgpt.com "Building a Rich Client Platform application"
[6]: https://neovim.io/doc/user/lua-guide/?utm_source=chatgpt.com "Lua-guide - Neovim docs"
[7]: https://neovim.io/doc/user/api?utm_source=chatgpt.com "Api - Neovim docs"
[8]: https://zed.dev/docs/extensions/languages?utm_source=chatgpt.com "Language Extensions | Language Extensions"

