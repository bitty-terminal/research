不应该全部放在 Lua 插件里。**真正的 Agent Harness 可以主要由 Lua 组装，但安全边界不能由 Lua 自己保证。**

更准确的架构应该是：

```text
bitty
  └─ Host Security / Execution / Panel / FS / PTY
        ↑ 不可绕过的硬边界

bitty-ai
  └─ Agent / Task / Delegation / Budget / Capability
        ↑ AI 语义层的硬约束

Lua Harness
  └─ workflow / strategy / scheduling policy / UX
        ↑ 可以自由组合，但不能扩大权限

User / Project Policy
  └─ ~/.config/bitty / .bitty
        ↑ 在上层允许范围内进一步收紧
```

核心原则可以直接定成一句：

> **Lua 可以决定“怎么做”，但不能决定“有没有权做”。**

## 1. “不要一次开十几个 Subagent”：Lua 决策，Core 限制

例如一个 `/review` Lua workflow 可以决定：

```lua
if task.size == "small" then
    agents = 1
elseif task.size == "medium" then
    agents = 3
else
    agents = 6
end
```

这是 Harness policy，很适合 Lua。

但真正允许的最大值不能由 Lua 决定：

```text
Host ceiling:      max_agents = 8
User preference:   max_agents = 4
Project policy:    max_agents = 3
Lua requests:      6

effective = 3
```

而且一个 Agent 再 spawn Subagent 时应该遵循 **authority attenuation**：

```text
Commander
budget = 4 agents
    │
    ├── Agent A
    ├── Agent B
    └── Agent C
```

A 不能自己突然：

```text
Agent A
 ├─ 10 subagents
 ├─ 10 subagents
 └─ ...
```

所以：

```rust
DelegationBudget {
    max_children,
    max_depth,
    max_parallel,
    token_budget,
    cost_budget,
    execution_budget,
}
```

应该由 `bitty-ai` runtime 强制执行。

Lua 只能在 budget 内调度。

---

# 2. CPU / RAM / GPU / Disk 资源绝对不能靠 Lua“自律”

这属于真正的 resource enforcement。

Lua 可以声明：

```lua
job = exec {
    command = "python train.py",
    resources = {
        memory = "12GiB",
        gpu_memory = "8GiB",
        cpu = 8,
    }
}
```

但谁来判断：

```text
现在机器只有 16 GB RAM
GPU 只剩 4 GB VRAM
磁盘只剩 3 GB
```

以及谁阻止任务启动？

应该是：

```text
Lua
 ↓ request
bitty-ai Resource/Budget policy
 ↓
bitty Execution Supervisor
 ↓ enforce
OS
```

Linux 后面可以：

```text
cgroup v2
RLIMIT
process groups
GPU observation/backend
filesystem quota / free-space guard
```

Windows 可以：

```text
Job Objects
```

macOS 再用对应机制。

Lua 本身不应该成为安全边界。

否则插件 bug：

```lua
for i = 1, 1000 do
    spawn("cargo build")
end
```

就把机器打挂了。

正确结果应该是 Core 在第 N 个时直接返回：

```text
ResourceLimitExceeded
ExecutionBudgetExhausted
ConcurrencyLimitReached
```

---

# 3. Agent 之间“不争抢 Panel”：需要 Core lease，Lua 管调度纪律

这一点特别适合你现在 Bitty 的设计。

不能只在 system prompt 里写：

> “请不要和其他 Agent 抢 Panel。”

应该真的有：

```text
Panel P7

read leases:
  Agent A
  Agent B
  Human

interactive writer:
  Agent A generation=12
```

于是 B 想输入：

```text
panel.write(P7)
```

Core 直接：

```text
Denied:
interactive writer lease belongs to Agent A
```

如果 commander 要交给 B：

```text
handoff P7 A -> B

generation 12 fenced
generation 13 assigned to B
```

这样才不会出现：

```text
Agent A: cargo test
Agent B: Ctrl+C
Agent A: cd ...
Human:   typing
Agent B: rm ...
```

全部同时往一个 PTY 写。

### Lua Harness 做什么？

Lua 决定策略：

```text
优先给每个 worker 独立 Execution
确实需要 interactive Panel 时才 acquire
结束后立即 release
不要长期占着不放
```

也就是说：

> **纪律由 Lua 表达，互斥由 Core 保证。**

---

# 4. `.env`、`.ssh`、credentials 不能只是 Prompt 规则

这个更不能写成：

```text
System Prompt:
Never read .env.
```

模型迟早可能：

```bash
cat .env
```

或者：

```python
open(".env").read()
```

所以 filesystem tool 应该存在真正的 authorization layer。

例如默认：

```text
deny:
  **/.env
  **/.env.*
  ~/.ssh/**
  ~/.gnupg/**
  ~/.aws/credentials
  ~/.config/.../tokens
  browser credential stores
```

但这里不要简单依赖文件名，因为：

```text
secret.txt
credentials.json
prod-config.yaml
```

也可能有 secret。

更好的模型是：

```text
FilesystemScope
+
SensitivePathPolicy
+
Secret Detection/Redaction
```

例如：

```text
Agent asks read(".env")

        ↓

FS authorization

        ↓

SensitiveResource

        ↓

DENY
or user consent
```

Lua 可以决定 `/review` workflow 根本不请求这些路径，但 **Lua 插件自己也不应该有绕过这一检查的能力**。

---

# 5. Secret 最好不要进入 Agent Context

比如需要 GitHub token：

最差设计：

```text
Agent:
GITHUB_TOKEN=ghp_xxxxx
```

好一些：

```text
Agent knows:
credential handle = secret://github/default
```

然后执行：

```text
execution.spawn(
    credential_handles = [github]
)
```

Host 在 process environment 里注入：

```text
GITHUB_TOKEN=...
```

但是：

```text
Agent context
execution logs
Lua logs
Panel history
```

都拿不到原始值。

也就是：

```text
Secret value
   │
   ├── Host Credential Store
   │
   └── child process environment
            ↑
          opaque
            ↑
Agent ── secret handle
```

这比“告诉 Agent 不要打印密钥”强太多。

---

# 6. root / sudo 同样不能靠 Agent 自觉

默认应该类似：

```text
privilege escalation = denied
```

Agent：

```bash
sudo pacman -S ...
```

不能因为模型认为合理就直接执行。

可以有：

```text
PrivilegeRequest {
    reason,
    command,
    target,
}
```

然后：

```text
Agent
 ↓
request elevation
 ↓
Host policy
 ↓
User approval
 ↓
one scoped privileged execution
```

而不是给 Agent：

```text
sudo session forever
```

更不能：

```text
Agent knows sudo password
```

Bitty 可以提供“需要提升权限”这个 mechanism，但是否允许属于用户 policy。

---

# 7. 所以应该把规则分成 Hard Safety 与 Harness Policy

这是整个设计里非常重要的分类。

| 规则                       | 类型                    | 实现位置                       |
| ------------------------ | --------------------- | -------------------------- |
| 最大 Agent 数               | Hard ceiling + policy | `bitty-ai` + Lua           |
| 最大 Subagent depth        | Hard ceiling          | `bitty-ai`                 |
| 是否需要 2/3/5 个 Agent       | Strategy              | Lua                        |
| RAM / CPU 限额             | Hard safety           | `bitty`                    |
| GPU / VRAM budget        | Hard/resource policy  | `bitty` / resource backend |
| Disk free-space guard    | Hard safety           | `bitty`                    |
| Task token/cost budget   | Hard budget           | `bitty-ai`                 |
| Agent 调度顺序               | Workflow              | Lua                        |
| Panel writer exclusivity | Hard safety           | `bitty`                    |
| Agent 应优先使用独立 Panel      | Workflow              | Lua                        |
| `.env` 禁止读取              | Authorization         | `bitty`                    |
| Secret handle            | Security mechanism    | `bitty`                    |
| 是否允许某项目读取特殊文件            | User/project policy   | config                     |
| root escalation          | Authorization         | `bitty`                    |
| 自动 retry 策略              | Workflow              | Lua                        |
| 是否 parallel review       | Workflow              | Lua                        |
| Task delegation          | AI semantics + policy | `bitty-ai` + Lua           |

所以可以理解为：

```text
Hard Safety
    ↓
不可突破

Policy
    ↓
Lua 可组合

Strategy
    ↓
Agent 可选择
```

---

# 8. 一个非常重要的规则：Lua 插件不能“给自己加权限”

假设插件写：

```lua
local agent = bitty.agent.spawn({
    filesystem = "all",
    root = true,
    max_agents = 100,
})
```

这里绝对不能意味着它真的得到这些权限。

它应该只是 **request**：

```text
Lua request
    ↓
Capability Engine
    ↓
User / Project / Host Policy
    ↓
Effective Capability
```

所以权限计算可以形成很漂亮的交集模型：

```text
EffectiveCapability =

Host Ceiling
∩ User Policy
∩ Project Policy
∩ Parent Delegation
∩ Task Grant
∩ Agent Request
```

任何下层只能：

```text
缩小权限
```

不能扩大。

比如：

```text
User:
FS = project/**

Commander:
FS = project/src/**

Worker:
request = project/**

effective:
project/src/**
```

Worker 不可能把权限重新扩成整个 project。

---

# 9. Subagent 也必须遵守“能力衰减”

这是 Multi-Agent 最需要防止的问题之一。

不要：

```text
Commander permissions
        ↓ copy
Worker permissions
        ↓ copy
Subworker permissions
```

应该：

```text
Commander
FS: repository
Exec: build/test
Agents: 4
       │
       ▼
Worker
FS: src/parser/**
Exec: cargo test parser
Agents: 1
       │
       ▼
Reviewer
FS: read-only src/parser/**
Exec: tests only
Agents: 0
```

也就是越往下：

```text
Capability(child) ⊆ Capability(parent)
```

这个必须由 `bitty-ai` authority engine 强制。

Lua workflow 可以说：

```lua
spawn_reviewer {
    profile = "readonly-reviewer"
}
```

但实际 capability attenuation 由 runtime 完成。

---

# 10. 甚至 “Commander” 这个角色都应该只是 Lua 概念

这一点反而非常符合你所说的：

> 真正的 Agent Harness 是 Lua 插件。

我会让 Rust Core 完全不知道：

```text
Commander
Planner
Researcher
Reviewer
Debugger
Architect
```

Core 只知道：

```text
Agent
Task
Grant
Budget
Delegation
Mailbox
Execution
Lease
```

然后 Lua：

```lua
team = agent.team {
    commander = agent {
        profile = "commander",
    },

    workers = {
        agent { profile = "implementer" },
        agent { profile = "reviewer" },
    }
}
```

甚至其他社区插件完全可以设计：

```text
Flat swarm
Manager-worker
Planner-executor-reviewer
Debate
MapReduce
Pair programming
RACI team
```

Core 都不用更新。

这就是非常好的：

> **mechanism in Rust, organization model in Lua**

---

# 11. 用户自己遵守的规则，则是另外一层

你说：

> 用户不能上传密钥、密码、不该随便进入 root 权限。

这里应该分成两种。

### 能技术防止的，不要只教育用户

例如：

```text
Agent 请求 sudo
→ confirmation

Agent 读取 .env
→ deny

Agent 输出疑似 token
→ redact

Agent 要创建 20 workers
→ deny
```

尽量由系统兜底。

### 系统无法完全控制的，才属于 User Hygiene

例如用户直接输入：

```text
我的密码是 xxxxx
```

Agent 已经收到了，这时候系统只能尽量：

```text
warning
redaction
don't persist
don't log
```

所以可以提供安全提示：

```text
Do not paste:
- passwords
- private keys
- API keys
- recovery codes
```

但不要把系统安全依赖在：

> “用户应该懂安全。”

---

# 12. 最终最好形成四层 Policy Stack

我觉得这可以直接成为 Bitty AI 的一个正式设计：

```text
┌─────────────────────────────────────┐
│ Lua Harness Policy                  │
│ team shape / workflow / strategy    │
├─────────────────────────────────────┤
│ Project Policy (.bitty)             │
│ allowed paths / task constraints    │
├─────────────────────────────────────┤
│ User Policy                         │
│ max agents / root / network / etc.  │
├─────────────────────────────────────┤
│ Host Security Ceiling               │
│ capability / sandbox / resources    │
└─────────────────────────────────────┘
```

优先级不是“下层覆盖上层”，而是：

```text
所有限制取交集 / 最严格结果
```

例如：

```text
Host max agents     = 16
User max agents     = 6
Project max agents  = 4
Lua wants           = 8

=> 4
```

路径同样：

```text
Host:
~/Projects/**

User:
~/Projects/bitty/**

Project:
repository/**

Task:
src/**

Worker:
src/parser/**

=> src/parser/**
```

---

# 13. Lua API 也应该设计成“受控请求”，而不是 raw capability

以后 Lua API 最好看起来是：

```lua
local worker = agent.spawn {
    task = task,
    capability = {
        fs = {
            read = { "src/parser/**" },
            write = { "src/parser/**" },
        },
        exec = {
            "cargo test *",
            "cargo clippy *",
        },
    },
    budget = {
        agents = 0,
        memory = "4GiB",
        tokens = 100000,
    }
}
```

而不是：

```lua
os.execute(...)
io.open(...)
```

至少对于 Harness sandbox 来说，这些 raw escape hatch 应该被限制。

Lua 调：

```text
agent.spawn()
execution.run()
panel.acquire()
fs.read()
```

每一个最终都进入统一 authorization path。

这样即使第三方 Harness 写得很烂：

```lua
while true do
    agent.spawn(...)
end
```

系统最多不停收到：

```text
BudgetExceeded
```

而不是机器直接炸掉。

---

## 我认为这会形成 Bitty 一个非常强的设计特点

上游其实不需要实现一个固定的：

```text
Bitty Agent Harness
```

而是实现：

```text
Agent Runtime primitives
Task primitives
Delegation primitives
Capability primitives
Budget primitives
Execution primitives
Panel lease primitives
Context primitives
Provider primitives
Mailbox/event primitives
```

然后官方 `bitty-ai` Lua 插件只是其中一种 Harness：

```text
Bitty Official Harness
```

社区以后可以自己写完全不同的。

但无论 Harness 写成什么样：

```text
无限 swarm
恶意插件
有 bug 的插件
失控 Agent
错误 delegation
```

都突破不了 Core 的：

```text
capability
budget
resource
secret
filesystem
privilege
execution
lease
```

边界。

所以最准确的划分就是：

> **Agent 的“行为规范”大量属于 Lua；Agent 的“不可逾越边界”绝不能属于 Lua。**

这也是后面设计 Bitty Lua API 时最值得坚持的一条原则。
