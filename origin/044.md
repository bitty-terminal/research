在设计 bitty-ai 的时候，我想起了我用 opencode 的时候，agent 会用 pty_spawn 这种工具，比如这样的：
# pty_spawn [command=bash, description=Watch devtools PR91 checks, notifyOnExit=true, timeoutSeconds=3600, title=Devtools PR91 checks, workdir=/mnt/data/Workspace/Projects/bitty-terminal]
<pty_spawned>
ID: pty_bdb9c3f9
Title: Devtools PR91 checks
Command: bash -c export HTTPS_PROXY=$NETWORK_PROXY; gh pr checks 91 --repo bitty-terminal/bitty-devtools --watch --interval 30; echo EXIT=$?
Workdir: /mnt/data/Workspace/Projects/bitty-terminal
PID: 3390404
Status: running
NotifyOnExit: true
TimeoutSeconds: 3600
</pty_spawned>

<system_reminder>
Completion signal for this session is the future `<pty_exited>` message.
If you only need to know whether the command finished, do not call `pty_read`; wait for `<pty_exited>`.
Never use sleep plus `pty_read` loops to check completion for this session.
Call `pty_read` before exit only if you need live output now, the user explicitly asks for logs, or the exit notification reports a non-zero status and you need to investigate.
</system_reminder>
那么就是只要这个任务结束，那么就会把这个结果再输入给 Agent，如：
<pty_exited>
ID: pty_bdb9c3f9
Description: Watch devtools PR91 checks
Exit Code: 0
TimeoutSeconds: 3600
Timed Out: no
Output Lines: 64
Last Line: EXIT=0
</pty_exited>

Use pty_read to check the full output.
 QUEUED 
这样的，在不同的 Agent Harness 应该是不一样的，这个是 OpenCode 的设计，你应该需要再看看 Codex, Claude Code, Cursor, Kiro 等等来看看是怎么设计的。
但是我想起了我以前用过的另一个工具——https://github.com/Nukesor/pueue，你可以了解下这个工具的机制，我觉得 bitty-ai 可以借鉴这个的很多设计，只不过呢，在 bitty-ai 需要考虑这些，在 Multi Agents, Agent Team 这种多智能体的情况下，Agent 从自己之前 Panel 到了另一个 Panel 了，这个任务完成了后应该还是输出给他的吧，然后如果出现中断，比如网络中断、用户不小心手动终止了 Agent、Agent 自己在宣布结束之前应该会看自己是否有没有结束的任务吧？然后就是如何跑非常长的任务要怎么办，比如训练模型、下载大文件等等，然后如果任务异常，比如网络中断、OOM 了等等情况要如何解决，一个这种任务是否可以被别的 Agent 看到呢等等情况。
那么你可以看看各个 Agent Harness 和 pueue 的设计，看看 bitty-ai 可以怎么设计这个部分，因为这个部分是跟执行命令相关的，所以还是需要好好讨论怎么设计才行

## 核心结论

这个方向很值得纳入 Bitty，但我会把问题再往下抽象一层：

> **Bitty 不应该设计一个更强的 `pty_spawn`，而应该设计一个独立于 Agent、Panel、Conversation 的 `Execution Supervisor / Job Service`。**

`pty_spawn` 只是其中一种执行后端。真正需要成为一等公民的是 **Job / Execution**。

这其实与你现在 `bitty-ai` 文档里的方向已经高度一致了。当前 R1 已经明确倾向：

```text
Agent -> ExecutionContext <- Panel
```

`ExecutionContext` 才是执行主体，Panel 只是可选 projection；结构化任务甚至可以既没有 shell，也没有 Panel。

而 `agent-coordination.md` 又已经明确把 Agent、Execution、Workspace、Tooling service、Evidence、Panel 的 lifetime 分离，并提出 mailbox、lease、generation fencing、handoff、critical message 等概念。

所以你这次想到的 **Pueue + background job + Multi-Agent notification**，其实正好补上目前架构里还没有真正落地的那一块：

> **ExecutionContext 下面，到底怎么管理长期 process/job。**

---

# 1. 现在几个 Harness 实际上走了几条不同路线

| 系统                      | 核心模型                       | 值得 Bitty 学的                                                  | 主要不足                                    |
| ----------------------- | -------------------------- | ------------------------------------------------------------ | --------------------------------------- |
| OpenCode `opencode-pty` | PTY Session                | `spawn/read/write/list/kill`、exit notification、ring buffer   | 主要还是 PTY abstraction，生命周期和 Harness 联系较紧 |
| Codex                   | Exec Session               | 独立 process protocol、output event、signal、write、exit           | 模型层仍容易退化成 polling                       |
| Claude Code             | Background Task / Monitor  | background task、`/tasks`、event-like Monitor、Agent supervisor | Bash task 和 Agent task 有一定混合            |
| Cursor                  | Agent + Run + Subscription | durable run、event stream、resume cursor、外部事件 wake-up          | 更偏 cloud agent，而非本地 process manager     |
| Kiro                    | Delegate Task              | async delegation、独立 task ID、progress/result                  | 官方文档明确目前偏 session-scoped                |
| Pueue                   | Daemon-managed Task        | daemon、queue、groups、dependency、pause/resume、durable logs     | 不理解 Agent、capability、Panel、handoff      |

其中我认为 **Pueue + Cursor + OpenCode PTY** 三者组合，对 Bitty 最有启发。

---

# 2. Pueue 最值得借鉴的不是 Queue，而是“任务脱离终端”

Pueue 的关键特征不是简单地 `command &`。它有独立 `pueued` daemon，所以：

```text
Terminal A
   │
   └── pueue add ...
           │
           ▼
        pueued
       /      \
    Job 1    Job 2
```

终端关闭、SSH 断开，都不代表 Job 消失。它还提供 group 并发数、dependency、pause/resume、restart、wait、日志持久化、环境变量快照、callback 等。

内部也是典型的：

```text
Client requests
       │
       ▼
┌──────────────┐
│    Daemon    │
├──────────────┤
│ State        │
│ Scheduler    │
│ Processes    │
│ IPC          │
└──────────────┘
```

一个 task handler loop 管调度、结束、dependency、delayed task，另一个 loop 处理 IPC。

它的 Task 本身已经有：

```rust
Task {
    id,
    command,
    path,
    envs,
    group,
    dependencies,
    priority,
    label,
    status,
}
```

并区分 `Queued / Running / Paused / Done`，以及 `Success / Failed / FailedToSpawn / Killed / DependencyFailed` 等 outcome。

这就是 Bitty 可以借的骨架。

但是不能直接照搬 Pueue。Pueue 自己也明确说明它面向 **single-user human interaction**，不是 distributed / multi-user / sophisticated scheduler。

Bitty 还需要多出：

```text
Agent ownership
Task binding
capability / permission
mailbox
handoff
generation fencing
Panel projection
event delivery
evidence
```

---

# 3. OpenCode 的 `pty_spawn` 已经证明了 event-driven 比 polling 好

你截图里的机制来自这一类 OpenCode PTY 插件设计。

它暴露：

```text
pty_spawn
pty_read
pty_write
pty_list
pty_kill
```

同时 `notifyOnExit=true` 后，任务结束时直接发送 `<pty_exited>`，而不是让 Agent 一遍遍：

```text
pty_read
sleep
pty_read
sleep
pty_read
```

内部是 PTY + rolling buffer + notification。

这个思想完全应该保留。

但我不建议 Bitty 把核心 abstraction 命名为 `PTY Session`。

因为：

```bash
cargo test
cargo build
git clone
wget ...
python train.py
gh pr checks --watch
```

绝大多数任务实际上都不需要 PTY。

PTY 更适合：

```text
REPL
interactive debugger
ssh
dev server with TTY semantics
fzf
vim
interactive prompt
```

Bitty 当前自己的 R1 也已经明确支持：

> supervised structured process，只有需要时才创建 PTY。

所以应该是：

```text
Execution
 ├── Pipe Process
 └── PTY Process
```

而不是：

```text
Everything
 └── PTY
```

---

# 4. Codex 值得学习的是“Process Protocol”，但不要学习 polling

现在 Codex 的 exec-server 已经把进程管理抽成独立 JSON-RPC：

```text
process/start
process/read
process/write
process/signal
process/terminate

process/output   // event
process/exited   // event
process/closed   // event
```

这是很干净的。

但 Codex 的模型工具侧目前仍存在：

```text
exec_command
    ↓
command 没结束
    ↓
process/session ID
    ↓
write_stdin(...)
    ↓
write_stdin(...)
```

官方仓库代码里 `exec_command` 和 `write_stdin` 就是这么组织的。

社区 issue 也已经暴露这种模型的典型问题：长任务进入 `write_stdin` polling 后，会增加 round-trip/token 开销，甚至模型遗忘原 session 后重新执行相同命令。([GitHub][1])

所以 Bitty 最好从一开始就确定：

> **等待是 Runtime 的工作，不是 LLM 的工作。**

Agent 不应该为了等待进程结束而消耗一个推理 turn。

---

# 5. Claude Code 给了另一个很好的启发：Monitor

现在 Claude Code 的 `Bash` 已经支持：

```ts
run_in_background?: boolean
```

同时还有单独的：

```ts
Monitor {
    command,
    description,
    timeout_ms?,
    persistent?
}
```

Monitor 会把 stdout line 作为事件交给 Claude，而不是要求模型不断 poll。([Claude][2])

而 Claude Code 现在还区分：

```text
background shell task
subagent
background session
agent team
```

`/tasks` 管当前 session 的后台工作，`claude agents` 则管理独立 background sessions。([Claude][3])

这说明一个很重要的问题：

> **Process Job 和 Agent Task 不应该是同一个东西。**

Claude Code 现在已经逐渐往这个方向拆了。

---

# 6. Cursor 最值得 Bitty 学的是 Subscription，而不是 Cloud VM

Cursor Cloud Agent 目前已经有非常清楚的两个层次：

```text
Agent
 └── Run
```

Run 有自己的：

```text
RUNNING
FINISHED
ERROR
CANCELLED
EXPIRED
```

并通过 SSE 推：

```text
status
assistant
thinking
tool_call
heartbeat
result
error
done
```

断线以后还能通过 `Last-Event-ID` 恢复 event stream。([Cursor][4])

更重要的是 Cursor 已经提供 **Subscriptions**：

> Agent 可以订阅 GitHub CI、PR activity、Slack reply、timer 等事件；Agent 当前 turn 可以结束，等事件发生后再被唤醒继续工作。([Cursor][5])

这实际上就是我认为 Bitty 最终应该达到的模型：

```text
Agent 不等 Job。

Agent 订阅 Job。
```

---

# 7. 所以 Bitty 最重要的是明确四个对象

我建议最终 ontology 固定成这样：

| 对象                  | 含义                           |
| ------------------- | ---------------------------- |
| `Task`              | 语义上的工作，例如“修复 issue #91”      |
| `Execution` / `Job` | 一个真实 OS process/process tree |
| `Agent`             | 做决策和工作的逻辑主体                  |
| `Panel`             | 人类观察/交互的 UI projection       |

例如：

```text
Task #128 "Fix parser regression"
│
├── Agent A
│    ├── Job 41: cargo test parser
│    └── Job 45: cargo clippy
│
└── Agent B (reviewer)
     └── Job 52: cargo test --all

Panel 7 ──────── observes Job 41
Panel 12 ─────── observes Job 52
```

Panel 消失：

```text
Job 41 continues
```

Agent 切换 Panel：

```text
Job 41 continues
```

Agent conversation compact：

```text
Job 41 continues
```

甚至 Agent 暂时不存在：

```text
Job 41 may continue
```

这其实已经与你当前 `agent-coordination.md` 定义的 identity separation 完全吻合。

---

# 8. Job 不应该“属于 Panel”

这是你提到的第一个关键问题：

> Agent 从之前 Panel 到另一个 Panel 后，任务完成应该输出给谁？

答案是：

> **发给 Agent mailbox，而不是 Panel。**

创建时可以记录：

```text
job_id
task_id
created_by_agent
owner_agent
origin_panel
execution_context
```

但：

```text
origin_panel != owner
```

`origin_panel` 只是：

```text
"我是从这里启动的"
```

可以用于 UI：

```text
Job #41
Origin: Panel 8
Current observers:
  Panel 8
  Panel 17
```

真正的 notification route 是：

```text
Job
  ↓
Agent mailbox
  ↓
当前 Agent Session
```

因此 Agent 切换到另外一个 Panel 后：

```text
job.completed(job_41)
```

还是交给这个 Agent。

而不是找原来的 Panel。

---

# 9. Multi-Agent 下，需要 Owner + Subscriber，而不是“这个任务是谁的”

一个 Job 可以是：

```text
Owner:
    agent-a

Subscribers:
    agent-a
    team-lead
    agent-reviewer
    human-ui
```

于是：

```text
Agent A ───────┐
Lead ──────────┼── subscribe ── Job 41
Panel UI ──────┘
```

但是控制权限和观察权限分开：

```text
observe
read_output
send_input
signal
cancel
adopt
delete
```

不能因为 Agent B 能看到 Job 41：

```text
=> Agent B 就能 kill Job 41
```

你现在的 coordination 文档其实已经定义过类似 lease 原则：多个 read lease 可以共存，但 interactive writer 只能有一个，并且 transfer 要通过 generation fencing。

所以 Job 完全可以沿用这一套。

---

# 10. Agent 被用户杀掉，Job 怎么办？

这里不要只有一个规则。

应该在 spawn 时明确：

```rust
enum Lifetime {
    Agent,
    Task,
    Workspace,
    Detached,
}
```

于是行为自然变成：

| Lifetime    | Agent 死亡               |
| ----------- | ---------------------- |
| `Agent`     | cancel                 |
| `Task`      | Task 还活着就继续            |
| `Workspace` | workspace 活着就继续        |
| `Detached`  | Supervisor/daemon 独立继续 |

例如：

```text
cargo test
```

通常：

```text
lifetime = Task
```

开发服务器：

```text
npm run dev

lifetime = Workspace
```

训练模型：

```text
python train.py

lifetime = Detached
```

一次临时 shell：

```text
grep ...

lifetime = Agent
```

这比简单的：

```text
notifyOnExit=true
```

强很多。

---

# 11. Agent “准备结束”时，不应该靠 Prompt 自觉检查

你这里提到：

> Agent 自己在宣布结束之前是不是应该看有没有没结束的任务？

我的建议是：

> **要检查，但不能依赖 Agent 自己记得检查。**

应该做成 Harness invariant。

类似：

```text
Agent wants to finish
        │
        ▼
   Quiescence Gate
        │
        ├── no live owned jobs
        │      └── allow finish
        │
        └── live jobs exist
               ↓
        wait / detach / handoff / cancel
```

比如：

```text
2 owned executions still active:

job_41 cargo test
  state: running
  lifetime: task

job_42 npm run dev
  state: running
  lifetime: workspace
```

Agent 必须明确：

```text
job_41 -> wait
job_42 -> detach
```

之后才能正常结束。

如果用户强制 Stop Agent，则 runtime 根据 `Lifetime` 自动执行 policy。

这比写进 System Prompt：

> "Before finishing, remember to check background tasks"

可靠得多。

---

# 12. 长达几个小时甚至几天的任务，不能使用普通 timeout 模型

你截图里：

```text
timeoutSeconds=3600
```

对于 CI watch 很合理。

但：

```text
模型训练
大文件下载
编译 LLVM
同步数据集
dev server
数据库
```

不适合。

应该把：

```text
hard_timeout
idle_timeout
retention_ttl
```

完全分开。

例如训练：

```text
hard_timeout = none
idle_timeout = none
retention_ttl = 7d
```

测试：

```text
hard_timeout = 30m
```

下载：

```text
hard_timeout = 12h
idle_timeout = 10m
```

Server：

```text
hard_timeout = none
kind = service
```

尤其不要：

> “后台任务默认 1h，1h 后 kill”

这种规则迟早会把合法任务杀掉。

---

# 13. Job 最好再有一个 `kind`

除了 lifetime，我会再加：

```rust
enum JobKind {
    Command,
    Interactive,
    Service,
    Watch,
}
```

于是：

```text
cargo test
Command

python train.py
Command

python
Interactive

npm run dev
Service

gh pr checks --watch
Watch
```

`Watch` 和 `Service` 的预期就是长期不退出。

所以 Runtime 不会错误地认为：

```text
“为什么它还没结束？是不是卡住了？”
```

---

# 14. 非 interactive Job 默认关闭 stdin

这一点非常重要。

Claude Code 当前 background Bash 就出现过 background stdin 导致命令永久等待的问题；也出现过 session 结束后遗留 orphan process 的社区报告。([GitHub][6])

Bitty 可以直接规避：

```text
tty = false
stdin = closed
```

默认。

只有：

```text
interactive = true
```

时才：

```text
PTY + writable stdin
```

也就是：

```text
cargo test
    stdout pipe
    stderr pipe
    stdin closed

python REPL
    PTY
```

这是非常重要的安全默认值。

---

# 15. Output 不应该不断塞进 Agent Context

这里也可以比现有 Harness 做得更好。

Execution Supervisor 保存：

```text
ring buffer
+
optional persisted log
```

Agent 得到：

```text
job.completed {
    id: job_41,
    exit_code: 1,
    output_lines: 3124,
    tail: "...",
    evidence_ref: ...
}
```

而不是 3124 行全部塞进 context。

然后 Agent 再决定：

```text
job_read(job_41, tail=100)

job_read(
    job_41,
    filter="error|failed|panic"
)
```

也就是你截图里的 OpenCode 思路，但把它变成正式 architecture。

---

# 16. Completion Event 应该是 Critical Message

你现在自己的 R5 其实已经把这里最难的一部分写出来了：

```text
accepted into mailbox
delivered to live recipient
processed + acknowledged
```

三者必须区分，而且 task assignment、approval、cancel、result acknowledgement 不能 silently drop。

这特别适合 Job：

```text
job.exited
    event_id = evt_981
    job_id = job_41
    generation = 7
```

如果 Agent 暂时掉线：

```text
accepted
but not delivered
```

重新连接：

```text
deliver evt_981
```

Agent 处理：

```text
ack evt_981
```

如果重复收到：

```text
event_id 相同
=> dedupe
```

不要追求：

```text
exactly once delivery
```

更应该是：

```text
at-least-once delivery
+
idempotent event handling
+
stable event ID
```

至于 effect 本身，你现在 R5/R6 已经很正确地规定：

> crash 在 effect 和 acknowledgement 之间，结果必须是 `Unknown`，不能假装 exactly-once。

---

# 17. 断网其实应该分三类

这个区别很重要。

### LLM Provider 网络断开

```text
Agent API connection dies
```

Job 不应该受到影响：

```text
python train.py
       ↓
Execution Supervisor
       ↓
continues
```

LLM 恢复以后：

```text
mailbox:
  job_41 completed 14 minutes ago
```

这正是独立 Execution Supervisor 的意义。

### Job 自己的网络断开

例如：

```bash
wget ...
git clone ...
gh ...
```

Supervisor 不应该自己武断判断：

```text
network_error
```

除非 OS/tool 明确提供信息。

通常应该记录：

```text
exit code
signal
stderr
timeout
```

然后由 Agent 分析。

### Bitty IPC 断开

如果 Executor 和 AI Runtime 之间的 IPC 断了：

```text
Execution continues
Event retained
```

重新连接后按：

```text
last_event_seq
```

恢复。

Cursor 的 SSE `Last-Event-ID` 就是很值得参考的模式。([Cursor][4])

---

# 18. OOM、Crash、异常结束，需要结构化 Outcome

不要只返回：

```text
Exit Code: 137
```

建议最终形成类似：

```rust
enum JobOutcome {
    Success,
    ExitCode(i32),
    Signaled(Signal),
    SpawnFailed,
    Cancelled,
    TimedOut,
    OomKilled,
    SupervisorLost,
    Unknown,
}
```

但有一个原则：

```text
只有确定时才写 OomKilled。
```

Linux 如果以后采用 per-job cgroup，可以通过 `memory.events` 判断 OOM；你的 coordination 文档里其实已经把 cgroup resource accounting 作为 deferred backend candidate，而且明确要求不能把它伪装成跨平台保证。

跨平台可以分别：

```text
Linux
  process group
  pidfd
  optional cgroup v2

macOS
  process group
  kqueue/process wait

Windows
  Job Object
  ConPTY
```

最重要的是：

> kill 一个 Job 要 kill **owned process tree**，不能只 kill 一个 PID。

---

# 19. Retry 千万不要默认自动做

比如：

```bash
cargo test
```

重跑一般没问题。

但是：

```bash
stripe charge ...
kubectl delete ...
terraform apply
git push
database migration
```

盲目 retry 可能造成二次 effect。

所以：

```text
retry = none
```

应该是默认。

只有工具明确声明：

```text
idempotent = true
```

或者 user/agent 明确批准：

```text
retry {
    max_attempts = 3
    backoff = exponential
}
```

才自动 retry。

`Unknown` outcome 更是必须：

```text
inspect first
then retry
```

这一点你现有 R1/R5/R6 都已经定得很好，不应该为了 background runner 把它破坏掉。

---

# 20. 我建议 Bitty 最终的工具不是 `pty_spawn`

可以设计成这一组：

```text
exec
job_spawn
job_get
job_list
job_read
job_write
job_signal
job_cancel
job_subscribe
job_attach
job_adopt
```

其中：

```text
exec
```

负责短任务：

```text
Agent
  ↓
exec
  ↓
Execution Supervisor
  ↓
完成很快
  ↓
ExecResult
```

如果超过同步窗口：

```text
exec
  ↓
still running
  ↓
JobHandle
  +
automatic subscription
```

**不是让模型主动 poll。**

而：

```text
job_spawn
```

一开始就表示：

```text
这是异步 execution。
```

例如：

```text
job_spawn {
    argv: ["cargo", "test"],
    cwd: ...,
    tty: false,
    lifetime: "task",
    kind: "command"
}
```

---

# 21. `bash -c` 也不应该成为默认 execution model

尽量：

```text
argv = [
  "cargo",
  "test",
  "--workspace"
]
```

而不是：

```text
bash -c "cargo test --workspace"
```

只有真正需要：

```text
pipe
redirection
&&
shell expansion
```

时：

```text
shell = true
```

这样 Bitty 更容易做：

```text
permission checking
command attribution
resource accounting
process tree
argument logging/redaction
```

---

# 22. Multi-Agent 可见性我建议这样做

Control Console 最后可以变成：

```text
TASKS

T-128 Fix parser regression
  Agent A
    J-041 cargo test      RUNNING  2m31s
    J-045 cargo clippy    SUCCESS  8.1s

  Reviewer B
    J-052 cargo test      QUEUED

T-130 Benchmark renderer
  Agent C
    J-055 criterion       RUNNING  18m
```

点击 J-041：

```text
Owner
Subscribers
Execution target
Working directory
Environment profile
Process tree
CPU
Memory
Duration
Output
Evidence
Panels observing
```

Agent 和人都可以：

```text
observe
attach
tail
signal
cancel
```

但权限不同。

这正好也能成为你之前设想的 **Agent/Panel Control Console** 的底层基础。

---

# 23. 对于 Owner Agent 消失，可以采用 Handoff + Generation

比如：

```text
Job J41

owner:
  agent-A

generation:
  7
```

Agent A 崩了。

Team Lead 决定交给 B：

```text
handoff J41 A -> B
```

变成：

```text
owner:
  agent-B

generation:
  8
```

之后来自：

```text
Agent A / generation 7
```

的：

```text
kill
write stdin
change policy
```

全部拒绝。

这就是 fencing。

你现在的 R5 已经提出：

> outgoing generation 要先 fenced，再允许 incoming owner，避免旧 owner 和新 owner 同时操作。

所以这个可以直接复用。

---

# 24. 一个很关键的改进：不要让 Progress Event 唤醒 LLM

例如训练：

```text
Epoch 1/100
Epoch 2/100
Epoch 3/100
...
```

如果每一条都：

```text
wake Agent
```

会非常浪费 token。

事件应该分两类：

```text
Observation Event
Critical Event
```

Observation：

```text
stdout
progress
CPU usage
download percentage
heartbeat
```

可以：

```text
coalesce
drop-oldest
UI-only
```

Critical：

```text
completed
failed
needs_input
permission_required
timeout
ownership_changed
cancelled
```

必须可靠送达。

这和你 R5 已经规定的：

> progress observations 可以 coalesce/drop，但 critical messages 不能 silently disappear

完全一致。

---

# 25. “任务需要用户输入”也应该成为一等事件

以后会遇到：

```text
Are you sure? [y/N]
Password:
Select option:
Debugger breakpoint
```

可以：

```text
JobState = WaitingInput
```

然后：

```text
job.needs_input {
    job_id,
    prompt_hint,
}
```

但涉及 secret 时：

```text
Agent 不能读取用户输入
```

这应该继续服从你当前 IPC 的 sensitive-input boundary。

对于大部分 background task，因为默认 stdin closed：

```text
意外 prompt
=> fail / WaitingInput
```

而不是永久挂在那里。

---

# 26. Persistence 方面，我不会把所有 output 都塞 SQLite

这也和你之前担心 OpenCode `opencode.db` 变 25GB 是同一个问题。

比较好的形式是：

```text
SQLite
  jobs
  job_events
  subscriptions
  ownership
  metadata
  output index

Filesystem
  logs/
    J41.log
    J42.log

Artifacts
  hashes / larger outputs
```

内存再有：

```text
bounded ring buffer
```

这样 SQLite 不会因为 stdout 变成几十 GB。

你的 R6 当前也已经倾向：

```text
transactional journal
+
file-held artifact bytes
```

而不是所有内容都灌进数据库。

---

# 27. 不过这里和当前 v0.1 有一个明确边界

你现在 R6 明确写的是：

```text
v0.1 = ephemeral
no durable journal
no persistent evidence store
```

durability 放在 post-v0.1。

所以我不会现在直接上：

```text
bitty-execd daemon
SQLite persistence
crash adoption
detached week-long jobs
```

更合理的是分三阶段。

```text
Phase 1
Execution Supervisor
in-memory Jobs
event-driven completion
Panel-independent
Agent mailbox
process-tree cleanup
no app-restart survival

        ↓

Phase 2
persistent metadata/events/logs
restart reconciliation
Unknown handling
resume cursor

        ↓

Phase 3
detached supervisor daemon
Bitty GUI can exit
multi-day Job survives
handoff/adoption
resource scheduling
```

这会和你现在的架构演进非常顺。

---

# 28. 所以 Pueue 应该“借设计”，而不是“直接把 Pueue 当 backend”

我会明确这么定。

Pueue 值得直接借：

```text
daemon/supervisor
Job state
queue
group concurrency
dependency
pause/resume
restart
logs
env snapshot
callback
```

Cursor 值得借：

```text
event stream
resume cursor
subscription
wake-on-event
Agent/Run separation
```

OpenCode PTY 值得借：

```text
read/write/list/kill
ring buffer
exit notification
interactive PTY
```

Claude 值得借：

```text
background task
Monitor
agent supervisor
/tasks UI
```

Codex 值得借：

```text
process RPC protocol
process/output
process/exited
signal/write
```

最后组合成 Bitty 自己的：

```text
                  bitty-ai
        ┌────────────────────────┐
        │ Agent / Team / Task    │
        │ Mailbox / Subscription │
        └───────────┬────────────┘
                    │ scoped IPC
                    ▼
        ┌────────────────────────┐
        │ Execution Service      │
        │                        │
        │ Job Registry           │
        │ Scheduler              │
        │ Event Bus              │
        │ Process Supervisor     │
        │ Output Store           │
        └──────┬─────────┬───────┘
               │         │
         Pipe Process    PTY
               │         │
               └────┬────┘
                    │
                 OS Process

Panel ───────── projection / attach ─────────► Job
```

这其实就是你现有 `Command and Tool Architecture` 中已经提出的：

```text
agent.exec
  -> ExecutionContext
  -> supervised structured process / optional PTY
  -> evidence
  -> structured result
```

的完整下一层。

---

# 我会把它单独升成一个 RFC

我甚至不建议继续把这些内容散在 `agent-coordination.md`、R1、R5、R6 里面。

可以新增类似：

```text
specifications/execution-supervisor.md
```

或者：

```text
background-execution-r7.md
```

专门定义：

```text
Task vs Job
ExecutionContext
Job state/outcome
Lifetime policy
PTY vs pipe
Supervisor ownership
process tree
Agent mailbox
subscriptions
critical events
output retention
Panel projection
handoff/adoption
generation fencing
retry policy
long-running execution
crash/reconnect
Agent finalization gate
```

因为我觉得这个已经不是 `exec tool` 的一个小功能了。

**它实际上会成为 Bitty 的 Agent Runtime 和 Terminal Runtime 之间最重要的一条基础设施。**

而且一旦这一层做对，后面的：

```text
Multi Agents
Agent Team
CI watcher
background tests
dev server
model training
downloads
LSP/server lifecycle
debugger
Agent dashboard
headless Panel
remote execution
```

都会自然建立在同一套 Job / Event / Supervisor primitive 上，而不用每个功能再单独发明一套后台进程管理机制。

[1]: https://github.com/openai/codex/issues/33816?utm_source=chatgpt.com "GPT-5.6 Terra abandons yielded exec_command sessions after ~10s and attempts duplicate commands · Issue #33816 · openai/codex · GitHub"
[2]: https://code.claude.com/docs/en/agent-sdk/typescript?utm_source=chatgpt.com "Agent SDK reference - TypeScript - Claude Code Docs"
[3]: https://code.claude.com/docs/en/agents?utm_source=chatgpt.com "Run agents in parallel - Claude Code Docs"
[4]: https://prod.cursor.com/docs/cloud-agent/api/endpoints?utm_source=chatgpt.com "Cloud Agents API | Cursor Docs"
[5]: https://cursor.com/docs/cloud-agent/capabilities?utm_source=chatgpt.com "Capabilities | Cursor Docs"
[6]: https://github.com/anthropics/claude-code/issues/79175?utm_source=chatgpt.com "Background Bash tasks hang forever on any stdin read (foreground does not) · Issue #79175 · anthropics/claude-code · GitHub"

那么这些
1. job ↔ task 绑定：谁为哪个任务跑、跑在哪棵 worktree、退出后谁记 progress note——现在全靠 commander 手工编排。
2. 结果契约：typed outcome（exit + 结构化摘要 + artifact 指针）vs 文本 blob 翻找。
3. 多 agent 安全：group/queue 的可见性与杀权限（capability），stale claim 超时与认领冲突。
4. ** noms？**——没有，第四点是：取消与超时语义：谁能取消、取消后是 kill 还是 graceful、超时算失败还是 отдельный outcome。

然后就是哪些部分是 bitty 实现的，还是说都是 bitty-ai 仓库中实现呢

## 不是“全放 bitty-ai”，而是要按 **机制 / 语义与策略** 切开

你列出的四点正好说明为什么 `Execution` 不能完全归 `bitty-ai`。

我会固定成：

```text
bitty
  = Execution Host / 机制层
  = 真正拥有 OS process / PTY / signal / timeout / capability enforcement

bitty-ai
  = Agent Runtime / 语义与协调层
  = Task / Agent / Job association / subscription / handoff / progress / policy

bitty ↔ bitty-ai
  = typed Execution Protocol
```

这跟现在 R1 的方向是一致的：`ExecutionContext` 是实际执行记录，Panel 只是 projection，而且 terminal side 应提供 generic execution primitives；Agent 不应该通过占据 Panel 获得执行权。

因此你这四项最好逐项拆。

---

## 1. Job ↔ Task 绑定：**主要属于 bitty-ai，但 Execution Target 属于 bitty**

这一项其实包含三件不同的事：

```text
谁为哪个任务跑
跑在哪棵 worktree
跑完谁记 progress
```

应该拆成：

```text
bitty-ai
Task T128
  │
  ├─ owner = Agent A
  ├─ workspace = Worktree W7
  └─ JobBinding
       │
       └─ Execution E41
                ↓
bitty
实际启动 process
```

### bitty-ai 负责

```rust
JobBinding {
    task_id: TaskId,
    execution_id: ExecutionId,
    owner: AgentId,
    assignment_generation: Generation,
    workspace_ref: WorkspaceRef,
}
```

也就是：

* `Task -> Agent`
* `Task -> Execution`
* `Agent -> Execution`
* Task dependency
* assignment / claim / handoff
* execution 完成后给哪个 Task 记 progress
* progress note
* Task 是否因此变成 completed / blocked / ready

这些属于 **Agent orchestration / Task semantics**，Terminal 根本没必要知道：

> “这个 `cargo test` 是为了修 Issue #128。”

当前 R5 本身也已经把 **product Task model 作为 Task lifecycle 唯一 authority**，并强调 execution binding、assignment generation、handoff 都是 Task 层概念。

### bitty 负责

但是：

```text
/run/media/.../worktrees/foo
```

到底是不是一个合法的 execution target、当前 generation 是什么、cwd/env 是什么，应该由 bitty 的 host-side generic execution service 解析和验证。

例如 AI 不能直接说：

```json
{
  "cwd": "/whatever/i/want"
}
```

然后 Core 无脑执行。

更合理的是：

```text
bitty-ai:
    WorkspaceRef(W7)

          ↓ IPC

bitty:
    resolve W7
    authorize Agent A
    capture ExecutionTarget
    execute
```

所以：

> **Worktree 的“业务选择”归 bitty-ai；worktree/cwd 作为真实执行 target 的解析与 enforcement 归 bitty。**

---

# 2. Typed Result Contract：必须拆成两层

你说的：

> `exit + 结构化摘要 + artifact 指针`

这里很容易犯一个架构错误：

```text
把 AI 生成的 summary
和 OS execution result
做成同一个东西
```

不要这么做。

应该至少有：

```text
ExecutionResult       // bitty authoritative
        +
JobResult             // bitty-ai semantic projection
```

### `bitty` 产生 authoritative `ExecutionResult`

例如：

```rust
struct ExecutionResult {
    execution_id: ExecutionId,

    outcome: ExecutionOutcome,

    started_at: Timestamp,
    ended_at: Timestamp,

    stdout: EvidenceRef,
    stderr: EvidenceRef,

    artifacts: Vec<ArtifactRef>,

    resource_usage: Option<ResourceUsage>,

    truncated: bool,
}
```

以及：

```rust
enum ExecutionOutcome {
    Success,
    ExitCode(i32),
    Signaled(Signal),
    SpawnFailed(SpawnError),
    Cancelled(CancelReason),
    TimedOut(TimeoutKind),
    OomKilled,
    SupervisorLost,
    Unknown,
}
```

这些是 **事实**。

AI 不应该拥有：

```text
exit_code
process tree
signal
OOM 判断
timeout timer
```

因为真正知道这些的是 Process Supervisor。

---

### `bitty-ai` 再产生 `JobResult`

比如：

```rust
struct JobResult {
    execution: ExecutionResultRef,

    summary: StructuredSummary,

    diagnostics: Vec<Diagnostic>,

    task_effect: TaskEffect,

    progress_note: Option<ProgressNote>,
}
```

例如：

```text
ExecutionResult:
    ExitCode(101)
    stderr = artifact://abc...

bitty-ai summary:
    "cargo test failed:
     2 tests failed in parser::tests,
     likely related to escape sequence handling."
```

这两个可信度完全不同。

所以 UI 以后甚至可以显示：

```text
FAILED · exit 101

Agent summary
2 parser tests failed...

Evidence
stdout · stderr · diagnostics
```

而不是只有：

```text
"Tests failed."
```

---

## Artifact 也应该类似

实际 artifact：

```text
core dump
test report
coverage.json
benchmark.json
log file
generated binary
```

其存储、digest、retention reference 可以是 generic host/evidence mechanism。

而：

```text
“这个 artifact 对 Task #128 有什么意义”
```

属于 bitty-ai。

你现在 R6 也已经明确区分 journal / evidence / artifact bytes，并提出 artifact bytes 可以 file-held、通过 digest/reference 引用，而不是把所有东西当 conversation blob。

---

# 3. Multi-Agent 安全：这里必须 **bitty 最终 enforcement，bitty-ai 做 coordination**

这个尤其不能只放 bitty-ai。

假设：

```text
Agent A owns J41
Agent B can observe J41
Agent C shouldn't know J41 exists
```

bitty-ai 可以决定逻辑：

```text
A = owner
B = reviewer/subscriber
C = unrelated
```

但是不能只是 bitty-ai 自己检查：

```rust
if agent == owner {
    kill()
}
```

然后 IPC 到 bitty 以后：

```text
kill J41
```

完全不验证。

因为那等于：

> `bitty-ai` 一个 bug 就可以 kill 任意 process。

正确结构应该是：

```text
Agent B
  ↓
bitty-ai
  ↓
request {
    principal = AgentB,
    execution = J41,
    operation = Signal(SIGTERM),
    generation = 8
  }
  ↓
bitty authorization
  ↓
ALLOW / DENY
```

### bitty 负责 capability enforcement

比如：

```text
execution.observe
execution.read_output
execution.write_input
execution.signal
execution.cancel
execution.attach
execution.transfer
```

每个权限独立。

当前 `agent-coordination` 其实也已经提出：

> observe / assist / control 不能成为一个巨大的权限 bundle，每个 effect 都应该按真实 scope 授权。

这个原则应该直接延伸到 Job。

---

### bitty-ai 负责 claim / ownership semantics

例如：

```rust
JobClaim {
    owner: AgentId,
    assignment: AssignmentId,
    generation: u64,
    lease_until: Instant,
}
```

bitty-ai 管：

```text
Agent A claim
Agent B handoff
stale agent detection
team lead reassignment
Task owner
reviewer
subscriber
```

然后把已授权、已版本化的操作请求交给 Host。

---

## stale claim 要区分两种 stale

### Agent claim stale

```text
Agent A 心跳消失
```

属于：

```text
bitty-ai coordination
```

例如：

```text
lease expired
→ mark owner unavailable
→ commander / supervisor decides handoff
```

### Execution handle stale

例如：

```text
Execution J41 generation=5 已经结束
后来 ID 被重建成 generation=6
```

这属于：

```text
bitty host
```

bitty 必须拒绝：

```text
cancel(J41, generation=5)
```

不能靠 bitty-ai 自己保证。

所以应该有两套 generation：

```text
assignment_generation     // bitty-ai
execution_generation      // bitty
```

它们不是一回事。

这个区别非常重要。

---

# 4. Cancel / Timeout：**机制归 bitty，策略归 bitty-ai**

这一项最适合明确切层。

比如 Agent 想取消：

```text
Agent A
   ↓
“Cancel J41”
```

bitty-ai 负责：

```text
Agent A 是否应该发这个请求？
当前 Task policy 允许吗？
Team ownership 是否正确？
这个 Job 是否仍需要被别的 Agent 使用？
```

但是：

```text
SIGINT
wait 5 seconds
SIGTERM
wait 3 seconds
SIGKILL
kill process group
reap descendants
```

这些绝对是 bitty execution host 的工作。

---

## 我会把 cancel protocol 做成这样

```rust
struct CancelExecution {
    execution_id: ExecutionId,
    generation: ExecutionGeneration,

    mode: CancelMode,
    grace_period: Duration,
}
```

比如：

```rust
enum CancelMode {
    Graceful,
    Immediate,
    GracefulThenKill,
}
```

host 负责执行。

返回：

```rust
enum CancelOutcome {
    CancelledGracefully,
    Killed,
    AlreadyExited,
    PermissionDenied,
    StaleGeneration,
    Unknown,
}
```

这样 AI 永远不能自己推断：

```text
“我发了 Ctrl+C，所以它一定停了。”
```

---

# Timeout 也一样

### bitty 负责计时和执行 timeout

因为不能依赖 Agent 还活着：

```text
Agent API 网络断了
```

但：

```text
30 min hard timeout
```

还是必须生效。

所以 Deadline 必须被 Execution Supervisor 持有。

例如：

```rust
ExecutionLimits {
    hard_timeout: Option<Duration>,
    idle_timeout: Option<Duration>,
}
```

---

### bitty-ai 负责选择 policy

例如：

```text
cargo test
hard_timeout = 20 min

model training
hard_timeout = none

download
idle_timeout = 15 min

dev server
hard_timeout = none
```

这些是 workflow/task semantics。

---

# “Timeout 算 Failed 吗？”

我会明确说：

> **底层不能把 timeout 折叠成普通 `Failed`。**

应该：

```text
ExecutionOutcome::TimedOut(...)
```

这是事实。

至于 Task 怎么解释：

```text
TimedOut
    ↓
Task failed?
Task blocked?
retry?
ask user?
```

才由 bitty-ai policy 决定。

例如：

```text
CI command timeout
→ Task execution failed

training time budget reached
→ Task paused / incomplete

Watch task TTL reached
→ normal policy termination
```

所以：

```text
ExecutionOutcome != TaskOutcome
```

这是一个非常重要的分层。

---

# 最终我建议形成这张边界表

| 能力                      | `bitty`                   | `bitty-ai`        |
| ----------------------- | ------------------------- | ----------------- |
| Process spawn           | **拥有**                    | 请求                |
| PTY                     | **拥有**                    | 使用                |
| stdout/stderr capture   | **拥有**                    | 消费                |
| Process tree            | **拥有**                    | 查看授权后的 projection |
| cwd/env/worktree target | **解析、验证、捕获**              | 选择目标              |
| Execution ID/generation | **拥有**                    | 引用                |
| Task ID                 | 不理解语义                     | **拥有**            |
| Job ↔ Task              | 提供 opaque metadata/ref 即可 | **拥有**            |
| Job ↔ Agent owner       | capability principal      | **协调语义拥有**        |
| Claim / handoff         | enforcement primitive     | **拥有策略**          |
| subscribe               | event mechanism           | **订阅策略**          |
| read/kill 权限            | **最终 enforcement**        | 决定/请求 scope       |
| graceful cancel         | **执行**                    | 选择 policy         |
| timeout clock           | **执行**                    | 设置 policy         |
| process-tree kill       | **拥有**                    | 请求                |
| exit/signal/OOM         | **authoritative fact**    | 解释                |
| raw artifacts/evidence  | **产生/引用机制**               | 解释/关联 Task        |
| summary                 | 不生成 AI 语义                 | **拥有**            |
| progress note           | 不理解                       | **拥有**            |
| retry                   | 提供重新执行 primitive          | **决定是否 retry**    |
| stale execution         | **检测**                    | 响应                |
| stale agent claim       | 不负责 Agent semantics       | **检测/协调**         |
| Task completion         | 不负责                       | **拥有**            |

---

# Repo 层面我会这样放

你现在已经有：

```text
bitty-terminal/bitty
bitty-terminal/bitty-ai
```

我会让 `bitty` 中出现一个完全 **AI-agnostic** 的 execution subsystem：

```text
bitty/
  crates/
    bitty-execution/
      process.rs
      pty.rs
      supervisor.rs
      outcome.rs
      limits.rs
      signals.rs
      output.rs
      capability.rs
```

注意它里面不应该出现：

```rust
AgentId
TaskId
LLM
Prompt
ModelProvider
Commander
Reviewer
```

最多认识 generic：

```rust
PrincipalId
ExecutionId
TargetId
Capability
OpaqueMetadata
```

---

而 `bitty-ai`：

```text
bitty-ai/
  execution/
    job.rs
    binding.rs
    claim.rs
    subscription.rs
    progress.rs
    result.rs
    retry.rs
    handoff.rs
```

认识：

```text
TaskId
AgentId
TeamId
AssignmentId
ExecutionId
WorkspaceRef
```

然后：

```text
Job = AI-side semantic wrapper over Execution
```

我觉得这句话可以直接成为架构原则：

> **Every Job may reference an Execution, but an Execution is not inherently an AI Job.**

因为 Bitty 普通用户、Lua plugin、普通 terminal feature 也都会执行 process。

例如：

```text
用户在 Panel 里启动 cargo test
```

这是 `Execution`。

不一定是：

```text
bitty-ai Job
```

---

## IPC 则是两边共同的 contract

比如：

```text
execution.spawn
execution.get
execution.cancel
execution.signal
execution.read
execution.subscribe
execution.attach
```

这个协议应该定义 generic execution semantics，而不是：

```text
agent.job.start
agent.job.kill
```

因为 Terminal Core 不应该知道 Agent ontology。

你现在 `command-tool-architecture` 已经明确强调 AI runtime 和 Terminal Core 的边界：Terminal Core 提供 generic host capability，AI runtime 做 agent/tool orchestration；AI-specific semantics 不应泄漏进 terminal process。

---

## 一个完整例子

Agent A 接到：

```text
Task T128:
Fix parser bug
```

bitty-ai 做：

```text
Task T128
owner = A

choose WorkspaceRef W5

spawn Job J31
J31.task = T128
J31.owner = A
J31.workspace = W5
```

然后 IPC：

```text
execution.spawn {
    target: W5,
    argv: ["cargo", "test", "parser"],
    limits: {...},
    capabilities: ...
}
```

bitty：

```text
resolve W5
authorize A
capture cwd/env
spawn process
assign E77 generation 3
```

返回：

```text
E77
```

bitty-ai：

```text
J31.execution = E77
```

结束后 bitty 发：

```text
execution.exited {
    E77,
    generation: 3,
    outcome: ExitCode(101),
    stdout_ref,
    stderr_ref
}
```

bitty-ai 收到以后：

```text
E77
 ↓
J31
 ↓
T128
```

再生成：

```text
ProgressNote:
"Parser tests still fail in 2 cases..."

Task T128:
state = in_progress
```

这整个过程中：

```text
bitty 不知道 T128 是什么。
bitty 也不知道 Agent A 是“implementer”还是“reviewer”。
```

它只知道：

```text
Principal P7
拥有 Execution E77 的这些 capabilities。
```

我认为这是最干净、也最能支撑以后 Multi-Agent / Lua Plugin / 非 AI 用户共用同一个 Bitty execution infrastructure 的边界。

