# 016: 终端 Mod/Leader 键选型哲学与超越 Flash 的空间航标动作引擎 (Bitty Beacon)

> **归档位置**: `recording/research/016.md`  
> **研究主题**: 终端修饰键（Mod）与引导键（Leader）选型依据、全键盘单手流人机工程学、超越 `flash.nvim` 的空间航标与动作引擎（Bitty Beacon / Spatial Action Engine）、语义输出折叠、跨面板焦点瞬移与自定义脚本调度体系。  
> **对应规范**: `bitty-docs/docs/specifications/` (`input-pointer-rfc.md`, `panel-runtime-pre-study.md`, `rich-presentation-rfc.md`, `workspace-compositor.md`)。

---

## 目录

1. [概述与设计动机：从“四键齐拧”到“模态单手流”](#一概述与设计动机从四键齐拧到模态单手流)
2. [修饰键（Mod）选型哲学：为什么 Alt 是终端的最优解](#二修饰键mod选型哲学为什么-alt-是终端的最优解)
3. [引导键（Leader）选型与跨平台适配策略](#三引导键leader选型与跨平台适配策略)
4. [核心架构跃迁：超越 flash.nvim 的 Bitty Beacon 空间引擎](#四核心架构跃迁超越-flashnvim-的-bitty-beacon-空间引擎)
5. [四大核心动作体系（Action Matrix）](#五四大核心动作体系action-matrix)
6. [单手人机工程学（One-Handed Workflow）建模](#六单手人机工程学one-handed-workflow建模)
7. [技术实现管线：Rust Core 机制与 Lua 策略定义](#七技术实现管线rust-core-机制与-lua-策略定义)
8. [总结与演进蓝图](#八总结与演进蓝图)

---

## 一、概述与设计动机：从“四键齐拧”到“模态单手流”

在传统终端模拟器与桌面环境中，快捷键体系正陷入严重的**指关节健康危机（Emacs Pinky & Chord Fatigue）**：

- 随着终端功能的增加（分屏、切标签、搜索、滚动、呼出 AI），开发者被迫使用 `Ctrl + Alt + Shift + F` 这种四键齐拧的反人类组合；
- **核心冲突**：终端无法分清用户按下的 `Ctrl + C` 究竟是要“向 Shell 发送中断信号 `SIGINT`”，还是要“复制文本”；
- **设计突破**：Bitty 借鉴 Vim/Neovim 最成功的 **Leader 键（模态解耦）哲学**，引入两段式单手流：
  $$\text{按下 Leader (激活航标模态)} \longrightarrow \text{敲击单个按键 (精准执行动作)}$$
  实现完全不需要扭曲手指、单手即可在 0.2 秒内掌控整个终端工作区的极致流畅体验。

---

## 二、修饰键（Mod）选型哲学：为什么 Alt 是终端的最优解

在现代个人电脑键盘上，可选的修饰键只有四个：`Ctrl`、`Super/Win/Cmd`、`Shift`、`Alt/Option`。Bitty 经过深度权衡，决定将 **`Alt`（Meta 键）确立为核心默认 Mod 键**。

### 1. 各修饰键在终端环境下的生态冲突分析

| 修饰键            | 为什么不能作为终端主 Mod？（致命缺陷）                                                                                                                                                                                                                                                   |
| :---------------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`Ctrl`**        | **与 Shell/PTY 行编辑深度绑定，冲突极其严重**。Bash/Zsh/Fish 默认采用 Emacs 键位映射：`Ctrl+A`（跳到行首）、`Ctrl+E`（行尾）、`Ctrl+K`（剪切到行尾）、`Ctrl+R`（历史搜索）、`Ctrl+C/Z`（进程信号）、`Ctrl+L`（清屏）。终端模拟器若占用 Ctrl，将彻底摧毁所有 CLI 程序的原生操控肌肉记忆。 |
| **`Super / Win`** | **已被现代操作系统与平铺窗口管理器（WM）彻底霸占**。在 Linux 桌面（Hyprland、Niri、Sway、i3、GNOME、KDE）以及 Windows/macOS 上，`Super` 键是桌面级的保留键（用于全局切工作区、呼出启动器、分屏窗口）。终端作为客户端软件，无法绕过操作系统的全局抢占，更无法保证跨平台一致性。           |
| **`Shift`**       | **无法独立作为字母组合键**。`Shift + a` 直接被解释为大写字母 `A`，无法产生独特的修饰信号，只能作为辅助从属键。                                                                                                                                                                           |
| **`Alt` (Meta)**  | **【最优解】生态冲突极少，符合现代浏览器直觉**。在主流浏览器（Chrome、Firefox）中，`Alt + 1..9` 是切换标签页的全球通用标准；在 Linux/Windows 键盘上，左手大拇指按压 `Alt` 是最符合人体工学的自然姿势，极少与标准行编辑快捷键冲突。                                                       |

---

## 三、引导键（Leader）选型与跨平台适配策略

### 1. 引导键与 Mod 键的分工

- **Mod 键（如 `Alt + 1..9`）**：用于高频、直觉、无模态的单步操作（如极速切换前 9 个标签页/工作区）；
- **Leader 键（如 `Alt + Space`）**：用于唤起 **Bitty Beacon 空间航标体系**，进入短暂的动作执行模态。

### 2. 跨平台候选策略与自适应配置

- **Linux / X11 / Wayland**：
  默认采用 `Alt + Space`。在纯键盘窗口管理器（如 Hyprland / Niri）中，大拇指击打空格极其自然。
- **Windows 原生平台兼容**：
  在 Windows 下，`Alt + Space` 历史上被系统用于呼出窗口控制菜单（或被 PowerToys Run 占用）。因此在 Windows 环境下提供自适应备选方案：
  - 备选方案 A：`Alt + \``（反引号，左手无名指微动即可触发）；
  - 备选方案 B：`Double Alt`（双击 Alt 激活，类似 IntelliJ 搜索）；
  - 备选方案 C：`Alt + q`（极速左手组合）。
- **用户绝对裁决权**：
  在 `~/.config/bitty/init.lua` 中暴露单行声明：

  ```lua
  bitty.config({
      leader = "Alt-Space", -- 支持自由绑定为 "Space", "Alt-q", "Super-Space" 等
      timeout_ms = 1000, -- 模态等待超时，超时自动安全退出，不卡死输入
  })
  ```

---

## 四、核心架构跃迁：超越 flash.nvim 的 Bitty Beacon 空间引擎

用户在 Neovim 中喜爱的 `flash.nvim`，本质上只是一个在文本缓冲区（Buffer）内进行二维光标跳跃的辅助工具。
而 Bitty 将其**升维为全终端工作区的“空间航标与动作引擎”（Bitty Beacon & Spatial Action Engine）**：

```text
 ┌─────────────────────────────────────────────────────────────────────────────┐
 │                         Bitty Beacon 空间架构升维                           │
 ├───────────────────┬──────────────────────────┬──────────────────────────────┤
 │ 维度              │ Neovim 的 flash.nvim     │ Bitty Beacon 空间航标引擎    │
 ├───────────────────┼──────────────────────────┼──────────────────────────────┤
 │ **操作对象**      │ 单个文本编辑器的可见字符 │ 整个多面板平铺工作区与系统   │
 ├───────────────────┼──────────────────────────┼──────────────────────────────┤
 │ **目标实体**      │ 行号、单词、文本匹配点   │ Panel、折叠块、输入框、URL   │
 ├───────────────────┼──────────────────────────┼──────────────────────────────┤
 │ **执行能力**      │ 只能移动光标 (Motion)    │ 跳转 + 折叠 + 调度脚本 + 聚焦│
 ├───────────────────┼──────────────────────────┼──────────────────────────────┤
 │ **渲染层级**      │ 依赖 Neovim 字符网格重绘 │ 原生 WGPU 场景树独立图层浮现 │
 └───────────────────┴──────────────────────────┴──────────────────────────────┘
```

按下 Leader 瞬间，Bitty 核心在当前所有视口元素上方渲染半透明的高对比度字母徽章（Badges：`[a]`, `[s]`, `[d]`），用户敲击字母**不仅能瞬移焦点，更能原地触发结构化动作**。

---

## 五、四大核心动作体系（Action Matrix）

```text
 ┌─────────────────────────────────── Bitty Workspace ───────────────────────────────────┐
 │                                                                                        │
 │  ┌─── Panel 1 (Terminal) ────┐  ┌─── Panel 2 (AI Assistant) ──┐  ┌─── Panel 3 (Log) ───┐  │
 │  │ [A] 聚焦此面板            │  │ [C] 聚焦此面板              │  │ [E] 聚焦此面板      │  │
 │  │                           │  │                             │  │                     │  │
 │  │ $ cargo build             │  │ 🤖 请问有什么可以帮您？     │  │ $ tail -f app.log   │  │
 │  │ [S] ▶ 展开构建日志 (42行) │  │                             │  │                     │  │
 │  │                           │  │ ┌─────────────────────────┐ │  │ [D] 挂起此服务      │  │
 │  │ $ cargo test              │  │ │ [I] 瞬移进入提问输入框   │ │  │                     │  │
 │  │ [W] ▼ 折叠测试日志 (128行)│  │ └─────────────────────────┘ │  │                     │  │
 │  └───────────────────────────┘  └─────────────────────────────┘  └─────────────────────┘  │
 └────────────────────────────────────────────────────────────────────────────────────────┘
```

### 1. 动作 A：空间焦点切换（Spatial Panel & Workspace Focus）

- **触发**：`<Leader> a`
- **行为**：工作区内所有分屏面板的标题栏同时亮起直观徽章 `[A]`, `[B]`, `[C]`；
- **效果**：单手敲下 `b`，键盘焦点（Focus）瞬间切换至 Panel 2，彻底摆脱 `Ctrl+W h/j/k/l` 需要多次敲击的笨拙移动。

### 2. 动作 B：命令日志输出的极速折叠与展开（Semantic Output Folding）

- **触发**：`<Leader> s`
- **行为**：扫描当前屏幕上所有基于 OSC 133 语义标记的命令块，在其折叠图标旁打上标签 `[s]`, `[w]`, `[e]`；
- **效果**：
  - 敲击对应的键位，该命令块的输出在 0 毫秒内完成折叠/展开切换；
  - **巨大收益**：单手即可随时折叠占据屏幕的成千上万行构建噪声，或者在需要排错时秒级展开查看，无需来回滚动鼠标滚轮。

### 3. 动作 C：跨面板元素瞬移与交互框接入（Focus Routing）

- **触发**：`<Leader> i`
- **行为**：识别所有的可交互控件（例如 AI 侧边栏的提问输入框、浮动搜索栏、表单组件）；
- **效果**：无论当前光标身处何方，敲击对应键位，光标立即跳入 AI 输入框并激活 IME 输入法，立刻开始输入提示词，体验浑然天成。

### 4. 动作 D：自定义外部脚本与环境调度（Script & Workflow Dispatch）

- **触发**：`<Leader> d`、`<Leader> x` 或用户自定义键位
- **设计上限**：不仅限于内置动作，支持直接派发执行系统脚本或工作流。
- **Lua 声明机制**：

  ```lua
  -- ~/.config/bitty/init.lua
  local beacon = require("bitty.beacon")

  -- 注册单手快速启动本地 Docker 环境
  beacon.register_action("d", {
      description = "Spin up local Docker stack",
      run = function()
          -- 优先从 PATH 检索，也支持直接绝对路径
          bitty.process.spawn({
              program = "docker-compose",
              args = { "up", "-d" },
              cwd = bitty.workspace:root(),
          })
          bitty.notify("Docker stack started in background!")
      end
  })

  -- 注册单手一键运行 Git 快速审查
  beacon.register_action("g", {
      description = "Toggle Git Diff Overlay",
      run = function()
          bitty.views:toggle_overlay("bitty.git-diff-panel")
      end
  })
  ```

---

## 六、单手人机工程学（One-Handed Workflow）建模

为了让程序员在喝咖啡、查资料、或者单手操作时仍能保持高效生产力，Bitty Beacon 的键位布局经过了严格的物理按键距离建模：

```text
 ┌───── 左手高频极速区 (Home Row Left) ─────┐
 │                                          │
 │      [Q] 关闭面板    [W] 折叠/展开输出   │
 │      [A] 空间切屏    [S] 航标动作调度    │
 │      [Z] 全屏放大    [D] 运行自定义脚本  │
 │                                          │
 └──────────────────┬───────────────────────┘
                    │
            [Alt / Space] 拇指自然落点
```

1. **零大跨度移动（键盘区域聚类 / Handedness Clustering）**：
   - **痛点痛击**：`flash.nvim` 的标签通常按字母频率全键盘随机分布（常跳出 `o, p, j, k, l, n, m` 等右侧键），迫使用户必须双手归位；
   - **Bitty 创新**：引入**按键分区池（Key Regional Pool）**。支持在配置中指定手偏好：

     ```lua
     bitty.config({
         beacon = {
             handedness = "left", -- "left" (默认) | "right" | "both"
             -- 左手极速池优先序列：
             keys_left = "asdfqwerzxcv",
             -- 右手备选池优先序列：
             keys_right = "jkl;uiopm,.",
         }
     })
     ```

   - 默认模式下，生成的徽章标签**100% 锁定在键盘左半区**（`A-S-D-F-Q-W-E-R-Z-X-C-V` 共 12 个键）。右手完全可以继续握着鼠标、触控板或端着咖啡，彻底实现单手纯盲打流。

2. **单键极高命中率 vs. 按需自适应双键（Single-Key Dominance & 2-Char Overflow）**：
   - **目标密度差异**：在 Neovim 的文本 Buffer 中，单屏可能有数千字符，一个搜索词会命中 50~200 处，因此 `flash.nvim` 必须高频依赖两字符（如 `aa`, `ab`, `ac`）；
   - **Bitty 工作区特征**：当前视口内的交互实体数量通常极其收敛（2~~6 个 Panel、3~~12 个折叠命令胶囊、1~2 个输入框，总数通常在 15 个以内）；
   - **自适应降级机制**：
     - **90% 场景（目标数 $\le 12$）**：**单键瞬达（1-Keystroke Hit）**。大拇指按 `Alt+Space`，食指/中指敲击单个字母立刻触发动作，行云流水；
     - **极密场景（目标数 $> 12$，如 4K 屏开启了 20+ 个折叠块）**：前缀树自动升维为两字符标签（如 `aa`, `as`, `ad`），且**双字符依然全部保持在同侧手区域**，绝不产生跨手击打。

3. **两段式心智模型**：
   - 第一段（`Alt+Space`）：大拇指自然落点触发唤醒；
   - 第二段（单敲 `S` 或同侧连续两击）：食指/中指敲击功能键；
   - **彻底消灭了需要小指极力扭曲按压造成的键位损伤风险（Emacs Pinky）**。

---

## 七、技术实现管线：Rust Core 机制与 Lua 策略定义

遵循 Bitty 一贯的“机制归 Core，策略归 Lua”原则，Bitty Beacon 的底层渲染与输入拦截完全内置在 Rust 核心，保障微秒级性能：

```text
  [ 键盘按键事件到达 (Input Router) ]
                │
                ▼ 是否命中 Leader 键？
     ┌──────────┴──────────┐
     │ No                  │ Yes
     ▼                     ▼
  透传送入 PTY 运行     [ 激活 Beacon 模态 (Rust Core) ]
  (Shell / Neovim 零感知)  │
                          ├─ 1. 扫描可见视口元素 (Panes / OSC 133 / UI)
                          ├─ 2. 分配高对比度徽章字符 (a, s, d...)
                          ├─ 3. WGPU 渲染顶层半透明 Badge 场景图层
                          │
                          ▼ 捕获后续单键按键
               ┌──────────┴──────────┐
               │ 命中动作字符        │ 敲击 Esc / 超时
               ▼                     ▼
            触发对应 Action       退出模态，擦除图层，恢复常态
            (执行折叠/切屏/脚本)
```

- **性能保证（[`PB-4`](file:///mnt/data/Workspace/Projects/bitty-terminal/bitty-docs/docs/specifications/text-rendering-rfc.md#L728)）**：
  徽章的收集与绘制完全由 Rust 预分配的小数组完成，耗时严格控制在 **$1 \sim 2$ 毫秒** 内，用户完全感知不到任何停顿；
- **策略开放**：
  哪些元素需要贴徽章、命中后执行什么 Lua 回调，由 Lua 插件层自由决定，开放性无限延伸。

---

## 八、总结与演进蓝图

引入 **Mod 键（Alt）+ Leader 模态（Bitty Beacon）**，使 Bitty 完成了从“模仿传统终端”到“重新定义终端人机工效”的质变：

1. **彻底终结快捷键冲突**：将宿主层控制与客户机 PTY 流彻底物理隔离；
2. **将 Neovim 顶尖全键盘交互搬上全终端**：无论是切面板、折叠万行日志、抓取链接、呼出 AI 输入框，还是派发 Docker 脚本，双手永远无需离开主键盘区；
3. **极简优雅的单手流**：以最小的物理阻力赋予开发者最高的控制自由度，真正兑现了 **“高度可自定义”** 的终极承诺。
