研究下 bitty 现在的 Panel 有什么特点。
就是 Panel 应该是算是 Lua Plugins 生态中非常重要的一部分，大多人应该都是会从 UI 部分做插件的，因为这个对用户来说更直观，而不是像 cargo, kubectl 这种以 CLI 形式的。
那么这样的话 Panel 应该有很多基本的功能才行，并且暴露出去供 Lua 来扩展。
比如这些：

- Panel 在拖动的时候可以不止是有 Hyprland 的布局动画，我以前是用过 KDE Plamsa 的，里面可以对窗口设置很多东西，比如透明度、关闭特效、拖动特效（比如像一张布一样的效果）、Focus 和 Idle 窗口的区别等等。
- 区别于 TUI，也就是 nmtui, btop, 各个 Agent Harness, vim/nvim 等等，Bitty 的 Panel 应该本身就可以进行编辑修改。
- 针对第二条，应该是一个 Panel 可以作为只输入输出命令的 Shell 进程，也可以作为一个独立的程序？之后肯定是有像 Docker, Mail, Discord, Telegram 等等这种看板甚至交互的插件，那么这种的就可以作为单独占用一个 Panel 也是可以的吧？或者说一个 Panel 的状态是可以转换的，可以以一个插件程序展示，也可以退出来继续变成一个 Shell 继续使用，记录也是继续保持，就跟传统 TUI 那样。
- 那么这样的话就会诞生出不止是看板的插件了，甚至可以诞生出可以交互操作的程序，比如就像 Vim/Neovim/Nano 这种可以直接做出一个独立的 Lua 插件，比如叫作 bitter 这个名字，那么这个打开后就直接是 Bitty 原生 UI，而且速度应该是比启动第三方 TUI 更快的吧？
- 像这种的原生 UI 我想到的就是 Windows (WinUI / UWP / WPF) 和 MacOS(AppKit/SwiftUI) 的原生 UI 就是用对应的语言来写的，就像是跟 Flutter, WebView, Electron, Qt, GTK 等等的类比，在终端应该也是可以有这种原生的 UI，而不是像 TUI 这种，有 Bubble Tea, Termui, Ratatui, Textual, Ink, OpenTUI 等等库来做？

在这个方面有什么建议呢？因为 Bitty 本来就就是从最开始类似 Neovim 变成了 Emacs 的，而且定义为可编程形的终端，那么各个部分应该都是可以改造的，如果做得好的话，完全可以变成一个 IDE 和个人工作区吧


## 结论

我看了现在 Bitty 的仓库、最新的 **Panel Runtime RFC、Plugin API v1、Panel Animations RFC**。你的这个方向是成立的，而且实际上 **Bitty 目前的架构已经走到了这个方向的门口**。

现在最关键的一点是：

> **不要继续把 Panel 理解成“终端里的一个窗口”。Panel 应该成为 Bitty Native Application Runtime 的宿主。**

也就是说，未来：

```text
Bitty
├── Terminal Emulator
├── Window / Workspace Manager
├── Native UI Runtime
├── Lua Application Platform
├── Plugin Runtime
├── Automation Runtime
└── AI Workspace
```

而 `Panel` 正好是这些东西汇合的地方。

这会让 Bitty 和 Kitty / Ghostty / WezTerm 的差异越来越大。它更接近：

> **Emacs + Hyprland + Terminal + Native UI Toolkit**

只是底层是 Rust + wgpu，扩展层是 Lua。

---

# 1. 其实现在的 Panel 已经不是 Terminal 了

当前公开仓库仍然明确处于 pre-implementation / foundation 阶段，16-crate workspace 已经拆出了 `ui`、`render`、`lua`、`plugin-host`、`runtime` 等边界，但还没有承诺稳定的 public API 或最终 product behavior。([GitHub][1])

而 9 月 14 日 accepted 的 Panel Runtime RFC 已经明确写出了：

> `Panel` 是 **Generic workspace-managed application container**，不是 OS Window，也不是 PTY。

而且：

```text
PanelId != ViewId != TerminalId
```

`PanelRuntime` 负责 create / mount / suspend / resume / dispose；PTY、GPU object、OS window handle 都不能归 Panel 自己所有。

RFC 甚至已经明确：

```rust
enum PanelContent {
    Terminal(TerminalId),
    Rich(RichBlockId),
    Browser(BrowserSurfaceId),
    Helper(HelperHandle),
    Canvas(CanvasSurfaceId),
}
```

并说明：

> `Panel != Pty`

`TerminalPanel` 可以包装 Terminal，而 `CanvasPanel`、`FilePanel`、`GraphPanel`、`HelperProcessPanel` 都不要求 PTY。

所以你现在提出的 Docker、Mail、Discord、Telegram、编辑器，**不是推翻当前 Panel 架构，而是在继续完成它。**

---

# 2. 现在 Panel 已经非常像一个“小型 Window Manager”

你以前借鉴 Hyprland 的方向实际上已经进入 RFC。

当前 accepted Panel Runtime 已经有：

| Mode         | 语义                                       |
| ------------ | ---------------------------------------- |
| `tiled`      | 普通 Workspace tile                        |
| `floating`   | help/settings/AI/pet/tool                |
| `overlay`    | palette / flash / which-key / completion |
| `fullscreen` | 当前 Workspace 最大化                         |
| `scratchpad` | 隐藏并快速召回                                  |
| `pinned`     | 固定在 Workspace 边缘                         |
| `popover`    | 依附某个 UI element                          |

而且 **Mode 是 Panel 的 runtime property，不是 Panel 类型**。

因此一个 `PanelId`：

```text
tiled
  ↓
floating
  ↓
fullscreen
  ↓
tiled
```

身份、生命周期、输入、surface 都可以保持不变。

这个决定非常重要，因为以后：

```text
bitter
Docker
Git
Mail
Bitty AI
Telegram
File Manager
System Monitor
Markdown Viewer
```

全部可以使用完全相同的 Workspace 操作。

例如：

```text
Mod + F       fullscreen
Mod + Space   tiled ↔ floating
Mod + S       scratchpad
Mod + H/J/K/L focus
Mod + Shift + H/J/K/L move
```

插件根本不用重新发明窗口管理。

---

# 3. 但我建议再加一层：Panel ≠ Activity

这是我认为你现在最应该补上的抽象。

不要设计成：

```lua
panel:set_mode("shell")
panel:set_mode("docker")
panel:set_mode("editor")
```

因为 `shell` / `docker` / `editor` 不是 Panel Mode。

`floating` / `tiled` 才是 Mode。

更好的模型是：

```text
Workspace
    │
    └── Panel
         │
         ├── Presentation Mode
         │    ├── tiled
         │    ├── floating
         │    ├── fullscreen
         │    └── scratchpad
         │
         └── Activity
              ├── TerminalActivity
              ├── LuaAppActivity
              ├── RichActivity
              ├── CanvasActivity
              └── HelperActivity
```

甚至进一步：

```text
Panel
┌──────────────────────────────────┐
│ Activity Stack                   │
│                                  │
│ ┌──────────────────────────────┐ │
│ │ Docker Native UI             │ │ ← active
│ └──────────────────────────────┘ │
│                                  │
│ Terminal Activity               │ ← suspended but alive
└──────────────────────────────────┘
```

这就解决了你说的：

> 打开一个 Docker 插件 → 占据 Panel → 退出 → 回到原来的 Shell，而且 Shell 进程和历史还在。

可以做成类似：

```lua
panel:push(docker_activity)
panel:pop()
```

而不是：

```text
kill shell
→ start app
→ kill app
→ start new shell
```

尤其当前 Bitty 已经把 `TerminalRegistry` 作为 PTY 生命周期真正 owner，Panel 本来就不直接拥有 PTY fd。

因此非常适合：

```text
TerminalSession ─────────────── alive
       │
       │ hidden
       ▼
Panel → DockerActivity
       │
       │ pop()
       ▼
Panel → TerminalActivity
```

PTY 根本没死。

---

# 4. 这个思想其实比传统 TUI 强很多

传统 TUI 的结构是：

```text
Terminal Emulator
    ↓ PTY
Application Process
    ↓ ANSI / VT escape sequences
Cell Grid
    ↓
Renderer
```

例如：

```text
nvim
btop
lazygit
opencode
nmtui
```

本质上都只能告诉终端：

> 第 20 行第 37 列画一个字符。

哪怕 Ratatui / Bubble Tea / Textual 做得再复杂，最后仍然压成 terminal cell grid。

而 Bitty Native UI 可以绕开这一层：

```text
Lua Plugin
    ↓
Bitty UI Tree
    ↓
Layout Engine
    ↓
Text / Widget / Canvas / Image
    ↓
wgpu
```

即：

```text
bitter.lua
    ↓
Bitty Native UI
    ↓
wgpu
```

而不是：

```text
bitter process
    ↓
PTY
    ↓
ANSI
    ↓
VT parser
    ↓
cell grid
    ↓
wgpu
```

这就是你说的“Native UI”。

这个类比基本成立：

| Desktop              | Bitty                        |
| -------------------- | ---------------------------- |
| WinUI / WPF          | Bitty Native UI              |
| AppKit / SwiftUI     | Bitty Native UI              |
| Electron             | WebView-based Panel          |
| GTK / Qt             | 外部 GUI Toolkit               |
| Ratatui / Bubble Tea | TUI over PTY                 |
| ANSI/VT              | Terminal compatibility layer |

但需要强调：

**它是 Bitty-native，不是 OS-native。**

这点非常像 Emacs：Emacs 自己提供 Lisp application environment，并不要求每个应用都变成另一个 OS application。

---

# 5. 所以 `bitter` 完全可以存在

比如：

```text
bitty-terminal/bitter
```

它甚至可以是一个很好的 **first-party reference plugin**。

启动：

```bash
bitty plugin add bitty-terminal/bitter
```

然后：

```text
:e README.md
```

或者：

```lua
bitty.commands.register {
    id = "edit",
    run = function(args)
        bitter.open(args.file)
    end
}
```

Panel 直接切换：

```text
┌─────────────────────────────────────────┐
│ README.md                         bitter │
├─────────────────────────────────────────┤
│                                         │
│ # Bitty                                 │
│                                         │
│ Programmable terminal runtime...        │
│                                         │
│                                         │
├─────────────────────────────────────────┤
│ NORMAL  README.md                 34:12 │
└─────────────────────────────────────────┘
```

退出：

```text
:q
```

不是：

```text
destroy Panel
```

而只是：

```text
panel.pop_activity()
```

然后原来的：

```console
~/Projects/bitty $
```

重新显示。

---

# 6. 但不要让 Lua 自己实现 Text Editor Engine

这是一个非常重要的边界。

`bitter` 可以是 Lua plugin。

但是这些东西不应该用 Lua 重写：

| Rust Core primitive        | Lua application |
| -------------------------- | --------------- |
| Text Buffer / Rope         | Vim-like mode   |
| Cursor                     | Normal / Insert |
| Selection                  | Visual mode     |
| IME                        | keymap          |
| grapheme segmentation      | commands        |
| Unicode shaping            | editor behavior |
| undo/redo engine           | workflows       |
| viewport                   | UI composition  |
| virtualized text rendering | plugins         |
| clipboard primitive        | user commands   |

也就是说：

```text
Rust
 └── TextEditorPrimitive

Lua
 └── bitter
      ├── modes
      ├── keymaps
      ├── commands
      ├── LSP integration
      ├── syntax configuration
      └── UI
```

这样以后不是只有 `bitter` 能用。

Mail composer：

```text
Mail plugin
    ↓
TextEditor
```

Git commit：

```text
Git plugin
    ↓
TextEditor
```

AI prompt editor：

```text
bitty-ai
    ↓
TextEditor
```

Configuration editor：

```text
Settings
    ↓
TextEditor
```

全部复用。

这其实正是 Emacs 最值得 Bitty 学的地方。Emacs 把 `buffer` 作为独立的 editable object，一个 buffer 可以不被任何 window 显示，也可以显示在多个 window 中；window 自身只负责展示，并且维护自己的 display state/history。([gnu.org][2])

Bitty 最好也保持：

```text
Document ≠ View ≠ Panel
```

---

# 7. 我甚至会建议 Bitty 建立一套正式的 Native Widget Layer

当前 Plugin API v1 的 UI 其实非常保守。

现在 accepted 的：

```lua
bitty.ui.mount(slot, component)
bitty.ui.update(handle, component)
```

只能挂载到：

```text
terminal
top
bottom
left
right
tabline
statusline
overlay
```

而 component 当前只开放：

```text
Text
Row
Column
List
```

并明确禁止 global coordinate、shader、pipeline、renderer handle 等底层能力。

这对于 v1 是正确的。

但未来可以从：

```text
Lua UI v1
↓
Declarative Slot UI
```

进化到：

```text
Lua UI v2 / Native Panel UI
↓
Widget Tree
```

比如：

```text
Text
RichText
TextInput
Editor
Button
Toggle
Slider
List
VirtualList
Tree
Table
Tabs
ScrollView
Canvas
Image
Terminal
Split
Stack
Overlay
Popover
```

这里我强烈建议采用 **Retained/Declarative UI**，而不是让 Lua 每帧：

```lua
function draw()
    draw_rect(...)
    draw_text(...)
end
```

否则 Lua 很容易进入 rendering hot path。

更合适的是：

```lua
return ui.column {
    ui.toolbar {
        ui.button {
            text = "Refresh",
            on_click = refresh,
        }
    },

    ui.virtual_list {
        items = containers,
        render = render_container,
    }
}
```

然后：

```text
Lua
 ↓ declarative tree
Rust UI Runtime
 ↓ diff
Layout
 ↓
Hit Test
 ↓
Text shaping
 ↓
wgpu
```

Lua 负责 **state/policy/application logic**。

Rust 负责 **hot path/mechanism/rendering**。

这和 Bitty 当前“hot path 不经过 Lua”的安全和性能原则也是一致的；Panel Runtime RFC 当前明确要求输入链路不能阻塞于 Panel provider 或 Lua。

---

# 8. Terminal 本身最后也可以只是 Native Widget 的一种

这是进一步非常有意思的一步。

未来甚至可以：

```lua
ui.column {
    ui.text { "Logs" },

    ui.terminal {
        session = build_session,
    },

    ui.statusbar {
        ...
    }
}
```

也就是说：

```text
Terminal
```

不再一定等同于：

```text
整个 Panel
```

而可能只是：

```text
Panel
 └── Native UI
      ├── Toolbar
      ├── TerminalView
      ├── Problems
      └── StatusBar
```

这一下就真正开始接近 IDE 了。

比如一个 Rust Development Panel：

```text
┌──────────────────────────────────────────────────────────┐
│ Rust Project · bitty                    Build │ Test │ ▶ │
├──────────────────────────────────────────────────────────┤
│ src/                │ main.rs                            │
│ ├─ app.rs           │                                    │
│ ├─ runtime.rs       │ fn main() {                        │
│ └─ panel.rs         │     ...                            │
│                     │ }                                  │
├─────────────────────┴────────────────────────────────────┤
│ $ cargo test                                             │
│ 708 passed                                               │
├──────────────────────────────────────────────────────────┤
│ Problems 3 │ Tests 708 │ Git main                        │
└──────────────────────────────────────────────────────────┘
```

其中可能同时存在：

```text
TreeView
Editor
TerminalView
ProblemsList
StatusBar
```

全部在 **一个 Panel Application** 中。

---

# 9. 你说的 KDE 那种特效，也应该抽象，但不要直接做成 Panel API

目前 Bitty 的动画 RFC 已经实现了：

```text
Panel open
Panel close
Focus change
Workspace switch
```

并提供 `linear / ease_in / ease_out / ease_in_out / spring`，默认 duration 也有明确上界。

但目前它有意限制得很严：

> 只动画 Core-owned chrome。

同时明确：

> no plugin-defined shaders / native in-process effects

以及插件不能动画 Core-owned chrome。

我认为 **当前 v1 这样非常正确，不应该现在就解除限制**。

但未来可以引入：

```text
Panel Visual State
+
Transition
+
Effect
```

而不是不断加入：

```lua
panel:set_opacity()
panel:set_blur()
panel:set_wobble()
panel:set_scale()
panel:set_shadow()
...
```

未来概念上可以变成：

```lua
panel.style {
    opacity = {
        focused = 1.0,
        unfocused = 0.92,
        idle = 0.82,
    },

    transition = {
        focus = anim.spring(),
    },

    dragging = fx.wobble {
        stiffness = 0.7,
        damping = 0.2,
    },
}
```

内部：

```text
PanelState
├── focused
├── unfocused
├── idle
├── dragging
├── urgent
└── hidden

        ↓

Appearance Resolver
        ↓
VisualStyle
        ↓
Animation Engine
        ↓
Renderer
```

KDE 那种“像布一样拖动”的效果也不要 Lua 每帧计算 mesh。

应该是：

```lua
effect = "wobble"
```

Lua 只是配置参数。

真正的：

```text
mesh deformation
spring simulation
GPU transform
```

由 Rust/wgpu 完成。

---

# 10. Focus / Idle 也值得正式变成 Panel Visual State

这部分我很赞成。

现在大多数 terminal emulator 基本只有：

```text
focused / unfocused
```

Bitty 完全可以更进一步：

```text
Focused
Active
Inactive
Idle
Urgent
Dragging
Suspended
Hidden
```

注意其中有些是：

```text
Lifecycle State
```

有些是：

```text
Interaction State
```

有些是：

```text
Presentation State
```

不要混成一个 enum。

例如：

```text
Lifecycle:
Mounted / Suspended / Disposed

Focus:
Focused / Unfocused

Visibility:
Visible / Occluded / Hidden

Interaction:
Idle / Active / Dragging

Attention:
Normal / Urgent
```

然后 Theme / Effect resolver 根据状态组合决定效果。

这样以后甚至可以：

```lua
appearance.panel {
    ["focus=unfocused"] = {
        opacity = 0.92,
    },

    ["activity=idle"] = {
        opacity = 0.82,
    },

    ["attention=urgent"] = {
        border = theme.warning,
    },
}
```

这比硬编码 KDE-style effect 更具有 Bitty 自己的风格。

---

# 11. Docker / Mail / Discord / Telegram 这类插件会成为真正的“应用”

这里会出现一个很重要的变化：

现在我们通常叫：

```text
plugin
```

以后最好区分：

```text
Plugin
Service
Application
Widget
```

比如：

| 类型             | 示例                                |
| -------------- | --------------------------------- |
| Plugin         | git integration                   |
| Service        | LSP / notification / credential   |
| Widget         | clock / status / CPU graph        |
| Application    | bitter / Docker / Mail / Telegram |
| Panel Provider | 提供 Application surface            |

于是：

```text
bitty-terminal/mail
```

本质可能是：

```text
MailApplication
├── account service
├── mailbox model
├── message list widget
├── mail viewer
├── composer editor
└── commands
```

而不是简单：

```text
mail.lua
```

脚本。

这才会形成真正的 **Bitty App Ecosystem**。

---

# 12. 不过 UI 只是其中一半，另一半是 Application Services

一旦想做 Mail / Telegram / Docker，Native Widget 很快就不是最大的难题了。

真正需要的是：

```text
bitty.process
bitty.network
bitty.fs
bitty.store
bitty.secrets
bitty.tasks
bitty.notifications
bitty.clipboard
bitty.commands
bitty.events
bitty.services
```

当前 Plugin API 已经有 commands、events、settings、store、notifications、services、UI 等雏形，而且 capability model 是 deny-by-default。

以后一个 Telegram plugin 可以声明：

```toml
[capabilities]
network = ["api.telegram.org"]
store = true
notify = true
clipboard = true
```

而 Docker：

```toml
[capabilities]
process = ["docker"]
fs = [...]
```

这就比允许 Lua 任意：

```lua
os.execute(...)
```

强太多了。

所以以后 Bitty Native Application Runtime 的真正核心其实是：

```text
Native UI
+
Application Services
+
Capability Security
+
Lifecycle
```

四件事情。

---

# 13. `bitter` 是否会比 Neovim 这种 TUI 启动更快？

**有可能，但不要把“Native = 一定更快”作为设计承诺。**

理论上它确实省掉：

```text
process spawn
PTY allocation
shell/exec
ANSI serialization
VT parsing
terminal grid reconstruction
```

如果插件 VM 已经存在：

```text
command
 → create Activity
 → instantiate UI tree
 → render
```

可以非常快。

但如果：

```text
Lua 每帧创建几十万个 table
```

或者 editor document model 全部用 Lua 实现，那么一样可以很慢。

所以理想分工仍然是：

```text
Rust:
high-frequency + large-data + rendering primitives

Lua:
low-frequency + orchestration + application behavior
```

那么 `bitter` 的 native startup 确实很可能做到“几乎瞬间”。

---

# 14. 这会不会最后把 Bitty 做成 IDE？

会，但我反而建议 **不要把“IDE”设为最终定义**。

更好的定义是：

> **Terminal-first programmable workspace / application shell**

因为 IDE 只是它可以形成的一种配置。

比如程序员：

```text
Editor
Terminal
Git
AI
Tests
Problems
Docker
```

组合起来：

```text
Bitty → IDE
```

运维：

```text
SSH
Kubernetes
Docker
Logs
Metrics
Shell
```

组合起来：

```text
Bitty → DevOps Workspace
```

普通工作：

```text
Mail
Calendar
Telegram
Notes
Terminal
AI
```

组合起来：

```text
Bitty → Personal Workspace
```

AI 使用：

```text
Agent
Terminal
Diff
Task Board
Logs
Browser
```

组合起来：

```text
Bitty → Agent Workspace
```

所以：

> **Bitty 不需要成为 IDE。Bitty 应该成为“能够构造 IDE 的 Runtime”。**

这和 Emacs 强大的原因其实非常接近：Emacs 不是把 Git、Mail、IDE、Notes 全写进 Core，而是提供足够强且稳定的基本对象，让 Magit、Org、Dired 等应用自然长出来。

---

# 15. 我建议现在按这个顺序推进

这部分我会比较克制，因为现在 Panel Runtime RFC 本身仍明确写着：accepted contract **不等于已经实现的行为**，而首批到底实现 `terminal`，还是再加 `rich/helper/canvas`，目前仍属于 open question。

1. **先完成现有 Panel Runtime**：真正落地 `PanelId / lifecycle / modes / focus / suspend / resume`，不要现在就扩 UI 大系统。

2. **定义 `Activity/Surface` 层**：解决一个 Panel 怎样在 Terminal、Native App、Canvas 等内容之间切换，同时后台 TerminalSession 不死亡。

3. **把 Lua UI 从 Slot UI 扩展为 Widget Tree**：先做 `Text / Row / Column / List / Scroll / Button / Input`，然后再增加 `Tree / Table / Tabs / VirtualList`。

4. **实现 `TextEditor` primitive**：Rust 负责 buffer、cursor、selection、undo、IME、virtualization；然后用 Lua 写一个很小的 `bitter` reference plugin。这会是验证 Native UI Runtime 是否合理的最好项目。

5. **再做 Application Services**：`process / fs / network / tasks / secrets / clipboard / notifications`，严格沿用 capability sandbox。

6. **最后才开放高级 Panel effects**：先保留现在 bounded Core animation；等 Widget Runtime、renderer budget、plugin isolation 稳定以后，再设计 `VisualState + Transition + Effect`，而不是现在开放 raw shader。

---

## 最终我会把 Bitty 的对象模型定成这样

```text
Bitty Instance
│
├── Window
│   │
│   └── Workspace
│       │
│       └── Panel
│           │
│           ├── Presentation
│           │   ├── tiled
│           │   ├── floating
│           │   ├── fullscreen
│           │   ├── scratchpad
│           │   └── pinned
│           │
│           └── Activity
│               ├── Terminal
│               ├── Native App
│               ├── Rich
│               ├── Canvas
│               └── Helper
│
├── UI Runtime
│   ├── Widget Tree
│   ├── Layout
│   ├── Input
│   ├── Focus
│   ├── Text
│   └── Animation
│
├── Application Runtime
│   ├── Commands
│   ├── Events
│   ├── Tasks
│   ├── Services
│   └── Capabilities
│
└── Lua Runtime
    └── Plugins / Applications
```

其中最核心的一句话可以从现在的：

> **Panel is not Terminal**

继续发展成：

> **Panel is a host; Terminal is only one Activity.**

我觉得这句话非常适合成为下一阶段 Panel 架构的原则。它既不会破坏目前已经 accepted 的 Panel RFC，反而能解释你现在提出的 `bitter`、Docker、Mail、Telegram、AI、原生 UI、Shell 保活这些需求为什么应该属于同一套系统。这样发展下去，Bitty 的确会从“可编程终端”自然进入 **可编程 application shell / personal workspace**，而不是靠不断给 terminal emulator 塞功能实现。

[1]: https://github.com/bitty-terminal/bitty "GitHub - bitty-terminal/bitty: Programmable terminal runtime and core implementation · GitHub"
[2]: https://www.gnu.org/s/emacs/manual/html_node/elisp/Buffers.html?utm_source=chatgpt.com "Buffers (GNU Emacs Lisp Reference Manual)"
## 结论

我看了现在 Bitty 的仓库、最新的 **Panel Runtime RFC、Plugin API v1、Panel Animations RFC**。你的这个方向是成立的，而且实际上 **Bitty 目前的架构已经走到了这个方向的门口**。

现在最关键的一点是：

> **不要继续把 Panel 理解成“终端里的一个窗口”。Panel 应该成为 Bitty Native Application Runtime 的宿主。**

也就是说，未来：

```text
Bitty
├── Terminal Emulator
├── Window / Workspace Manager
├── Native UI Runtime
├── Lua Application Platform
├── Plugin Runtime
├── Automation Runtime
└── AI Workspace
```

而 `Panel` 正好是这些东西汇合的地方。

这会让 Bitty 和 Kitty / Ghostty / WezTerm 的差异越来越大。它更接近：

> **Emacs + Hyprland + Terminal + Native UI Toolkit**

只是底层是 Rust + wgpu，扩展层是 Lua。

---

# 1. 其实现在的 Panel 已经不是 Terminal 了

当前公开仓库仍然明确处于 pre-implementation / foundation 阶段，16-crate workspace 已经拆出了 `ui`、`render`、`lua`、`plugin-host`、`runtime` 等边界，但还没有承诺稳定的 public API 或最终 product behavior。([GitHub][1])

而 9 月 14 日 accepted 的 Panel Runtime RFC 已经明确写出了：

> `Panel` 是 **Generic workspace-managed application container**，不是 OS Window，也不是 PTY。

而且：

```text
PanelId != ViewId != TerminalId
```

`PanelRuntime` 负责 create / mount / suspend / resume / dispose；PTY、GPU object、OS window handle 都不能归 Panel 自己所有。

RFC 甚至已经明确：

```rust
enum PanelContent {
    Terminal(TerminalId),
    Rich(RichBlockId),
    Browser(BrowserSurfaceId),
    Helper(HelperHandle),
    Canvas(CanvasSurfaceId),
}
```

并说明：

> `Panel != Pty`

`TerminalPanel` 可以包装 Terminal，而 `CanvasPanel`、`FilePanel`、`GraphPanel`、`HelperProcessPanel` 都不要求 PTY。

所以你现在提出的 Docker、Mail、Discord、Telegram、编辑器，**不是推翻当前 Panel 架构，而是在继续完成它。**

---

# 2. 现在 Panel 已经非常像一个“小型 Window Manager”

你以前借鉴 Hyprland 的方向实际上已经进入 RFC。

当前 accepted Panel Runtime 已经有：

| Mode         | 语义                                       |
| ------------ | ---------------------------------------- |
| `tiled`      | 普通 Workspace tile                        |
| `floating`   | help/settings/AI/pet/tool                |
| `overlay`    | palette / flash / which-key / completion |
| `fullscreen` | 当前 Workspace 最大化                         |
| `scratchpad` | 隐藏并快速召回                                  |
| `pinned`     | 固定在 Workspace 边缘                         |
| `popover`    | 依附某个 UI element                          |

而且 **Mode 是 Panel 的 runtime property，不是 Panel 类型**。

因此一个 `PanelId`：

```text
tiled
  ↓
floating
  ↓
fullscreen
  ↓
tiled
```

身份、生命周期、输入、surface 都可以保持不变。

这个决定非常重要，因为以后：

```text
bitter
Docker
Git
Mail
Bitty AI
Telegram
File Manager
System Monitor
Markdown Viewer
```

全部可以使用完全相同的 Workspace 操作。

例如：

```text
Mod + F       fullscreen
Mod + Space   tiled ↔ floating
Mod + S       scratchpad
Mod + H/J/K/L focus
Mod + Shift + H/J/K/L move
```

插件根本不用重新发明窗口管理。

---

# 3. 但我建议再加一层：Panel ≠ Activity

这是我认为你现在最应该补上的抽象。

不要设计成：

```lua
panel:set_mode("shell")
panel:set_mode("docker")
panel:set_mode("editor")
```

因为 `shell` / `docker` / `editor` 不是 Panel Mode。

`floating` / `tiled` 才是 Mode。

更好的模型是：

```text
Workspace
    │
    └── Panel
         │
         ├── Presentation Mode
         │    ├── tiled
         │    ├── floating
         │    ├── fullscreen
         │    └── scratchpad
         │
         └── Activity
              ├── TerminalActivity
              ├── LuaAppActivity
              ├── RichActivity
              ├── CanvasActivity
              └── HelperActivity
```

甚至进一步：

```text
Panel
┌──────────────────────────────────┐
│ Activity Stack                   │
│                                  │
│ ┌──────────────────────────────┐ │
│ │ Docker Native UI             │ │ ← active
│ └──────────────────────────────┘ │
│                                  │
│ Terminal Activity               │ ← suspended but alive
└──────────────────────────────────┘
```

这就解决了你说的：

> 打开一个 Docker 插件 → 占据 Panel → 退出 → 回到原来的 Shell，而且 Shell 进程和历史还在。

可以做成类似：

```lua
panel:push(docker_activity)
panel:pop()
```

而不是：

```text
kill shell
→ start app
→ kill app
→ start new shell
```

尤其当前 Bitty 已经把 `TerminalRegistry` 作为 PTY 生命周期真正 owner，Panel 本来就不直接拥有 PTY fd。

因此非常适合：

```text
TerminalSession ─────────────── alive
       │
       │ hidden
       ▼
Panel → DockerActivity
       │
       │ pop()
       ▼
Panel → TerminalActivity
```

PTY 根本没死。

---

# 4. 这个思想其实比传统 TUI 强很多

传统 TUI 的结构是：

```text
Terminal Emulator
    ↓ PTY
Application Process
    ↓ ANSI / VT escape sequences
Cell Grid
    ↓
Renderer
```

例如：

```text
nvim
btop
lazygit
opencode
nmtui
```

本质上都只能告诉终端：

> 第 20 行第 37 列画一个字符。

哪怕 Ratatui / Bubble Tea / Textual 做得再复杂，最后仍然压成 terminal cell grid。

而 Bitty Native UI 可以绕开这一层：

```text
Lua Plugin
    ↓
Bitty UI Tree
    ↓
Layout Engine
    ↓
Text / Widget / Canvas / Image
    ↓
wgpu
```

即：

```text
bitter.lua
    ↓
Bitty Native UI
    ↓
wgpu
```

而不是：

```text
bitter process
    ↓
PTY
    ↓
ANSI
    ↓
VT parser
    ↓
cell grid
    ↓
wgpu
```

这就是你说的“Native UI”。

这个类比基本成立：

| Desktop              | Bitty                        |
| -------------------- | ---------------------------- |
| WinUI / WPF          | Bitty Native UI              |
| AppKit / SwiftUI     | Bitty Native UI              |
| Electron             | WebView-based Panel          |
| GTK / Qt             | 外部 GUI Toolkit               |
| Ratatui / Bubble Tea | TUI over PTY                 |
| ANSI/VT              | Terminal compatibility layer |

但需要强调：

**它是 Bitty-native，不是 OS-native。**

这点非常像 Emacs：Emacs 自己提供 Lisp application environment，并不要求每个应用都变成另一个 OS application。

---

# 5. 所以 `bitter` 完全可以存在

比如：

```text
bitty-terminal/bitter
```

它甚至可以是一个很好的 **first-party reference plugin**。

启动：

```bash
bitty plugin add bitty-terminal/bitter
```

然后：

```text
:e README.md
```

或者：

```lua
bitty.commands.register {
    id = "edit",
    run = function(args)
        bitter.open(args.file)
    end
}
```

Panel 直接切换：

```text
┌─────────────────────────────────────────┐
│ README.md                         bitter │
├─────────────────────────────────────────┤
│                                         │
│ # Bitty                                 │
│                                         │
│ Programmable terminal runtime...        │
│                                         │
│                                         │
├─────────────────────────────────────────┤
│ NORMAL  README.md                 34:12 │
└─────────────────────────────────────────┘
```

退出：

```text
:q
```

不是：

```text
destroy Panel
```

而只是：

```text
panel.pop_activity()
```

然后原来的：

```console
~/Projects/bitty $
```

重新显示。

---

# 6. 但不要让 Lua 自己实现 Text Editor Engine

这是一个非常重要的边界。

`bitter` 可以是 Lua plugin。

但是这些东西不应该用 Lua 重写：

| Rust Core primitive        | Lua application |
| -------------------------- | --------------- |
| Text Buffer / Rope         | Vim-like mode   |
| Cursor                     | Normal / Insert |
| Selection                  | Visual mode     |
| IME                        | keymap          |
| grapheme segmentation      | commands        |
| Unicode shaping            | editor behavior |
| undo/redo engine           | workflows       |
| viewport                   | UI composition  |
| virtualized text rendering | plugins         |
| clipboard primitive        | user commands   |

也就是说：

```text
Rust
 └── TextEditorPrimitive

Lua
 └── bitter
      ├── modes
      ├── keymaps
      ├── commands
      ├── LSP integration
      ├── syntax configuration
      └── UI
```

这样以后不是只有 `bitter` 能用。

Mail composer：

```text
Mail plugin
    ↓
TextEditor
```

Git commit：

```text
Git plugin
    ↓
TextEditor
```

AI prompt editor：

```text
bitty-ai
    ↓
TextEditor
```

Configuration editor：

```text
Settings
    ↓
TextEditor
```

全部复用。

这其实正是 Emacs 最值得 Bitty 学的地方。Emacs 把 `buffer` 作为独立的 editable object，一个 buffer 可以不被任何 window 显示，也可以显示在多个 window 中；window 自身只负责展示，并且维护自己的 display state/history。([gnu.org][2])

Bitty 最好也保持：

```text
Document ≠ View ≠ Panel
```

---

# 7. 我甚至会建议 Bitty 建立一套正式的 Native Widget Layer

当前 Plugin API v1 的 UI 其实非常保守。

现在 accepted 的：

```lua
bitty.ui.mount(slot, component)
bitty.ui.update(handle, component)
```

只能挂载到：

```text
terminal
top
bottom
left
right
tabline
statusline
overlay
```

而 component 当前只开放：

```text
Text
Row
Column
List
```

并明确禁止 global coordinate、shader、pipeline、renderer handle 等底层能力。

这对于 v1 是正确的。

但未来可以从：

```text
Lua UI v1
↓
Declarative Slot UI
```

进化到：

```text
Lua UI v2 / Native Panel UI
↓
Widget Tree
```

比如：

```text
Text
RichText
TextInput
Editor
Button
Toggle
Slider
List
VirtualList
Tree
Table
Tabs
ScrollView
Canvas
Image
Terminal
Split
Stack
Overlay
Popover
```

这里我强烈建议采用 **Retained/Declarative UI**，而不是让 Lua 每帧：

```lua
function draw()
    draw_rect(...)
    draw_text(...)
end
```

否则 Lua 很容易进入 rendering hot path。

更合适的是：

```lua
return ui.column {
    ui.toolbar {
        ui.button {
            text = "Refresh",
            on_click = refresh,
        }
    },

    ui.virtual_list {
        items = containers,
        render = render_container,
    }
}
```

然后：

```text
Lua
 ↓ declarative tree
Rust UI Runtime
 ↓ diff
Layout
 ↓
Hit Test
 ↓
Text shaping
 ↓
wgpu
```

Lua 负责 **state/policy/application logic**。

Rust 负责 **hot path/mechanism/rendering**。

这和 Bitty 当前“hot path 不经过 Lua”的安全和性能原则也是一致的；Panel Runtime RFC 当前明确要求输入链路不能阻塞于 Panel provider 或 Lua。

---

# 8. Terminal 本身最后也可以只是 Native Widget 的一种

这是进一步非常有意思的一步。

未来甚至可以：

```lua
ui.column {
    ui.text { "Logs" },

    ui.terminal {
        session = build_session,
    },

    ui.statusbar {
        ...
    }
}
```

也就是说：

```text
Terminal
```

不再一定等同于：

```text
整个 Panel
```

而可能只是：

```text
Panel
 └── Native UI
      ├── Toolbar
      ├── TerminalView
      ├── Problems
      └── StatusBar
```

这一下就真正开始接近 IDE 了。

比如一个 Rust Development Panel：

```text
┌──────────────────────────────────────────────────────────┐
│ Rust Project · bitty                    Build │ Test │ ▶ │
├──────────────────────────────────────────────────────────┤
│ src/                │ main.rs                            │
│ ├─ app.rs           │                                    │
│ ├─ runtime.rs       │ fn main() {                        │
│ └─ panel.rs         │     ...                            │
│                     │ }                                  │
├─────────────────────┴────────────────────────────────────┤
│ $ cargo test                                             │
│ 708 passed                                               │
├──────────────────────────────────────────────────────────┤
│ Problems 3 │ Tests 708 │ Git main                        │
└──────────────────────────────────────────────────────────┘
```

其中可能同时存在：

```text
TreeView
Editor
TerminalView
ProblemsList
StatusBar
```

全部在 **一个 Panel Application** 中。

---

# 9. 你说的 KDE 那种特效，也应该抽象，但不要直接做成 Panel API

目前 Bitty 的动画 RFC 已经实现了：

```text
Panel open
Panel close
Focus change
Workspace switch
```

并提供 `linear / ease_in / ease_out / ease_in_out / spring`，默认 duration 也有明确上界。

但目前它有意限制得很严：

> 只动画 Core-owned chrome。

同时明确：

> no plugin-defined shaders / native in-process effects

以及插件不能动画 Core-owned chrome。

我认为 **当前 v1 这样非常正确，不应该现在就解除限制**。

但未来可以引入：

```text
Panel Visual State
+
Transition
+
Effect
```

而不是不断加入：

```lua
panel:set_opacity()
panel:set_blur()
panel:set_wobble()
panel:set_scale()
panel:set_shadow()
...
```

未来概念上可以变成：

```lua
panel.style {
    opacity = {
        focused = 1.0,
        unfocused = 0.92,
        idle = 0.82,
    },

    transition = {
        focus = anim.spring(),
    },

    dragging = fx.wobble {
        stiffness = 0.7,
        damping = 0.2,
    },
}
```

内部：

```text
PanelState
├── focused
├── unfocused
├── idle
├── dragging
├── urgent
└── hidden

        ↓

Appearance Resolver
        ↓
VisualStyle
        ↓
Animation Engine
        ↓
Renderer
```

KDE 那种“像布一样拖动”的效果也不要 Lua 每帧计算 mesh。

应该是：

```lua
effect = "wobble"
```

Lua 只是配置参数。

真正的：

```text
mesh deformation
spring simulation
GPU transform
```

由 Rust/wgpu 完成。

---

# 10. Focus / Idle 也值得正式变成 Panel Visual State

这部分我很赞成。

现在大多数 terminal emulator 基本只有：

```text
focused / unfocused
```

Bitty 完全可以更进一步：

```text
Focused
Active
Inactive
Idle
Urgent
Dragging
Suspended
Hidden
```

注意其中有些是：

```text
Lifecycle State
```

有些是：

```text
Interaction State
```

有些是：

```text
Presentation State
```

不要混成一个 enum。

例如：

```text
Lifecycle:
Mounted / Suspended / Disposed

Focus:
Focused / Unfocused

Visibility:
Visible / Occluded / Hidden

Interaction:
Idle / Active / Dragging

Attention:
Normal / Urgent
```

然后 Theme / Effect resolver 根据状态组合决定效果。

这样以后甚至可以：

```lua
appearance.panel {
    ["focus=unfocused"] = {
        opacity = 0.92,
    },

    ["activity=idle"] = {
        opacity = 0.82,
    },

    ["attention=urgent"] = {
        border = theme.warning,
    },
}
```

这比硬编码 KDE-style effect 更具有 Bitty 自己的风格。

---

# 11. Docker / Mail / Discord / Telegram 这类插件会成为真正的“应用”

这里会出现一个很重要的变化：

现在我们通常叫：

```text
plugin
```

以后最好区分：

```text
Plugin
Service
Application
Widget
```

比如：

| 类型             | 示例                                |
| -------------- | --------------------------------- |
| Plugin         | git integration                   |
| Service        | LSP / notification / credential   |
| Widget         | clock / status / CPU graph        |
| Application    | bitter / Docker / Mail / Telegram |
| Panel Provider | 提供 Application surface            |

于是：

```text
bitty-terminal/mail
```

本质可能是：

```text
MailApplication
├── account service
├── mailbox model
├── message list widget
├── mail viewer
├── composer editor
└── commands
```

而不是简单：

```text
mail.lua
```

脚本。

这才会形成真正的 **Bitty App Ecosystem**。

---

# 12. 不过 UI 只是其中一半，另一半是 Application Services

一旦想做 Mail / Telegram / Docker，Native Widget 很快就不是最大的难题了。

真正需要的是：

```text
bitty.process
bitty.network
bitty.fs
bitty.store
bitty.secrets
bitty.tasks
bitty.notifications
bitty.clipboard
bitty.commands
bitty.events
bitty.services
```

当前 Plugin API 已经有 commands、events、settings、store、notifications、services、UI 等雏形，而且 capability model 是 deny-by-default。

以后一个 Telegram plugin 可以声明：

```toml
[capabilities]
network = ["api.telegram.org"]
store = true
notify = true
clipboard = true
```

而 Docker：

```toml
[capabilities]
process = ["docker"]
fs = [...]
```

这就比允许 Lua 任意：

```lua
os.execute(...)
```

强太多了。

所以以后 Bitty Native Application Runtime 的真正核心其实是：

```text
Native UI
+
Application Services
+
Capability Security
+
Lifecycle
```

四件事情。

---

# 13. `bitter` 是否会比 Neovim 这种 TUI 启动更快？

**有可能，但不要把“Native = 一定更快”作为设计承诺。**

理论上它确实省掉：

```text
process spawn
PTY allocation
shell/exec
ANSI serialization
VT parsing
terminal grid reconstruction
```

如果插件 VM 已经存在：

```text
command
 → create Activity
 → instantiate UI tree
 → render
```

可以非常快。

但如果：

```text
Lua 每帧创建几十万个 table
```

或者 editor document model 全部用 Lua 实现，那么一样可以很慢。

所以理想分工仍然是：

```text
Rust:
high-frequency + large-data + rendering primitives

Lua:
low-frequency + orchestration + application behavior
```

那么 `bitter` 的 native startup 确实很可能做到“几乎瞬间”。

---

# 14. 这会不会最后把 Bitty 做成 IDE？

会，但我反而建议 **不要把“IDE”设为最终定义**。

更好的定义是：

> **Terminal-first programmable workspace / application shell**

因为 IDE 只是它可以形成的一种配置。

比如程序员：

```text
Editor
Terminal
Git
AI
Tests
Problems
Docker
```

组合起来：

```text
Bitty → IDE
```

运维：

```text
SSH
Kubernetes
Docker
Logs
Metrics
Shell
```

组合起来：

```text
Bitty → DevOps Workspace
```

普通工作：

```text
Mail
Calendar
Telegram
Notes
Terminal
AI
```

组合起来：

```text
Bitty → Personal Workspace
```

AI 使用：

```text
Agent
Terminal
Diff
Task Board
Logs
Browser
```

组合起来：

```text
Bitty → Agent Workspace
```

所以：

> **Bitty 不需要成为 IDE。Bitty 应该成为“能够构造 IDE 的 Runtime”。**

这和 Emacs 强大的原因其实非常接近：Emacs 不是把 Git、Mail、IDE、Notes 全写进 Core，而是提供足够强且稳定的基本对象，让 Magit、Org、Dired 等应用自然长出来。

---

# 15. 我建议现在按这个顺序推进

这部分我会比较克制，因为现在 Panel Runtime RFC 本身仍明确写着：accepted contract **不等于已经实现的行为**，而首批到底实现 `terminal`，还是再加 `rich/helper/canvas`，目前仍属于 open question。

1. **先完成现有 Panel Runtime**：真正落地 `PanelId / lifecycle / modes / focus / suspend / resume`，不要现在就扩 UI 大系统。

2. **定义 `Activity/Surface` 层**：解决一个 Panel 怎样在 Terminal、Native App、Canvas 等内容之间切换，同时后台 TerminalSession 不死亡。

3. **把 Lua UI 从 Slot UI 扩展为 Widget Tree**：先做 `Text / Row / Column / List / Scroll / Button / Input`，然后再增加 `Tree / Table / Tabs / VirtualList`。

4. **实现 `TextEditor` primitive**：Rust 负责 buffer、cursor、selection、undo、IME、virtualization；然后用 Lua 写一个很小的 `bitter` reference plugin。这会是验证 Native UI Runtime 是否合理的最好项目。

5. **再做 Application Services**：`process / fs / network / tasks / secrets / clipboard / notifications`，严格沿用 capability sandbox。

6. **最后才开放高级 Panel effects**：先保留现在 bounded Core animation；等 Widget Runtime、renderer budget、plugin isolation 稳定以后，再设计 `VisualState + Transition + Effect`，而不是现在开放 raw shader。

---

## 最终我会把 Bitty 的对象模型定成这样

```text
Bitty Instance
│
├── Window
│   │
│   └── Workspace
│       │
│       └── Panel
│           │
│           ├── Presentation
│           │   ├── tiled
│           │   ├── floating
│           │   ├── fullscreen
│           │   ├── scratchpad
│           │   └── pinned
│           │
│           └── Activity
│               ├── Terminal
│               ├── Native App
│               ├── Rich
│               ├── Canvas
│               └── Helper
│
├── UI Runtime
│   ├── Widget Tree
│   ├── Layout
│   ├── Input
│   ├── Focus
│   ├── Text
│   └── Animation
│
├── Application Runtime
│   ├── Commands
│   ├── Events
│   ├── Tasks
│   ├── Services
│   └── Capabilities
│
└── Lua Runtime
    └── Plugins / Applications
```

其中最核心的一句话可以从现在的：

> **Panel is not Terminal**

继续发展成：

> **Panel is a host; Terminal is only one Activity.**

我觉得这句话非常适合成为下一阶段 Panel 架构的原则。它既不会破坏目前已经 accepted 的 Panel RFC，反而能解释你现在提出的 `bitter`、Docker、Mail、Telegram、AI、原生 UI、Shell 保活这些需求为什么应该属于同一套系统。这样发展下去，Bitty 的确会从“可编程终端”自然进入 **可编程 application shell / personal workspace**，而不是靠不断给 terminal emulator 塞功能实现。

[1]: https://github.com/bitty-terminal/bitty "GitHub - bitty-terminal/bitty: Programmable terminal runtime and core implementation · GitHub"
[2]: https://www.gnu.org/s/emacs/manual/html_node/elisp/Buffers.html?utm_source=chatgpt.com "Buffers (GNU Emacs Lisp Reference Manual)"
## 结论

我看了现在 Bitty 的仓库、最新的 **Panel Runtime RFC、Plugin API v1、Panel Animations RFC**。你的这个方向是成立的，而且实际上 **Bitty 目前的架构已经走到了这个方向的门口**。

现在最关键的一点是：

> **不要继续把 Panel 理解成“终端里的一个窗口”。Panel 应该成为 Bitty Native Application Runtime 的宿主。**

也就是说，未来：

```text
Bitty
├── Terminal Emulator
├── Window / Workspace Manager
├── Native UI Runtime
├── Lua Application Platform
├── Plugin Runtime
├── Automation Runtime
└── AI Workspace
```

而 `Panel` 正好是这些东西汇合的地方。

这会让 Bitty 和 Kitty / Ghostty / WezTerm 的差异越来越大。它更接近：

> **Emacs + Hyprland + Terminal + Native UI Toolkit**

只是底层是 Rust + wgpu，扩展层是 Lua。

---

# 1. 其实现在的 Panel 已经不是 Terminal 了

当前公开仓库仍然明确处于 pre-implementation / foundation 阶段，16-crate workspace 已经拆出了 `ui`、`render`、`lua`、`plugin-host`、`runtime` 等边界，但还没有承诺稳定的 public API 或最终 product behavior。([GitHub][1])

而 9 月 14 日 accepted 的 Panel Runtime RFC 已经明确写出了：

> `Panel` 是 **Generic workspace-managed application container**，不是 OS Window，也不是 PTY。

而且：

```text
PanelId != ViewId != TerminalId
```

`PanelRuntime` 负责 create / mount / suspend / resume / dispose；PTY、GPU object、OS window handle 都不能归 Panel 自己所有。

RFC 甚至已经明确：

```rust
enum PanelContent {
    Terminal(TerminalId),
    Rich(RichBlockId),
    Browser(BrowserSurfaceId),
    Helper(HelperHandle),
    Canvas(CanvasSurfaceId),
}
```

并说明：

> `Panel != Pty`

`TerminalPanel` 可以包装 Terminal，而 `CanvasPanel`、`FilePanel`、`GraphPanel`、`HelperProcessPanel` 都不要求 PTY。

所以你现在提出的 Docker、Mail、Discord、Telegram、编辑器，**不是推翻当前 Panel 架构，而是在继续完成它。**

---

# 2. 现在 Panel 已经非常像一个“小型 Window Manager”

你以前借鉴 Hyprland 的方向实际上已经进入 RFC。

当前 accepted Panel Runtime 已经有：

| Mode         | 语义                                       |
| ------------ | ---------------------------------------- |
| `tiled`      | 普通 Workspace tile                        |
| `floating`   | help/settings/AI/pet/tool                |
| `overlay`    | palette / flash / which-key / completion |
| `fullscreen` | 当前 Workspace 最大化                         |
| `scratchpad` | 隐藏并快速召回                                  |
| `pinned`     | 固定在 Workspace 边缘                         |
| `popover`    | 依附某个 UI element                          |

而且 **Mode 是 Panel 的 runtime property，不是 Panel 类型**。

因此一个 `PanelId`：

```text
tiled
  ↓
floating
  ↓
fullscreen
  ↓
tiled
```

身份、生命周期、输入、surface 都可以保持不变。

这个决定非常重要，因为以后：

```text
bitter
Docker
Git
Mail
Bitty AI
Telegram
File Manager
System Monitor
Markdown Viewer
```

全部可以使用完全相同的 Workspace 操作。

例如：

```text
Mod + F       fullscreen
Mod + Space   tiled ↔ floating
Mod + S       scratchpad
Mod + H/J/K/L focus
Mod + Shift + H/J/K/L move
```

插件根本不用重新发明窗口管理。

---

# 3. 但我建议再加一层：Panel ≠ Activity

这是我认为你现在最应该补上的抽象。

不要设计成：

```lua
panel:set_mode("shell")
panel:set_mode("docker")
panel:set_mode("editor")
```

因为 `shell` / `docker` / `editor` 不是 Panel Mode。

`floating` / `tiled` 才是 Mode。

更好的模型是：

```text
Workspace
    │
    └── Panel
         │
         ├── Presentation Mode
         │    ├── tiled
         │    ├── floating
         │    ├── fullscreen
         │    └── scratchpad
         │
         └── Activity
              ├── TerminalActivity
              ├── LuaAppActivity
              ├── RichActivity
              ├── CanvasActivity
              └── HelperActivity
```

甚至进一步：

```text
Panel
┌──────────────────────────────────┐
│ Activity Stack                   │
│                                  │
│ ┌──────────────────────────────┐ │
│ │ Docker Native UI             │ │ ← active
│ └──────────────────────────────┘ │
│                                  │
│ Terminal Activity               │ ← suspended but alive
└──────────────────────────────────┘
```

这就解决了你说的：

> 打开一个 Docker 插件 → 占据 Panel → 退出 → 回到原来的 Shell，而且 Shell 进程和历史还在。

可以做成类似：

```lua
panel:push(docker_activity)
panel:pop()
```

而不是：

```text
kill shell
→ start app
→ kill app
→ start new shell
```

尤其当前 Bitty 已经把 `TerminalRegistry` 作为 PTY 生命周期真正 owner，Panel 本来就不直接拥有 PTY fd。

因此非常适合：

```text
TerminalSession ─────────────── alive
       │
       │ hidden
       ▼
Panel → DockerActivity
       │
       │ pop()
       ▼
Panel → TerminalActivity
```

PTY 根本没死。

---

# 4. 这个思想其实比传统 TUI 强很多

传统 TUI 的结构是：

```text
Terminal Emulator
    ↓ PTY
Application Process
    ↓ ANSI / VT escape sequences
Cell Grid
    ↓
Renderer
```

例如：

```text
nvim
btop
lazygit
opencode
nmtui
```

本质上都只能告诉终端：

> 第 20 行第 37 列画一个字符。

哪怕 Ratatui / Bubble Tea / Textual 做得再复杂，最后仍然压成 terminal cell grid。

而 Bitty Native UI 可以绕开这一层：

```text
Lua Plugin
    ↓
Bitty UI Tree
    ↓
Layout Engine
    ↓
Text / Widget / Canvas / Image
    ↓
wgpu
```

即：

```text
bitter.lua
    ↓
Bitty Native UI
    ↓
wgpu
```

而不是：

```text
bitter process
    ↓
PTY
    ↓
ANSI
    ↓
VT parser
    ↓
cell grid
    ↓
wgpu
```

这就是你说的“Native UI”。

这个类比基本成立：

| Desktop              | Bitty                        |
| -------------------- | ---------------------------- |
| WinUI / WPF          | Bitty Native UI              |
| AppKit / SwiftUI     | Bitty Native UI              |
| Electron             | WebView-based Panel          |
| GTK / Qt             | 外部 GUI Toolkit               |
| Ratatui / Bubble Tea | TUI over PTY                 |
| ANSI/VT              | Terminal compatibility layer |

但需要强调：

**它是 Bitty-native，不是 OS-native。**

这点非常像 Emacs：Emacs 自己提供 Lisp application environment，并不要求每个应用都变成另一个 OS application。

---

# 5. 所以 `bitter` 完全可以存在

比如：

```text
bitty-terminal/bitter
```

它甚至可以是一个很好的 **first-party reference plugin**。

启动：

```bash
bitty plugin add bitty-terminal/bitter
```

然后：

```text
:e README.md
```

或者：

```lua
bitty.commands.register {
    id = "edit",
    run = function(args)
        bitter.open(args.file)
    end
}
```

Panel 直接切换：

```text
┌─────────────────────────────────────────┐
│ README.md                         bitter │
├─────────────────────────────────────────┤
│                                         │
│ # Bitty                                 │
│                                         │
│ Programmable terminal runtime...        │
│                                         │
│                                         │
├─────────────────────────────────────────┤
│ NORMAL  README.md                 34:12 │
└─────────────────────────────────────────┘
```

退出：

```text
:q
```

不是：

```text
destroy Panel
```

而只是：

```text
panel.pop_activity()
```

然后原来的：

```console
~/Projects/bitty $
```

重新显示。

---

# 6. 但不要让 Lua 自己实现 Text Editor Engine

这是一个非常重要的边界。

`bitter` 可以是 Lua plugin。

但是这些东西不应该用 Lua 重写：

| Rust Core primitive        | Lua application |
| -------------------------- | --------------- |
| Text Buffer / Rope         | Vim-like mode   |
| Cursor                     | Normal / Insert |
| Selection                  | Visual mode     |
| IME                        | keymap          |
| grapheme segmentation      | commands        |
| Unicode shaping            | editor behavior |
| undo/redo engine           | workflows       |
| viewport                   | UI composition  |
| virtualized text rendering | plugins         |
| clipboard primitive        | user commands   |

也就是说：

```text
Rust
 └── TextEditorPrimitive

Lua
 └── bitter
      ├── modes
      ├── keymaps
      ├── commands
      ├── LSP integration
      ├── syntax configuration
      └── UI
```

这样以后不是只有 `bitter` 能用。

Mail composer：

```text
Mail plugin
    ↓
TextEditor
```

Git commit：

```text
Git plugin
    ↓
TextEditor
```

AI prompt editor：

```text
bitty-ai
    ↓
TextEditor
```

Configuration editor：

```text
Settings
    ↓
TextEditor
```

全部复用。

这其实正是 Emacs 最值得 Bitty 学的地方。Emacs 把 `buffer` 作为独立的 editable object，一个 buffer 可以不被任何 window 显示，也可以显示在多个 window 中；window 自身只负责展示，并且维护自己的 display state/history。([gnu.org][2])

Bitty 最好也保持：

```text
Document ≠ View ≠ Panel
```

---

# 7. 我甚至会建议 Bitty 建立一套正式的 Native Widget Layer

当前 Plugin API v1 的 UI 其实非常保守。

现在 accepted 的：

```lua
bitty.ui.mount(slot, component)
bitty.ui.update(handle, component)
```

只能挂载到：

```text
terminal
top
bottom
left
right
tabline
statusline
overlay
```

而 component 当前只开放：

```text
Text
Row
Column
List
```

并明确禁止 global coordinate、shader、pipeline、renderer handle 等底层能力。

这对于 v1 是正确的。

但未来可以从：

```text
Lua UI v1
↓
Declarative Slot UI
```

进化到：

```text
Lua UI v2 / Native Panel UI
↓
Widget Tree
```

比如：

```text
Text
RichText
TextInput
Editor
Button
Toggle
Slider
List
VirtualList
Tree
Table
Tabs
ScrollView
Canvas
Image
Terminal
Split
Stack
Overlay
Popover
```

这里我强烈建议采用 **Retained/Declarative UI**，而不是让 Lua 每帧：

```lua
function draw()
    draw_rect(...)
    draw_text(...)
end
```

否则 Lua 很容易进入 rendering hot path。

更合适的是：

```lua
return ui.column {
    ui.toolbar {
        ui.button {
            text = "Refresh",
            on_click = refresh,
        }
    },

    ui.virtual_list {
        items = containers,
        render = render_container,
    }
}
```

然后：

```text
Lua
 ↓ declarative tree
Rust UI Runtime
 ↓ diff
Layout
 ↓
Hit Test
 ↓
Text shaping
 ↓
wgpu
```

Lua 负责 **state/policy/application logic**。

Rust 负责 **hot path/mechanism/rendering**。

这和 Bitty 当前“hot path 不经过 Lua”的安全和性能原则也是一致的；Panel Runtime RFC 当前明确要求输入链路不能阻塞于 Panel provider 或 Lua。

---

# 8. Terminal 本身最后也可以只是 Native Widget 的一种

这是进一步非常有意思的一步。

未来甚至可以：

```lua
ui.column {
    ui.text { "Logs" },

    ui.terminal {
        session = build_session,
    },

    ui.statusbar {
        ...
    }
}
```

也就是说：

```text
Terminal
```

不再一定等同于：

```text
整个 Panel
```

而可能只是：

```text
Panel
 └── Native UI
      ├── Toolbar
      ├── TerminalView
      ├── Problems
      └── StatusBar
```

这一下就真正开始接近 IDE 了。

比如一个 Rust Development Panel：

```text
┌──────────────────────────────────────────────────────────┐
│ Rust Project · bitty                    Build │ Test │ ▶ │
├──────────────────────────────────────────────────────────┤
│ src/                │ main.rs                            │
│ ├─ app.rs           │                                    │
│ ├─ runtime.rs       │ fn main() {                        │
│ └─ panel.rs         │     ...                            │
│                     │ }                                  │
├─────────────────────┴────────────────────────────────────┤
│ $ cargo test                                             │
│ 708 passed                                               │
├──────────────────────────────────────────────────────────┤
│ Problems 3 │ Tests 708 │ Git main                        │
└──────────────────────────────────────────────────────────┘
```

其中可能同时存在：

```text
TreeView
Editor
TerminalView
ProblemsList
StatusBar
```

全部在 **一个 Panel Application** 中。

---

# 9. 你说的 KDE 那种特效，也应该抽象，但不要直接做成 Panel API

目前 Bitty 的动画 RFC 已经实现了：

```text
Panel open
Panel close
Focus change
Workspace switch
```

并提供 `linear / ease_in / ease_out / ease_in_out / spring`，默认 duration 也有明确上界。

但目前它有意限制得很严：

> 只动画 Core-owned chrome。

同时明确：

> no plugin-defined shaders / native in-process effects

以及插件不能动画 Core-owned chrome。

我认为 **当前 v1 这样非常正确，不应该现在就解除限制**。

但未来可以引入：

```text
Panel Visual State
+
Transition
+
Effect
```

而不是不断加入：

```lua
panel:set_opacity()
panel:set_blur()
panel:set_wobble()
panel:set_scale()
panel:set_shadow()
...
```

未来概念上可以变成：

```lua
panel.style {
    opacity = {
        focused = 1.0,
        unfocused = 0.92,
        idle = 0.82,
    },

    transition = {
        focus = anim.spring(),
    },

    dragging = fx.wobble {
        stiffness = 0.7,
        damping = 0.2,
    },
}
```

内部：

```text
PanelState
├── focused
├── unfocused
├── idle
├── dragging
├── urgent
└── hidden

        ↓

Appearance Resolver
        ↓
VisualStyle
        ↓
Animation Engine
        ↓
Renderer
```

KDE 那种“像布一样拖动”的效果也不要 Lua 每帧计算 mesh。

应该是：

```lua
effect = "wobble"
```

Lua 只是配置参数。

真正的：

```text
mesh deformation
spring simulation
GPU transform
```

由 Rust/wgpu 完成。

---

# 10. Focus / Idle 也值得正式变成 Panel Visual State

这部分我很赞成。

现在大多数 terminal emulator 基本只有：

```text
focused / unfocused
```

Bitty 完全可以更进一步：

```text
Focused
Active
Inactive
Idle
Urgent
Dragging
Suspended
Hidden
```

注意其中有些是：

```text
Lifecycle State
```

有些是：

```text
Interaction State
```

有些是：

```text
Presentation State
```

不要混成一个 enum。

例如：

```text
Lifecycle:
Mounted / Suspended / Disposed

Focus:
Focused / Unfocused

Visibility:
Visible / Occluded / Hidden

Interaction:
Idle / Active / Dragging

Attention:
Normal / Urgent
```

然后 Theme / Effect resolver 根据状态组合决定效果。

这样以后甚至可以：

```lua
appearance.panel {
    ["focus=unfocused"] = {
        opacity = 0.92,
    },

    ["activity=idle"] = {
        opacity = 0.82,
    },

    ["attention=urgent"] = {
        border = theme.warning,
    },
}
```

这比硬编码 KDE-style effect 更具有 Bitty 自己的风格。

---

# 11. Docker / Mail / Discord / Telegram 这类插件会成为真正的“应用”

这里会出现一个很重要的变化：

现在我们通常叫：

```text
plugin
```

以后最好区分：

```text
Plugin
Service
Application
Widget
```

比如：

| 类型             | 示例                                |
| -------------- | --------------------------------- |
| Plugin         | git integration                   |
| Service        | LSP / notification / credential   |
| Widget         | clock / status / CPU graph        |
| Application    | bitter / Docker / Mail / Telegram |
| Panel Provider | 提供 Application surface            |

于是：

```text
bitty-terminal/mail
```

本质可能是：

```text
MailApplication
├── account service
├── mailbox model
├── message list widget
├── mail viewer
├── composer editor
└── commands
```

而不是简单：

```text
mail.lua
```

脚本。

这才会形成真正的 **Bitty App Ecosystem**。

---

# 12. 不过 UI 只是其中一半，另一半是 Application Services

一旦想做 Mail / Telegram / Docker，Native Widget 很快就不是最大的难题了。

真正需要的是：

```text
bitty.process
bitty.network
bitty.fs
bitty.store
bitty.secrets
bitty.tasks
bitty.notifications
bitty.clipboard
bitty.commands
bitty.events
bitty.services
```

当前 Plugin API 已经有 commands、events、settings、store、notifications、services、UI 等雏形，而且 capability model 是 deny-by-default。

以后一个 Telegram plugin 可以声明：

```toml
[capabilities]
network = ["api.telegram.org"]
store = true
notify = true
clipboard = true
```

而 Docker：

```toml
[capabilities]
process = ["docker"]
fs = [...]
```

这就比允许 Lua 任意：

```lua
os.execute(...)
```

强太多了。

所以以后 Bitty Native Application Runtime 的真正核心其实是：

```text
Native UI
+
Application Services
+
Capability Security
+
Lifecycle
```

四件事情。

---

# 13. `bitter` 是否会比 Neovim 这种 TUI 启动更快？

**有可能，但不要把“Native = 一定更快”作为设计承诺。**

理论上它确实省掉：

```text
process spawn
PTY allocation
shell/exec
ANSI serialization
VT parsing
terminal grid reconstruction
```

如果插件 VM 已经存在：

```text
command
 → create Activity
 → instantiate UI tree
 → render
```

可以非常快。

但如果：

```text
Lua 每帧创建几十万个 table
```

或者 editor document model 全部用 Lua 实现，那么一样可以很慢。

所以理想分工仍然是：

```text
Rust:
high-frequency + large-data + rendering primitives

Lua:
low-frequency + orchestration + application behavior
```

那么 `bitter` 的 native startup 确实很可能做到“几乎瞬间”。

---

# 14. 这会不会最后把 Bitty 做成 IDE？

会，但我反而建议 **不要把“IDE”设为最终定义**。

更好的定义是：

> **Terminal-first programmable workspace / application shell**

因为 IDE 只是它可以形成的一种配置。

比如程序员：

```text
Editor
Terminal
Git
AI
Tests
Problems
Docker
```

组合起来：

```text
Bitty → IDE
```

运维：

```text
SSH
Kubernetes
Docker
Logs
Metrics
Shell
```

组合起来：

```text
Bitty → DevOps Workspace
```

普通工作：

```text
Mail
Calendar
Telegram
Notes
Terminal
AI
```

组合起来：

```text
Bitty → Personal Workspace
```

AI 使用：

```text
Agent
Terminal
Diff
Task Board
Logs
Browser
```

组合起来：

```text
Bitty → Agent Workspace
```

所以：

> **Bitty 不需要成为 IDE。Bitty 应该成为“能够构造 IDE 的 Runtime”。**

这和 Emacs 强大的原因其实非常接近：Emacs 不是把 Git、Mail、IDE、Notes 全写进 Core，而是提供足够强且稳定的基本对象，让 Magit、Org、Dired 等应用自然长出来。

---

# 15. 我建议现在按这个顺序推进

这部分我会比较克制，因为现在 Panel Runtime RFC 本身仍明确写着：accepted contract **不等于已经实现的行为**，而首批到底实现 `terminal`，还是再加 `rich/helper/canvas`，目前仍属于 open question。

1. **先完成现有 Panel Runtime**：真正落地 `PanelId / lifecycle / modes / focus / suspend / resume`，不要现在就扩 UI 大系统。

2. **定义 `Activity/Surface` 层**：解决一个 Panel 怎样在 Terminal、Native App、Canvas 等内容之间切换，同时后台 TerminalSession 不死亡。

3. **把 Lua UI 从 Slot UI 扩展为 Widget Tree**：先做 `Text / Row / Column / List / Scroll / Button / Input`，然后再增加 `Tree / Table / Tabs / VirtualList`。

4. **实现 `TextEditor` primitive**：Rust 负责 buffer、cursor、selection、undo、IME、virtualization；然后用 Lua 写一个很小的 `bitter` reference plugin。这会是验证 Native UI Runtime 是否合理的最好项目。

5. **再做 Application Services**：`process / fs / network / tasks / secrets / clipboard / notifications`，严格沿用 capability sandbox。

6. **最后才开放高级 Panel effects**：先保留现在 bounded Core animation；等 Widget Runtime、renderer budget、plugin isolation 稳定以后，再设计 `VisualState + Transition + Effect`，而不是现在开放 raw shader。

---

## 最终我会把 Bitty 的对象模型定成这样

```text
Bitty Instance
│
├── Window
│   │
│   └── Workspace
│       │
│       └── Panel
│           │
│           ├── Presentation
│           │   ├── tiled
│           │   ├── floating
│           │   ├── fullscreen
│           │   ├── scratchpad
│           │   └── pinned
│           │
│           └── Activity
│               ├── Terminal
│               ├── Native App
│               ├── Rich
│               ├── Canvas
│               └── Helper
│
├── UI Runtime
│   ├── Widget Tree
│   ├── Layout
│   ├── Input
│   ├── Focus
│   ├── Text
│   └── Animation
│
├── Application Runtime
│   ├── Commands
│   ├── Events
│   ├── Tasks
│   ├── Services
│   └── Capabilities
│
└── Lua Runtime
    └── Plugins / Applications
```

其中最核心的一句话可以从现在的：

> **Panel is not Terminal**

继续发展成：

> **Panel is a host; Terminal is only one Activity.**

我觉得这句话非常适合成为下一阶段 Panel 架构的原则。它既不会破坏目前已经 accepted 的 Panel RFC，反而能解释你现在提出的 `bitter`、Docker、Mail、Telegram、AI、原生 UI、Shell 保活这些需求为什么应该属于同一套系统。这样发展下去，Bitty 的确会从“可编程终端”自然进入 **可编程 application shell / personal workspace**，而不是靠不断给 terminal emulator 塞功能实现。

[1]: https://github.com/bitty-terminal/bitty "GitHub - bitty-terminal/bitty: Programmable terminal runtime and core implementation · GitHub"
[2]: https://www.gnu.org/s/emacs/manual/html_node/elisp/Buffers.html?utm_source=chatgpt.com "Buffers (GNU Emacs Lisp Reference Manual)"
## 结论

我看了现在 Bitty 的仓库、最新的 **Panel Runtime RFC、Plugin API v1、Panel Animations RFC**。你的这个方向是成立的，而且实际上 **Bitty 目前的架构已经走到了这个方向的门口**。

现在最关键的一点是：

> **不要继续把 Panel 理解成“终端里的一个窗口”。Panel 应该成为 Bitty Native Application Runtime 的宿主。**

也就是说，未来：

```text
Bitty
├── Terminal Emulator
├── Window / Workspace Manager
├── Native UI Runtime
├── Lua Application Platform
├── Plugin Runtime
├── Automation Runtime
└── AI Workspace
```

而 `Panel` 正好是这些东西汇合的地方。

这会让 Bitty 和 Kitty / Ghostty / WezTerm 的差异越来越大。它更接近：

> **Emacs + Hyprland + Terminal + Native UI Toolkit**

只是底层是 Rust + wgpu，扩展层是 Lua。

---

# 1. 其实现在的 Panel 已经不是 Terminal 了

当前公开仓库仍然明确处于 pre-implementation / foundation 阶段，16-crate workspace 已经拆出了 `ui`、`render`、`lua`、`plugin-host`、`runtime` 等边界，但还没有承诺稳定的 public API 或最终 product behavior。([GitHub][1])

而 9 月 14 日 accepted 的 Panel Runtime RFC 已经明确写出了：

> `Panel` 是 **Generic workspace-managed application container**，不是 OS Window，也不是 PTY。

而且：

```text
PanelId != ViewId != TerminalId
```

`PanelRuntime` 负责 create / mount / suspend / resume / dispose；PTY、GPU object、OS window handle 都不能归 Panel 自己所有。

RFC 甚至已经明确：

```rust
enum PanelContent {
    Terminal(TerminalId),
    Rich(RichBlockId),
    Browser(BrowserSurfaceId),
    Helper(HelperHandle),
    Canvas(CanvasSurfaceId),
}
```

并说明：

> `Panel != Pty`

`TerminalPanel` 可以包装 Terminal，而 `CanvasPanel`、`FilePanel`、`GraphPanel`、`HelperProcessPanel` 都不要求 PTY。

所以你现在提出的 Docker、Mail、Discord、Telegram、编辑器，**不是推翻当前 Panel 架构，而是在继续完成它。**

---

# 2. 现在 Panel 已经非常像一个“小型 Window Manager”

你以前借鉴 Hyprland 的方向实际上已经进入 RFC。

当前 accepted Panel Runtime 已经有：

| Mode         | 语义                                       |
| ------------ | ---------------------------------------- |
| `tiled`      | 普通 Workspace tile                        |
| `floating`   | help/settings/AI/pet/tool                |
| `overlay`    | palette / flash / which-key / completion |
| `fullscreen` | 当前 Workspace 最大化                         |
| `scratchpad` | 隐藏并快速召回                                  |
| `pinned`     | 固定在 Workspace 边缘                         |
| `popover`    | 依附某个 UI element                          |

而且 **Mode 是 Panel 的 runtime property，不是 Panel 类型**。

因此一个 `PanelId`：

```text
tiled
  ↓
floating
  ↓
fullscreen
  ↓
tiled
```

身份、生命周期、输入、surface 都可以保持不变。

这个决定非常重要，因为以后：

```text
bitter
Docker
Git
Mail
Bitty AI
Telegram
File Manager
System Monitor
Markdown Viewer
```

全部可以使用完全相同的 Workspace 操作。

例如：

```text
Mod + F       fullscreen
Mod + Space   tiled ↔ floating
Mod + S       scratchpad
Mod + H/J/K/L focus
Mod + Shift + H/J/K/L move
```

插件根本不用重新发明窗口管理。

---

# 3. 但我建议再加一层：Panel ≠ Activity

这是我认为你现在最应该补上的抽象。

不要设计成：

```lua
panel:set_mode("shell")
panel:set_mode("docker")
panel:set_mode("editor")
```

因为 `shell` / `docker` / `editor` 不是 Panel Mode。

`floating` / `tiled` 才是 Mode。

更好的模型是：

```text
Workspace
    │
    └── Panel
         │
         ├── Presentation Mode
         │    ├── tiled
         │    ├── floating
         │    ├── fullscreen
         │    └── scratchpad
         │
         └── Activity
              ├── TerminalActivity
              ├── LuaAppActivity
              ├── RichActivity
              ├── CanvasActivity
              └── HelperActivity
```

甚至进一步：

```text
Panel
┌──────────────────────────────────┐
│ Activity Stack                   │
│                                  │
│ ┌──────────────────────────────┐ │
│ │ Docker Native UI             │ │ ← active
│ └──────────────────────────────┘ │
│                                  │
│ Terminal Activity               │ ← suspended but alive
└──────────────────────────────────┘
```

这就解决了你说的：

> 打开一个 Docker 插件 → 占据 Panel → 退出 → 回到原来的 Shell，而且 Shell 进程和历史还在。

可以做成类似：

```lua
panel:push(docker_activity)
panel:pop()
```

而不是：

```text
kill shell
→ start app
→ kill app
→ start new shell
```

尤其当前 Bitty 已经把 `TerminalRegistry` 作为 PTY 生命周期真正 owner，Panel 本来就不直接拥有 PTY fd。

因此非常适合：

```text
TerminalSession ─────────────── alive
       │
       │ hidden
       ▼
Panel → DockerActivity
       │
       │ pop()
       ▼
Panel → TerminalActivity
```

PTY 根本没死。

---

# 4. 这个思想其实比传统 TUI 强很多

传统 TUI 的结构是：

```text
Terminal Emulator
    ↓ PTY
Application Process
    ↓ ANSI / VT escape sequences
Cell Grid
    ↓
Renderer
```

例如：

```text
nvim
btop
lazygit
opencode
nmtui
```

本质上都只能告诉终端：

> 第 20 行第 37 列画一个字符。

哪怕 Ratatui / Bubble Tea / Textual 做得再复杂，最后仍然压成 terminal cell grid。

而 Bitty Native UI 可以绕开这一层：

```text
Lua Plugin
    ↓
Bitty UI Tree
    ↓
Layout Engine
    ↓
Text / Widget / Canvas / Image
    ↓
wgpu
```

即：

```text
bitter.lua
    ↓
Bitty Native UI
    ↓
wgpu
```

而不是：

```text
bitter process
    ↓
PTY
    ↓
ANSI
    ↓
VT parser
    ↓
cell grid
    ↓
wgpu
```

这就是你说的“Native UI”。

这个类比基本成立：

| Desktop              | Bitty                        |
| -------------------- | ---------------------------- |
| WinUI / WPF          | Bitty Native UI              |
| AppKit / SwiftUI     | Bitty Native UI              |
| Electron             | WebView-based Panel          |
| GTK / Qt             | 外部 GUI Toolkit               |
| Ratatui / Bubble Tea | TUI over PTY                 |
| ANSI/VT              | Terminal compatibility layer |

但需要强调：

**它是 Bitty-native，不是 OS-native。**

这点非常像 Emacs：Emacs 自己提供 Lisp application environment，并不要求每个应用都变成另一个 OS application。

---

# 5. 所以 `bitter` 完全可以存在

比如：

```text
bitty-terminal/bitter
```

它甚至可以是一个很好的 **first-party reference plugin**。

启动：

```bash
bitty plugin add bitty-terminal/bitter
```

然后：

```text
:e README.md
```

或者：

```lua
bitty.commands.register {
    id = "edit",
    run = function(args)
        bitter.open(args.file)
    end
}
```

Panel 直接切换：

```text
┌─────────────────────────────────────────┐
│ README.md                         bitter │
├─────────────────────────────────────────┤
│                                         │
│ # Bitty                                 │
│                                         │
│ Programmable terminal runtime...        │
│                                         │
│                                         │
├─────────────────────────────────────────┤
│ NORMAL  README.md                 34:12 │
└─────────────────────────────────────────┘
```

退出：

```text
:q
```

不是：

```text
destroy Panel
```

而只是：

```text
panel.pop_activity()
```

然后原来的：

```console
~/Projects/bitty $
```

重新显示。

---

# 6. 但不要让 Lua 自己实现 Text Editor Engine

这是一个非常重要的边界。

`bitter` 可以是 Lua plugin。

但是这些东西不应该用 Lua 重写：

| Rust Core primitive        | Lua application |
| -------------------------- | --------------- |
| Text Buffer / Rope         | Vim-like mode   |
| Cursor                     | Normal / Insert |
| Selection                  | Visual mode     |
| IME                        | keymap          |
| grapheme segmentation      | commands        |
| Unicode shaping            | editor behavior |
| undo/redo engine           | workflows       |
| viewport                   | UI composition  |
| virtualized text rendering | plugins         |
| clipboard primitive        | user commands   |

也就是说：

```text
Rust
 └── TextEditorPrimitive

Lua
 └── bitter
      ├── modes
      ├── keymaps
      ├── commands
      ├── LSP integration
      ├── syntax configuration
      └── UI
```

这样以后不是只有 `bitter` 能用。

Mail composer：

```text
Mail plugin
    ↓
TextEditor
```

Git commit：

```text
Git plugin
    ↓
TextEditor
```

AI prompt editor：

```text
bitty-ai
    ↓
TextEditor
```

Configuration editor：

```text
Settings
    ↓
TextEditor
```

全部复用。

这其实正是 Emacs 最值得 Bitty 学的地方。Emacs 把 `buffer` 作为独立的 editable object，一个 buffer 可以不被任何 window 显示，也可以显示在多个 window 中；window 自身只负责展示，并且维护自己的 display state/history。([gnu.org][2])

Bitty 最好也保持：

```text
Document ≠ View ≠ Panel
```

---

# 7. 我甚至会建议 Bitty 建立一套正式的 Native Widget Layer

当前 Plugin API v1 的 UI 其实非常保守。

现在 accepted 的：

```lua
bitty.ui.mount(slot, component)
bitty.ui.update(handle, component)
```

只能挂载到：

```text
terminal
top
bottom
left
right
tabline
statusline
overlay
```

而 component 当前只开放：

```text
Text
Row
Column
List
```

并明确禁止 global coordinate、shader、pipeline、renderer handle 等底层能力。

这对于 v1 是正确的。

但未来可以从：

```text
Lua UI v1
↓
Declarative Slot UI
```

进化到：

```text
Lua UI v2 / Native Panel UI
↓
Widget Tree
```

比如：

```text
Text
RichText
TextInput
Editor
Button
Toggle
Slider
List
VirtualList
Tree
Table
Tabs
ScrollView
Canvas
Image
Terminal
Split
Stack
Overlay
Popover
```

这里我强烈建议采用 **Retained/Declarative UI**，而不是让 Lua 每帧：

```lua
function draw()
    draw_rect(...)
    draw_text(...)
end
```

否则 Lua 很容易进入 rendering hot path。

更合适的是：

```lua
return ui.column {
    ui.toolbar {
        ui.button {
            text = "Refresh",
            on_click = refresh,
        }
    },

    ui.virtual_list {
        items = containers,
        render = render_container,
    }
}
```

然后：

```text
Lua
 ↓ declarative tree
Rust UI Runtime
 ↓ diff
Layout
 ↓
Hit Test
 ↓
Text shaping
 ↓
wgpu
```

Lua 负责 **state/policy/application logic**。

Rust 负责 **hot path/mechanism/rendering**。

这和 Bitty 当前“hot path 不经过 Lua”的安全和性能原则也是一致的；Panel Runtime RFC 当前明确要求输入链路不能阻塞于 Panel provider 或 Lua。

---

# 8. Terminal 本身最后也可以只是 Native Widget 的一种

这是进一步非常有意思的一步。

未来甚至可以：

```lua
ui.column {
    ui.text { "Logs" },

    ui.terminal {
        session = build_session,
    },

    ui.statusbar {
        ...
    }
}
```

也就是说：

```text
Terminal
```

不再一定等同于：

```text
整个 Panel
```

而可能只是：

```text
Panel
 └── Native UI
      ├── Toolbar
      ├── TerminalView
      ├── Problems
      └── StatusBar
```

这一下就真正开始接近 IDE 了。

比如一个 Rust Development Panel：

```text
┌──────────────────────────────────────────────────────────┐
│ Rust Project · bitty                    Build │ Test │ ▶ │
├──────────────────────────────────────────────────────────┤
│ src/                │ main.rs                            │
│ ├─ app.rs           │                                    │
│ ├─ runtime.rs       │ fn main() {                        │
│ └─ panel.rs         │     ...                            │
│                     │ }                                  │
├─────────────────────┴────────────────────────────────────┤
│ $ cargo test                                             │
│ 708 passed                                               │
├──────────────────────────────────────────────────────────┤
│ Problems 3 │ Tests 708 │ Git main                        │
└──────────────────────────────────────────────────────────┘
```

其中可能同时存在：

```text
TreeView
Editor
TerminalView
ProblemsList
StatusBar
```

全部在 **一个 Panel Application** 中。

---

# 9. 你说的 KDE 那种特效，也应该抽象，但不要直接做成 Panel API

目前 Bitty 的动画 RFC 已经实现了：

```text
Panel open
Panel close
Focus change
Workspace switch
```

并提供 `linear / ease_in / ease_out / ease_in_out / spring`，默认 duration 也有明确上界。

但目前它有意限制得很严：

> 只动画 Core-owned chrome。

同时明确：

> no plugin-defined shaders / native in-process effects

以及插件不能动画 Core-owned chrome。

我认为 **当前 v1 这样非常正确，不应该现在就解除限制**。

但未来可以引入：

```text
Panel Visual State
+
Transition
+
Effect
```

而不是不断加入：

```lua
panel:set_opacity()
panel:set_blur()
panel:set_wobble()
panel:set_scale()
panel:set_shadow()
...
```

未来概念上可以变成：

```lua
panel.style {
    opacity = {
        focused = 1.0,
        unfocused = 0.92,
        idle = 0.82,
    },

    transition = {
        focus = anim.spring(),
    },

    dragging = fx.wobble {
        stiffness = 0.7,
        damping = 0.2,
    },
}
```

内部：

```text
PanelState
├── focused
├── unfocused
├── idle
├── dragging
├── urgent
└── hidden

        ↓

Appearance Resolver
        ↓
VisualStyle
        ↓
Animation Engine
        ↓
Renderer
```

KDE 那种“像布一样拖动”的效果也不要 Lua 每帧计算 mesh。

应该是：

```lua
effect = "wobble"
```

Lua 只是配置参数。

真正的：

```text
mesh deformation
spring simulation
GPU transform
```

由 Rust/wgpu 完成。

---

# 10. Focus / Idle 也值得正式变成 Panel Visual State

这部分我很赞成。

现在大多数 terminal emulator 基本只有：

```text
focused / unfocused
```

Bitty 完全可以更进一步：

```text
Focused
Active
Inactive
Idle
Urgent
Dragging
Suspended
Hidden
```

注意其中有些是：

```text
Lifecycle State
```

有些是：

```text
Interaction State
```

有些是：

```text
Presentation State
```

不要混成一个 enum。

例如：

```text
Lifecycle:
Mounted / Suspended / Disposed

Focus:
Focused / Unfocused

Visibility:
Visible / Occluded / Hidden

Interaction:
Idle / Active / Dragging

Attention:
Normal / Urgent
```

然后 Theme / Effect resolver 根据状态组合决定效果。

这样以后甚至可以：

```lua
appearance.panel {
    ["focus=unfocused"] = {
        opacity = 0.92,
    },

    ["activity=idle"] = {
        opacity = 0.82,
    },

    ["attention=urgent"] = {
        border = theme.warning,
    },
}
```

这比硬编码 KDE-style effect 更具有 Bitty 自己的风格。

---

# 11. Docker / Mail / Discord / Telegram 这类插件会成为真正的“应用”

这里会出现一个很重要的变化：

现在我们通常叫：

```text
plugin
```

以后最好区分：

```text
Plugin
Service
Application
Widget
```

比如：

| 类型             | 示例                                |
| -------------- | --------------------------------- |
| Plugin         | git integration                   |
| Service        | LSP / notification / credential   |
| Widget         | clock / status / CPU graph        |
| Application    | bitter / Docker / Mail / Telegram |
| Panel Provider | 提供 Application surface            |

于是：

```text
bitty-terminal/mail
```

本质可能是：

```text
MailApplication
├── account service
├── mailbox model
├── message list widget
├── mail viewer
├── composer editor
└── commands
```

而不是简单：

```text
mail.lua
```

脚本。

这才会形成真正的 **Bitty App Ecosystem**。

---

# 12. 不过 UI 只是其中一半，另一半是 Application Services

一旦想做 Mail / Telegram / Docker，Native Widget 很快就不是最大的难题了。

真正需要的是：

```text
bitty.process
bitty.network
bitty.fs
bitty.store
bitty.secrets
bitty.tasks
bitty.notifications
bitty.clipboard
bitty.commands
bitty.events
bitty.services
```

当前 Plugin API 已经有 commands、events、settings、store、notifications、services、UI 等雏形，而且 capability model 是 deny-by-default。

以后一个 Telegram plugin 可以声明：

```toml
[capabilities]
network = ["api.telegram.org"]
store = true
notify = true
clipboard = true
```

而 Docker：

```toml
[capabilities]
process = ["docker"]
fs = [...]
```

这就比允许 Lua 任意：

```lua
os.execute(...)
```

强太多了。

所以以后 Bitty Native Application Runtime 的真正核心其实是：

```text
Native UI
+
Application Services
+
Capability Security
+
Lifecycle
```

四件事情。

---

# 13. `bitter` 是否会比 Neovim 这种 TUI 启动更快？

**有可能，但不要把“Native = 一定更快”作为设计承诺。**

理论上它确实省掉：

```text
process spawn
PTY allocation
shell/exec
ANSI serialization
VT parsing
terminal grid reconstruction
```

如果插件 VM 已经存在：

```text
command
 → create Activity
 → instantiate UI tree
 → render
```

可以非常快。

但如果：

```text
Lua 每帧创建几十万个 table
```

或者 editor document model 全部用 Lua 实现，那么一样可以很慢。

所以理想分工仍然是：

```text
Rust:
high-frequency + large-data + rendering primitives

Lua:
low-frequency + orchestration + application behavior
```

那么 `bitter` 的 native startup 确实很可能做到“几乎瞬间”。

---

# 14. 这会不会最后把 Bitty 做成 IDE？

会，但我反而建议 **不要把“IDE”设为最终定义**。

更好的定义是：

> **Terminal-first programmable workspace / application shell**

因为 IDE 只是它可以形成的一种配置。

比如程序员：

```text
Editor
Terminal
Git
AI
Tests
Problems
Docker
```

组合起来：

```text
Bitty → IDE
```

运维：

```text
SSH
Kubernetes
Docker
Logs
Metrics
Shell
```

组合起来：

```text
Bitty → DevOps Workspace
```

普通工作：

```text
Mail
Calendar
Telegram
Notes
Terminal
AI
```

组合起来：

```text
Bitty → Personal Workspace
```

AI 使用：

```text
Agent
Terminal
Diff
Task Board
Logs
Browser
```

组合起来：

```text
Bitty → Agent Workspace
```

所以：

> **Bitty 不需要成为 IDE。Bitty 应该成为“能够构造 IDE 的 Runtime”。**

这和 Emacs 强大的原因其实非常接近：Emacs 不是把 Git、Mail、IDE、Notes 全写进 Core，而是提供足够强且稳定的基本对象，让 Magit、Org、Dired 等应用自然长出来。

---

# 15. 我建议现在按这个顺序推进

这部分我会比较克制，因为现在 Panel Runtime RFC 本身仍明确写着：accepted contract **不等于已经实现的行为**，而首批到底实现 `terminal`，还是再加 `rich/helper/canvas`，目前仍属于 open question。

1. **先完成现有 Panel Runtime**：真正落地 `PanelId / lifecycle / modes / focus / suspend / resume`，不要现在就扩 UI 大系统。

2. **定义 `Activity/Surface` 层**：解决一个 Panel 怎样在 Terminal、Native App、Canvas 等内容之间切换，同时后台 TerminalSession 不死亡。

3. **把 Lua UI 从 Slot UI 扩展为 Widget Tree**：先做 `Text / Row / Column / List / Scroll / Button / Input`，然后再增加 `Tree / Table / Tabs / VirtualList`。

4. **实现 `TextEditor` primitive**：Rust 负责 buffer、cursor、selection、undo、IME、virtualization；然后用 Lua 写一个很小的 `bitter` reference plugin。这会是验证 Native UI Runtime 是否合理的最好项目。

5. **再做 Application Services**：`process / fs / network / tasks / secrets / clipboard / notifications`，严格沿用 capability sandbox。

6. **最后才开放高级 Panel effects**：先保留现在 bounded Core animation；等 Widget Runtime、renderer budget、plugin isolation 稳定以后，再设计 `VisualState + Transition + Effect`，而不是现在开放 raw shader。

---

## 最终我会把 Bitty 的对象模型定成这样

```text
Bitty Instance
│
├── Window
│   │
│   └── Workspace
│       │
│       └── Panel
│           │
│           ├── Presentation
│           │   ├── tiled
│           │   ├── floating
│           │   ├── fullscreen
│           │   ├── scratchpad
│           │   └── pinned
│           │
│           └── Activity
│               ├── Terminal
│               ├── Native App
│               ├── Rich
│               ├── Canvas
│               └── Helper
│
├── UI Runtime
│   ├── Widget Tree
│   ├── Layout
│   ├── Input
│   ├── Focus
│   ├── Text
│   └── Animation
│
├── Application Runtime
│   ├── Commands
│   ├── Events
│   ├── Tasks
│   ├── Services
│   └── Capabilities
│
└── Lua Runtime
    └── Plugins / Applications
```

其中最核心的一句话可以从现在的：

> **Panel is not Terminal**

继续发展成：

> **Panel is a host; Terminal is only one Activity.**

我觉得这句话非常适合成为下一阶段 Panel 架构的原则。它既不会破坏目前已经 accepted 的 Panel RFC，反而能解释你现在提出的 `bitter`、Docker、Mail、Telegram、AI、原生 UI、Shell 保活这些需求为什么应该属于同一套系统。这样发展下去，Bitty 的确会从“可编程终端”自然进入 **可编程 application shell / personal workspace**，而不是靠不断给 terminal emulator 塞功能实现。

[1]: https://github.com/bitty-terminal/bitty "GitHub - bitty-terminal/bitty: Programmable terminal runtime and core implementation · GitHub"
[2]: https://www.gnu.org/s/emacs/manual/html_node/elisp/Buffers.html?utm_source=chatgpt.com "Buffers (GNU Emacs Lisp Reference Manual)"
## 结论

我看了现在 Bitty 的仓库、最新的 **Panel Runtime RFC、Plugin API v1、Panel Animations RFC**。你的这个方向是成立的，而且实际上 **Bitty 目前的架构已经走到了这个方向的门口**。

现在最关键的一点是：

> **不要继续把 Panel 理解成“终端里的一个窗口”。Panel 应该成为 Bitty Native Application Runtime 的宿主。**

也就是说，未来：

```text
Bitty
├── Terminal Emulator
├── Window / Workspace Manager
├── Native UI Runtime
├── Lua Application Platform
├── Plugin Runtime
├── Automation Runtime
└── AI Workspace
```

而 `Panel` 正好是这些东西汇合的地方。

这会让 Bitty 和 Kitty / Ghostty / WezTerm 的差异越来越大。它更接近：

> **Emacs + Hyprland + Terminal + Native UI Toolkit**

只是底层是 Rust + wgpu，扩展层是 Lua。

---

# 1. 其实现在的 Panel 已经不是 Terminal 了

当前公开仓库仍然明确处于 pre-implementation / foundation 阶段，16-crate workspace 已经拆出了 `ui`、`render`、`lua`、`plugin-host`、`runtime` 等边界，但还没有承诺稳定的 public API 或最终 product behavior。([GitHub][1])

而 9 月 14 日 accepted 的 Panel Runtime RFC 已经明确写出了：

> `Panel` 是 **Generic workspace-managed application container**，不是 OS Window，也不是 PTY。

而且：

```text
PanelId != ViewId != TerminalId
```

`PanelRuntime` 负责 create / mount / suspend / resume / dispose；PTY、GPU object、OS window handle 都不能归 Panel 自己所有。

RFC 甚至已经明确：

```rust
enum PanelContent {
    Terminal(TerminalId),
    Rich(RichBlockId),
    Browser(BrowserSurfaceId),
    Helper(HelperHandle),
    Canvas(CanvasSurfaceId),
}
```

并说明：

> `Panel != Pty`

`TerminalPanel` 可以包装 Terminal，而 `CanvasPanel`、`FilePanel`、`GraphPanel`、`HelperProcessPanel` 都不要求 PTY。

所以你现在提出的 Docker、Mail、Discord、Telegram、编辑器，**不是推翻当前 Panel 架构，而是在继续完成它。**

---

# 2. 现在 Panel 已经非常像一个“小型 Window Manager”

你以前借鉴 Hyprland 的方向实际上已经进入 RFC。

当前 accepted Panel Runtime 已经有：

| Mode         | 语义                                       |
| ------------ | ---------------------------------------- |
| `tiled`      | 普通 Workspace tile                        |
| `floating`   | help/settings/AI/pet/tool                |
| `overlay`    | palette / flash / which-key / completion |
| `fullscreen` | 当前 Workspace 最大化                         |
| `scratchpad` | 隐藏并快速召回                                  |
| `pinned`     | 固定在 Workspace 边缘                         |
| `popover`    | 依附某个 UI element                          |

而且 **Mode 是 Panel 的 runtime property，不是 Panel 类型**。

因此一个 `PanelId`：

```text
tiled
  ↓
floating
  ↓
fullscreen
  ↓
tiled
```

身份、生命周期、输入、surface 都可以保持不变。

这个决定非常重要，因为以后：

```text
bitter
Docker
Git
Mail
Bitty AI
Telegram
File Manager
System Monitor
Markdown Viewer
```

全部可以使用完全相同的 Workspace 操作。

例如：

```text
Mod + F       fullscreen
Mod + Space   tiled ↔ floating
Mod + S       scratchpad
Mod + H/J/K/L focus
Mod + Shift + H/J/K/L move
```

插件根本不用重新发明窗口管理。

---

# 3. 但我建议再加一层：Panel ≠ Activity

这是我认为你现在最应该补上的抽象。

不要设计成：

```lua
panel:set_mode("shell")
panel:set_mode("docker")
panel:set_mode("editor")
```

因为 `shell` / `docker` / `editor` 不是 Panel Mode。

`floating` / `tiled` 才是 Mode。

更好的模型是：

```text
Workspace
    │
    └── Panel
         │
         ├── Presentation Mode
         │    ├── tiled
         │    ├── floating
         │    ├── fullscreen
         │    └── scratchpad
         │
         └── Activity
              ├── TerminalActivity
              ├── LuaAppActivity
              ├── RichActivity
              ├── CanvasActivity
              └── HelperActivity
```

甚至进一步：

```text
Panel
┌──────────────────────────────────┐
│ Activity Stack                   │
│                                  │
│ ┌──────────────────────────────┐ │
│ │ Docker Native UI             │ │ ← active
│ └──────────────────────────────┘ │
│                                  │
│ Terminal Activity               │ ← suspended but alive
└──────────────────────────────────┘
```

这就解决了你说的：

> 打开一个 Docker 插件 → 占据 Panel → 退出 → 回到原来的 Shell，而且 Shell 进程和历史还在。

可以做成类似：

```lua
panel:push(docker_activity)
panel:pop()
```

而不是：

```text
kill shell
→ start app
→ kill app
→ start new shell
```

尤其当前 Bitty 已经把 `TerminalRegistry` 作为 PTY 生命周期真正 owner，Panel 本来就不直接拥有 PTY fd。

因此非常适合：

```text
TerminalSession ─────────────── alive
       │
       │ hidden
       ▼
Panel → DockerActivity
       │
       │ pop()
       ▼
Panel → TerminalActivity
```

PTY 根本没死。

---

# 4. 这个思想其实比传统 TUI 强很多

传统 TUI 的结构是：

```text
Terminal Emulator
    ↓ PTY
Application Process
    ↓ ANSI / VT escape sequences
Cell Grid
    ↓
Renderer
```

例如：

```text
nvim
btop
lazygit
opencode
nmtui
```

本质上都只能告诉终端：

> 第 20 行第 37 列画一个字符。

哪怕 Ratatui / Bubble Tea / Textual 做得再复杂，最后仍然压成 terminal cell grid。

而 Bitty Native UI 可以绕开这一层：

```text
Lua Plugin
    ↓
Bitty UI Tree
    ↓
Layout Engine
    ↓
Text / Widget / Canvas / Image
    ↓
wgpu
```

即：

```text
bitter.lua
    ↓
Bitty Native UI
    ↓
wgpu
```

而不是：

```text
bitter process
    ↓
PTY
    ↓
ANSI
    ↓
VT parser
    ↓
cell grid
    ↓
wgpu
```

这就是你说的“Native UI”。

这个类比基本成立：

| Desktop              | Bitty                        |
| -------------------- | ---------------------------- |
| WinUI / WPF          | Bitty Native UI              |
| AppKit / SwiftUI     | Bitty Native UI              |
| Electron             | WebView-based Panel          |
| GTK / Qt             | 外部 GUI Toolkit               |
| Ratatui / Bubble Tea | TUI over PTY                 |
| ANSI/VT              | Terminal compatibility layer |

但需要强调：

**它是 Bitty-native，不是 OS-native。**

这点非常像 Emacs：Emacs 自己提供 Lisp application environment，并不要求每个应用都变成另一个 OS application。

---

# 5. 所以 `bitter` 完全可以存在

比如：

```text
bitty-terminal/bitter
```

它甚至可以是一个很好的 **first-party reference plugin**。

启动：

```bash
bitty plugin add bitty-terminal/bitter
```

然后：

```text
:e README.md
```

或者：

```lua
bitty.commands.register {
    id = "edit",
    run = function(args)
        bitter.open(args.file)
    end
}
```

Panel 直接切换：

```text
┌─────────────────────────────────────────┐
│ README.md                         bitter │
├─────────────────────────────────────────┤
│                                         │
│ # Bitty                                 │
│                                         │
│ Programmable terminal runtime...        │
│                                         │
│                                         │
├─────────────────────────────────────────┤
│ NORMAL  README.md                 34:12 │
└─────────────────────────────────────────┘
```

退出：

```text
:q
```

不是：

```text
destroy Panel
```

而只是：

```text
panel.pop_activity()
```

然后原来的：

```console
~/Projects/bitty $
```

重新显示。

---

# 6. 但不要让 Lua 自己实现 Text Editor Engine

这是一个非常重要的边界。

`bitter` 可以是 Lua plugin。

但是这些东西不应该用 Lua 重写：

| Rust Core primitive        | Lua application |
| -------------------------- | --------------- |
| Text Buffer / Rope         | Vim-like mode   |
| Cursor                     | Normal / Insert |
| Selection                  | Visual mode     |
| IME                        | keymap          |
| grapheme segmentation      | commands        |
| Unicode shaping            | editor behavior |
| undo/redo engine           | workflows       |
| viewport                   | UI composition  |
| virtualized text rendering | plugins         |
| clipboard primitive        | user commands   |

也就是说：

```text
Rust
 └── TextEditorPrimitive

Lua
 └── bitter
      ├── modes
      ├── keymaps
      ├── commands
      ├── LSP integration
      ├── syntax configuration
      └── UI
```

这样以后不是只有 `bitter` 能用。

Mail composer：

```text
Mail plugin
    ↓
TextEditor
```

Git commit：

```text
Git plugin
    ↓
TextEditor
```

AI prompt editor：

```text
bitty-ai
    ↓
TextEditor
```

Configuration editor：

```text
Settings
    ↓
TextEditor
```

全部复用。

这其实正是 Emacs 最值得 Bitty 学的地方。Emacs 把 `buffer` 作为独立的 editable object，一个 buffer 可以不被任何 window 显示，也可以显示在多个 window 中；window 自身只负责展示，并且维护自己的 display state/history。([gnu.org][2])

Bitty 最好也保持：

```text
Document ≠ View ≠ Panel
```

---

# 7. 我甚至会建议 Bitty 建立一套正式的 Native Widget Layer

当前 Plugin API v1 的 UI 其实非常保守。

现在 accepted 的：

```lua
bitty.ui.mount(slot, component)
bitty.ui.update(handle, component)
```

只能挂载到：

```text
terminal
top
bottom
left
right
tabline
statusline
overlay
```

而 component 当前只开放：

```text
Text
Row
Column
List
```

并明确禁止 global coordinate、shader、pipeline、renderer handle 等底层能力。

这对于 v1 是正确的。

但未来可以从：

```text
Lua UI v1
↓
Declarative Slot UI
```

进化到：

```text
Lua UI v2 / Native Panel UI
↓
Widget Tree
```

比如：

```text
Text
RichText
TextInput
Editor
Button
Toggle
Slider
List
VirtualList
Tree
Table
Tabs
ScrollView
Canvas
Image
Terminal
Split
Stack
Overlay
Popover
```

这里我强烈建议采用 **Retained/Declarative UI**，而不是让 Lua 每帧：

```lua
function draw()
    draw_rect(...)
    draw_text(...)
end
```

否则 Lua 很容易进入 rendering hot path。

更合适的是：

```lua
return ui.column {
    ui.toolbar {
        ui.button {
            text = "Refresh",
            on_click = refresh,
        }
    },

    ui.virtual_list {
        items = containers,
        render = render_container,
    }
}
```

然后：

```text
Lua
 ↓ declarative tree
Rust UI Runtime
 ↓ diff
Layout
 ↓
Hit Test
 ↓
Text shaping
 ↓
wgpu
```

Lua 负责 **state/policy/application logic**。

Rust 负责 **hot path/mechanism/rendering**。

这和 Bitty 当前“hot path 不经过 Lua”的安全和性能原则也是一致的；Panel Runtime RFC 当前明确要求输入链路不能阻塞于 Panel provider 或 Lua。

---

# 8. Terminal 本身最后也可以只是 Native Widget 的一种

这是进一步非常有意思的一步。

未来甚至可以：

```lua
ui.column {
    ui.text { "Logs" },

    ui.terminal {
        session = build_session,
    },

    ui.statusbar {
        ...
    }
}
```

也就是说：

```text
Terminal
```

不再一定等同于：

```text
整个 Panel
```

而可能只是：

```text
Panel
 └── Native UI
      ├── Toolbar
      ├── TerminalView
      ├── Problems
      └── StatusBar
```

这一下就真正开始接近 IDE 了。

比如一个 Rust Development Panel：

```text
┌──────────────────────────────────────────────────────────┐
│ Rust Project · bitty                    Build │ Test │ ▶ │
├──────────────────────────────────────────────────────────┤
│ src/                │ main.rs                            │
│ ├─ app.rs           │                                    │
│ ├─ runtime.rs       │ fn main() {                        │
│ └─ panel.rs         │     ...                            │
│                     │ }                                  │
├─────────────────────┴────────────────────────────────────┤
│ $ cargo test                                             │
│ 708 passed                                               │
├──────────────────────────────────────────────────────────┤
│ Problems 3 │ Tests 708 │ Git main                        │
└──────────────────────────────────────────────────────────┘
```

其中可能同时存在：

```text
TreeView
Editor
TerminalView
ProblemsList
StatusBar
```

全部在 **一个 Panel Application** 中。

---

# 9. 你说的 KDE 那种特效，也应该抽象，但不要直接做成 Panel API

目前 Bitty 的动画 RFC 已经实现了：

```text
Panel open
Panel close
Focus change
Workspace switch
```

并提供 `linear / ease_in / ease_out / ease_in_out / spring`，默认 duration 也有明确上界。

但目前它有意限制得很严：

> 只动画 Core-owned chrome。

同时明确：

> no plugin-defined shaders / native in-process effects

以及插件不能动画 Core-owned chrome。

我认为 **当前 v1 这样非常正确，不应该现在就解除限制**。

但未来可以引入：

```text
Panel Visual State
+
Transition
+
Effect
```

而不是不断加入：

```lua
panel:set_opacity()
panel:set_blur()
panel:set_wobble()
panel:set_scale()
panel:set_shadow()
...
```

未来概念上可以变成：

```lua
panel.style {
    opacity = {
        focused = 1.0,
        unfocused = 0.92,
        idle = 0.82,
    },

    transition = {
        focus = anim.spring(),
    },

    dragging = fx.wobble {
        stiffness = 0.7,
        damping = 0.2,
    },
}
```

内部：

```text
PanelState
├── focused
├── unfocused
├── idle
├── dragging
├── urgent
└── hidden

        ↓

Appearance Resolver
        ↓
VisualStyle
        ↓
Animation Engine
        ↓
Renderer
```

KDE 那种“像布一样拖动”的效果也不要 Lua 每帧计算 mesh。

应该是：

```lua
effect = "wobble"
```

Lua 只是配置参数。

真正的：

```text
mesh deformation
spring simulation
GPU transform
```

由 Rust/wgpu 完成。

---

# 10. Focus / Idle 也值得正式变成 Panel Visual State

这部分我很赞成。

现在大多数 terminal emulator 基本只有：

```text
focused / unfocused
```

Bitty 完全可以更进一步：

```text
Focused
Active
Inactive
Idle
Urgent
Dragging
Suspended
Hidden
```

注意其中有些是：

```text
Lifecycle State
```

有些是：

```text
Interaction State
```

有些是：

```text
Presentation State
```

不要混成一个 enum。

例如：

```text
Lifecycle:
Mounted / Suspended / Disposed

Focus:
Focused / Unfocused

Visibility:
Visible / Occluded / Hidden

Interaction:
Idle / Active / Dragging

Attention:
Normal / Urgent
```

然后 Theme / Effect resolver 根据状态组合决定效果。

这样以后甚至可以：

```lua
appearance.panel {
    ["focus=unfocused"] = {
        opacity = 0.92,
    },

    ["activity=idle"] = {
        opacity = 0.82,
    },

    ["attention=urgent"] = {
        border = theme.warning,
    },
}
```

这比硬编码 KDE-style effect 更具有 Bitty 自己的风格。

---

# 11. Docker / Mail / Discord / Telegram 这类插件会成为真正的“应用”

这里会出现一个很重要的变化：

现在我们通常叫：

```text
plugin
```

以后最好区分：

```text
Plugin
Service
Application
Widget
```

比如：

| 类型             | 示例                                |
| -------------- | --------------------------------- |
| Plugin         | git integration                   |
| Service        | LSP / notification / credential   |
| Widget         | clock / status / CPU graph        |
| Application    | bitter / Docker / Mail / Telegram |
| Panel Provider | 提供 Application surface            |

于是：

```text
bitty-terminal/mail
```

本质可能是：

```text
MailApplication
├── account service
├── mailbox model
├── message list widget
├── mail viewer
├── composer editor
└── commands
```

而不是简单：

```text
mail.lua
```

脚本。

这才会形成真正的 **Bitty App Ecosystem**。

---

# 12. 不过 UI 只是其中一半，另一半是 Application Services

一旦想做 Mail / Telegram / Docker，Native Widget 很快就不是最大的难题了。

真正需要的是：

```text
bitty.process
bitty.network
bitty.fs
bitty.store
bitty.secrets
bitty.tasks
bitty.notifications
bitty.clipboard
bitty.commands
bitty.events
bitty.services
```

当前 Plugin API 已经有 commands、events、settings、store、notifications、services、UI 等雏形，而且 capability model 是 deny-by-default。

以后一个 Telegram plugin 可以声明：

```toml
[capabilities]
network = ["api.telegram.org"]
store = true
notify = true
clipboard = true
```

而 Docker：

```toml
[capabilities]
process = ["docker"]
fs = [...]
```

这就比允许 Lua 任意：

```lua
os.execute(...)
```

强太多了。

所以以后 Bitty Native Application Runtime 的真正核心其实是：

```text
Native UI
+
Application Services
+
Capability Security
+
Lifecycle
```

四件事情。

---

# 13. `bitter` 是否会比 Neovim 这种 TUI 启动更快？

**有可能，但不要把“Native = 一定更快”作为设计承诺。**

理论上它确实省掉：

```text
process spawn
PTY allocation
shell/exec
ANSI serialization
VT parsing
terminal grid reconstruction
```

如果插件 VM 已经存在：

```text
command
 → create Activity
 → instantiate UI tree
 → render
```

可以非常快。

但如果：

```text
Lua 每帧创建几十万个 table
```

或者 editor document model 全部用 Lua 实现，那么一样可以很慢。

所以理想分工仍然是：

```text
Rust:
high-frequency + large-data + rendering primitives

Lua:
low-frequency + orchestration + application behavior
```

那么 `bitter` 的 native startup 确实很可能做到“几乎瞬间”。

---

# 14. 这会不会最后把 Bitty 做成 IDE？

会，但我反而建议 **不要把“IDE”设为最终定义**。

更好的定义是：

> **Terminal-first programmable workspace / application shell**

因为 IDE 只是它可以形成的一种配置。

比如程序员：

```text
Editor
Terminal
Git
AI
Tests
Problems
Docker
```

组合起来：

```text
Bitty → IDE
```

运维：

```text
SSH
Kubernetes
Docker
Logs
Metrics
Shell
```

组合起来：

```text
Bitty → DevOps Workspace
```

普通工作：

```text
Mail
Calendar
Telegram
Notes
Terminal
AI
```

组合起来：

```text
Bitty → Personal Workspace
```

AI 使用：

```text
Agent
Terminal
Diff
Task Board
Logs
Browser
```

组合起来：

```text
Bitty → Agent Workspace
```

所以：

> **Bitty 不需要成为 IDE。Bitty 应该成为“能够构造 IDE 的 Runtime”。**

这和 Emacs 强大的原因其实非常接近：Emacs 不是把 Git、Mail、IDE、Notes 全写进 Core，而是提供足够强且稳定的基本对象，让 Magit、Org、Dired 等应用自然长出来。

---

# 15. 我建议现在按这个顺序推进

这部分我会比较克制，因为现在 Panel Runtime RFC 本身仍明确写着：accepted contract **不等于已经实现的行为**，而首批到底实现 `terminal`，还是再加 `rich/helper/canvas`，目前仍属于 open question。

1. **先完成现有 Panel Runtime**：真正落地 `PanelId / lifecycle / modes / focus / suspend / resume`，不要现在就扩 UI 大系统。

2. **定义 `Activity/Surface` 层**：解决一个 Panel 怎样在 Terminal、Native App、Canvas 等内容之间切换，同时后台 TerminalSession 不死亡。

3. **把 Lua UI 从 Slot UI 扩展为 Widget Tree**：先做 `Text / Row / Column / List / Scroll / Button / Input`，然后再增加 `Tree / Table / Tabs / VirtualList`。

4. **实现 `TextEditor` primitive**：Rust 负责 buffer、cursor、selection、undo、IME、virtualization；然后用 Lua 写一个很小的 `bitter` reference plugin。这会是验证 Native UI Runtime 是否合理的最好项目。

5. **再做 Application Services**：`process / fs / network / tasks / secrets / clipboard / notifications`，严格沿用 capability sandbox。

6. **最后才开放高级 Panel effects**：先保留现在 bounded Core animation；等 Widget Runtime、renderer budget、plugin isolation 稳定以后，再设计 `VisualState + Transition + Effect`，而不是现在开放 raw shader。

---

## 最终我会把 Bitty 的对象模型定成这样

```text
Bitty Instance
│
├── Window
│   │
│   └── Workspace
│       │
│       └── Panel
│           │
│           ├── Presentation
│           │   ├── tiled
│           │   ├── floating
│           │   ├── fullscreen
│           │   ├── scratchpad
│           │   └── pinned
│           │
│           └── Activity
│               ├── Terminal
│               ├── Native App
│               ├── Rich
│               ├── Canvas
│               └── Helper
│
├── UI Runtime
│   ├── Widget Tree
│   ├── Layout
│   ├── Input
│   ├── Focus
│   ├── Text
│   └── Animation
│
├── Application Runtime
│   ├── Commands
│   ├── Events
│   ├── Tasks
│   ├── Services
│   └── Capabilities
│
└── Lua Runtime
    └── Plugins / Applications
```

其中最核心的一句话可以从现在的：

> **Panel is not Terminal**

继续发展成：

> **Panel is a host; Terminal is only one Activity.**

我觉得这句话非常适合成为下一阶段 Panel 架构的原则。它既不会破坏目前已经 accepted 的 Panel RFC，反而能解释你现在提出的 `bitter`、Docker、Mail、Telegram、AI、原生 UI、Shell 保活这些需求为什么应该属于同一套系统。这样发展下去，Bitty 的确会从“可编程终端”自然进入 **可编程 application shell / personal workspace**，而不是靠不断给 terminal emulator 塞功能实现。

[1]: https://github.com/bitty-terminal/bitty "GitHub - bitty-terminal/bitty: Programmable terminal runtime and core implementation · GitHub"
[2]: https://www.gnu.org/s/emacs/manual/html_node/elisp/Buffers.html?utm_source=chatgpt.com "Buffers (GNU Emacs Lisp Reference Manual)"
